# 裝備詞綴×品質×等級門檻掉落系統 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 讓打怪/開寶箱掉落帶「品質階 × 隨機前後綴詞綴」的裝備實例，強力 base 有等級帶門檻（如 lv50–60 才掉），穿上後真的改變戰鬥數值，並在 UI/商店完整呈現。

**Architecture:** 裝備從「單一 id」升級為 `ItemInstance`（base_id + quality + ilvl + rolled affixes / unique）。純資料表（Quality/AffixCatalog/UniqueCatalog/ItemStat/ItemInstance/LootRoller）放 `engine/inventory`，RNG 注入以利可重現與模擬；需要載 `.tres` 的 base 池由 `presentation/inventory/loot_pool.gd` 提供並注入。消耗品維持既有 id-count 堆疊（混合模型）。

**Tech Stack:** Godot 4.7 / GDScript、GUT 測試框架、既有 `engine/`(純邏輯) + `presentation/`(載入呈現) 分層。

## Global Constraints

- **Godot 4.7 / GDScript**。避免 `:=` 用在 Variant 右值（`Dictionary.get`、`load()`、無型別回傳）——改用 `var x: T = ...`，否則 4.7 不編譯。
- **不需向後相容**：存檔直接升 `SaveSerializer.VERSION` 11→12，不寫相容/遷移層；一併更新所有呼叫端與既有 test 資料。舊存檔壞掉可接受。
- **對使用者的說明一律繁體中文**；程式碼/註解/commit 訊息維持既有慣例。
- **UI 一律比例式版面**（anchor 比例、不寫死像素）。
- 純邏輯留 `engine/`、RNG 一律注入；載入/呈現留 `presentation/`。
- **測試指令**（單檔）：`godot --headless --path . -s res://addons/gut/gut_cmdln.gd -gdir=res://tests -ginclude_subdirs -gselect=<test_file.gd> -gexit`
  全套：同上去掉 `-gselect`。GODOT 未在 PATH 時前置 `GODOT=/path/to/godot`。
- **commit 訊息結尾**加：`Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
- 每個 task 最後一步 commit；分支 `feat/equipment-affix-quality-loot`（已建立、spec 已在其上）。

## 共用型別（所有 task 依賴，於 Task 1–3 建立）

```gdscript
# ItemStat.S — 詞綴/裝備可加成的屬性域
enum S { ATTACK, ARMOR, MIGHT, INTELLECT, PERSONALITY, ENDURANCE, SPEED, ACCURACY, LUCK, HP_MAX, SP_MAX }

# Quality.Q — 品質階
enum Q { COMMON, FINE, RARE, LEGENDARY }

# AffixCatalog kind
const PREFIX := 0
const SUFFIX := 1

# ItemInstance 核心方法簽章
base_id: String; quality: int; ilvl: int; affixes: Array; unique_id: String
func base_def() -> ItemDef
func display_name() -> String
func total_attack() -> int
func total_armor() -> int
func total_stat(stat: int) -> int          # ItemStat.S
func weapon_on_hit() -> Dictionary          # {} 或 {kind,potency,duration,chance}
func resist_bonus() -> Dictionary           # {} 或 {Element(int): pct}
func sell_value() -> int
func to_dict() -> Dictionary
static func from_dict(d: Dictionary) -> ItemInstance

# LootRoller
enum Source { OVERWORLD, DUNGEON }
static func roll(bases: Array, monster_level: int, source: int, rng: RandomNumberGenerator) -> ItemInstance  # 可回 null
```

---

# Phase 1：資料模型

### Task 1: `ItemStat` 屬性域 enum + `ItemDef` 擴充欄位

**Files:**
- Create: `engine/inventory/item_stat.gd`
- Modify: `resources/item_def.gd`
- Test: `tests/engine/inventory/test_item_stat.gd`, `tests/resources/test_item_def.gd`（既有，追加）

**Interfaces:**
- Produces: `ItemStat.S`（enum：ATTACK…SP_MAX）、`ItemStat.from_name(String)->int`、`ItemStat.to_name(int)->String`；`ItemDef.min_level/max_level/drop_weight/slot_tag`。

- [ ] **Step 1: 寫失敗測試** `tests/engine/inventory/test_item_stat.gd`

```gdscript
extends GutTest

func test_name_roundtrip():
	assert_eq(ItemStat.to_name(ItemStat.S.MIGHT), "might")
	assert_eq(ItemStat.from_name("might"), ItemStat.S.MIGHT)
	assert_eq(ItemStat.from_name("hp_max"), ItemStat.S.HP_MAX)

func test_from_name_unknown_returns_minus_one():
	assert_eq(ItemStat.from_name("nope"), -1)

func test_item_def_new_fields_default():
	var d := ItemDef.new()
	assert_eq(d.min_level, 1)
	assert_eq(d.max_level, 999)
	assert_eq(d.drop_weight, 0)
	assert_eq(d.slot_tag, "")
```

- [ ] **Step 2: 執行確認失敗**
Run: `godot --headless --path . -s res://addons/gut/gut_cmdln.gd -gdir=res://tests -ginclude_subdirs -gselect=test_item_stat.gd -gexit`
Expected: FAIL（`ItemStat` 未定義 / 欄位不存在）

- [ ] **Step 3: 建立 `engine/inventory/item_stat.gd`**

```gdscript
class_name ItemStat
extends Object

# 詞綴與裝備可加成的屬性域。順序即序列化用的 enum int。
enum S { ATTACK, ARMOR, MIGHT, INTELLECT, PERSONALITY, ENDURANCE, SPEED, ACCURACY, LUCK, HP_MAX, SP_MAX }

const _NAMES := ["attack", "armor", "might", "intellect", "personality", "endurance", "speed", "accuracy", "luck", "hp_max", "sp_max"]

static func to_name(stat: int) -> String:
	if stat < 0 or stat >= _NAMES.size():
		return ""
	return _NAMES[stat]

static func from_name(name: String) -> int:
	return _NAMES.find(name)   # 找不到回 -1
```

- [ ] **Step 4: 擴充 `resources/item_def.gd`** — 在 `stackable` 那行後追加：

```gdscript
@export var min_level: int = 1       # 這個 base 最早出現的怪物等級（程序掉落）
@export var max_level: int = 999     # 最晚出現（強力 base 設窄帶，如 50~60）
@export var drop_weight: int = 0     # 進程序掉落池的權重；0 = 不參與程序掉落
@export var slot_tag: String = ""    # 細分（sword/axe…）；V1 預留、不使用
```

- [ ] **Step 5: 執行確認通過**
Run: `...-gselect=test_item_stat.gd -gexit`；再跑 `...-gselect=test_item_def.gd -gexit`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add engine/inventory/item_stat.gd resources/item_def.gd tests/engine/inventory/test_item_stat.gd && git commit -m "feat(equipment): ItemStat 屬性域 enum + ItemDef 等級帶/權重欄位

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: `Quality` 品質階常數表

**Files:**
- Create: `engine/inventory/quality.gd`
- Test: `tests/engine/inventory/test_quality.gd`

**Interfaces:**
- Produces：`Quality.Q`（COMMON/FINE/RARE/LEGENDARY）、`Quality.affix_count(q, rng)->int`、`Quality.color(q)->Color`、`Quality.value_mult(q)->float`、`Quality.display_name(q)->String`、`Quality.id(q)->String`、`Quality.from_id(String)->int`。

- [ ] **Step 1: 寫失敗測試** `tests/engine/inventory/test_quality.gd`

```gdscript
extends GutTest

func _rng(seed: int) -> RandomNumberGenerator:
	var r := RandomNumberGenerator.new()
	r.seed = seed
	return r

func test_affix_count_bounds():
	assert_eq(Quality.affix_count(Quality.Q.COMMON, _rng(1)), 0)
	assert_eq(Quality.affix_count(Quality.Q.FINE, _rng(1)), 1)
	var rare := Quality.affix_count(Quality.Q.RARE, _rng(7))
	assert_true(rare >= 2 and rare <= 3, "rare 詞綴數 2~3")
	var leg := Quality.affix_count(Quality.Q.LEGENDARY, _rng(7))
	assert_true(leg >= 4 and leg <= 5, "legendary 詞綴數 4~5")

func test_meta():
	assert_eq(Quality.display_name(Quality.Q.RARE), "稀有")
	assert_eq(Quality.id(Quality.Q.LEGENDARY), "legendary")
	assert_eq(Quality.from_id("fine"), Quality.Q.FINE)
	assert_almost_eq(Quality.value_mult(Quality.Q.LEGENDARY), 10.0, 0.001)
```

- [ ] **Step 2: 執行確認失敗** — `...-gselect=test_quality.gd -gexit` → FAIL

- [ ] **Step 3: 建立 `engine/inventory/quality.gd`**

```gdscript
class_name Quality
extends Object

# 品質階常數表（4 階）。仿 MonsterTiers 風格；數值由模擬器調校。
enum Q { COMMON, FINE, RARE, LEGENDARY }

# idx → {id, name, color(hex), affix_min, affix_max, value_mult}
const _Q := [
	{"id": "common",    "name": "普通", "color": "#c8c8c8", "amin": 0, "amax": 0, "mult": 1.0},
	{"id": "fine",      "name": "精良", "color": "#4caf50", "amin": 1, "amax": 1, "mult": 1.5},
	{"id": "rare",      "name": "稀有", "color": "#9c27b0", "amin": 2, "amax": 3, "mult": 3.5},
	{"id": "legendary", "name": "傳說", "color": "#ff9800", "amin": 4, "amax": 5, "mult": 10.0},
]

static func affix_count(q: int, rng: RandomNumberGenerator) -> int:
	var e: Dictionary = _Q[q]
	return rng.randi_range(int(e["amin"]), int(e["amax"]))

static func color(q: int) -> Color:
	return Color(String(_Q[q]["color"]))

static func value_mult(q: int) -> float:
	return float(_Q[q]["mult"])

static func display_name(q: int) -> String:
	return String(_Q[q]["name"])

static func id(q: int) -> String:
	return String(_Q[q]["id"])

static func from_id(s: String) -> int:
	for i in _Q.size():
		if _Q[i]["id"] == s:
			return i
	return Q.COMMON
```

- [ ] **Step 4: 執行確認通過** — `...-gselect=test_quality.gd -gexit` → PASS
- [ ] **Step 5: Commit**

```bash
git add engine/inventory/quality.gd tests/engine/inventory/test_quality.gd && git commit -m "feat(equipment): Quality 品質階常數表（白/綠/紫/橙）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: `AffixCatalog` 詞綴常數表

**Files:**
- Create: `engine/inventory/affix_catalog.gd`
- Test: `tests/engine/inventory/test_affix_catalog.gd`

**Interfaces:**
- Produces：`AffixCatalog.PREFIX/SUFFIX`、`AffixCatalog.get(id)->Dictionary`、`AffixCatalog.eligible(category, ilvl, kind)->Array`（回 id 陣列）、`AffixCatalog.roll_mods(id, rng)->Dictionary`（stat_int→值）、`AffixCatalog.name_of(id)->String`。
- 每筆 entry 結構：`{name, kind, applies_to:Array[int], min_ilvl, mods:{ItemStat.S:[min,max]}, on_hit:{}, resist:{}}`。

- [ ] **Step 1: 寫失敗測試** `tests/engine/inventory/test_affix_catalog.gd`

```gdscript
extends GutTest

func _rng(seed: int) -> RandomNumberGenerator:
	var r := RandomNumberGenerator.new(); r.seed = seed; return r

func test_eligible_filters_by_category_and_ilvl():
	# "sharp"（前綴，武器，min_ilvl 1）在 ilvl 5 的武器合格
	var ids := AffixCatalog.eligible(ItemDef.Category.WEAPON, 5, AffixCatalog.PREFIX)
	assert_true(ids.has("sharp"))
	# 高階詞綴 min_ilvl 高，ilvl 5 不合格
	assert_false(AffixCatalog.eligible(ItemDef.Category.WEAPON, 5, AffixCatalog.PREFIX).has("cruel"))

func test_roll_mods_in_range():
	var mods := AffixCatalog.roll_mods("sharp", _rng(3))
	assert_true(mods.has(ItemStat.S.ATTACK))
	var v: int = mods[ItemStat.S.ATTACK]
	assert_true(v >= 2 and v <= 5)

func test_suffix_kind_and_name():
	var e := AffixCatalog.get("of_the_bear")
	assert_eq(int(e["kind"]), AffixCatalog.SUFFIX)
	assert_eq(AffixCatalog.name_of("of_the_bear"), "·熊之力")
```

- [ ] **Step 2: 執行確認失敗** — `...-gselect=test_affix_catalog.gd -gexit` → FAIL

- [ ] **Step 3: 建立 `engine/inventory/affix_catalog.gd`**（V1 起始詞綴池；Phase 7 擴充）

```gdscript
class_name AffixCatalog
extends Object

# 詞綴常數表。mods 值為 [min,max]，roll 時取區間整數。高階詞綴用較高 min_ilvl 分層。
# stat 鍵用 ItemStat.S（int）。武器詞綴可帶 on_hit；防具詞綴可帶 resist。
const PREFIX := 0
const SUFFIX := 1

# 為簡潔，applies_to 用 ItemDef.Category：WEAPON=0 ARMOR=1 ACCESSORY=2
const _A := {
	# ---- 前綴（PREFIX）----
	"sharp":   {"name": "鋒利的", "kind": PREFIX, "applies_to": [0], "min_ilvl": 1,  "mods": {ItemStat.S.ATTACK: [2, 5]}},
	"heavy":   {"name": "沉重的", "kind": PREFIX, "applies_to": [0], "min_ilvl": 8,  "mods": {ItemStat.S.ATTACK: [4, 9]}},
	"cruel":   {"name": "殘暴的", "kind": PREFIX, "applies_to": [0], "min_ilvl": 30, "mods": {ItemStat.S.ATTACK: [8, 15], ItemStat.S.LUCK: [1, 4]}},
	"sturdy":  {"name": "堅固的", "kind": PREFIX, "applies_to": [1], "min_ilvl": 1,  "mods": {ItemStat.S.ARMOR: [2, 4]}},
	"plated":  {"name": "鑲甲的", "kind": PREFIX, "applies_to": [1], "min_ilvl": 20, "mods": {ItemStat.S.ARMOR: [5, 10]}},
	"blazing": {"name": "燃焰的", "kind": PREFIX, "applies_to": [0], "min_ilvl": 12, "mods": {}, "on_hit": {"kind": StatusEffect.Kind.BURN, "potency": 3, "duration": 3, "chance": 0.35}},
	"venom":   {"name": "淬毒的", "kind": PREFIX, "applies_to": [0], "min_ilvl": 6,  "mods": {}, "on_hit": {"kind": StatusEffect.Kind.POISON, "potency": 2, "duration": 3, "chance": 0.35}},
	# ---- 後綴（SUFFIX）----
	"of_the_bear": {"name": "·熊之力", "kind": SUFFIX, "applies_to": [0, 1, 2], "min_ilvl": 1,  "mods": {ItemStat.S.MIGHT: [2, 6]}},
	"of_haste":    {"name": "·神速",   "kind": SUFFIX, "applies_to": [0, 1, 2], "min_ilvl": 4,  "mods": {ItemStat.S.SPEED: [1, 4]}},
	"of_the_eagle":{"name": "·鷹眼",   "kind": SUFFIX, "applies_to": [0, 2],    "min_ilvl": 4,  "mods": {ItemStat.S.ACCURACY: [2, 5]}},
	"of_fortune":  {"name": "·幸運",   "kind": SUFFIX, "applies_to": [0, 1, 2], "min_ilvl": 10, "mods": {ItemStat.S.LUCK: [2, 6]}},
	"of_vigor":    {"name": "·活力",   "kind": SUFFIX, "applies_to": [1, 2],    "min_ilvl": 8,  "mods": {ItemStat.S.HP_MAX: [6, 15]}},
	"of_the_mind": {"name": "·心智",   "kind": SUFFIX, "applies_to": [2],       "min_ilvl": 8,  "mods": {ItemStat.S.INTELLECT: [2, 6], ItemStat.S.SP_MAX: [3, 8]}},
	"of_warding":  {"name": "·守護",   "kind": SUFFIX, "applies_to": [1, 2],    "min_ilvl": 14, "mods": {}, "resist": {}},  # resist 元素於 Phase 7 補
}

static func get(id: String) -> Dictionary:
	return _A.get(id, {})

static func name_of(id: String) -> String:
	return String(_A.get(id, {}).get("name", id))

static func eligible(category: int, ilvl: int, kind: int) -> Array:
	var out: Array = []
	for id in _A:
		var e: Dictionary = _A[id]
		if int(e["kind"]) != kind:
			continue
		if not (e["applies_to"] as Array).has(category):
			continue
		if int(e["min_ilvl"]) > ilvl:
			continue
		out.append(id)
	return out

# 回傳 {ItemStat.S(int): rolled_int}
static func roll_mods(id: String, rng: RandomNumberGenerator) -> Dictionary:
	var out: Dictionary = {}
	var e: Dictionary = _A.get(id, {})
	var mods: Dictionary = e.get("mods", {})
	for stat in mods:
		var rng_pair: Array = mods[stat]
		out[stat] = rng.randi_range(int(rng_pair[0]), int(rng_pair[1]))
	return out
```

> 注意：`_A` 參照 `StatusEffect.Kind` 常數，`StatusEffect` 已存在（`engine/combat/status_effect.gd`）。

- [ ] **Step 4: 執行確認通過** — `...-gselect=test_affix_catalog.gd -gexit` → PASS
- [ ] **Step 5: Commit**

```bash
git add engine/inventory/affix_catalog.gd tests/engine/inventory/test_affix_catalog.gd && git commit -m "feat(equipment): AffixCatalog 前後綴詞綴常數表 + eligible/roll_mods

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: `UniqueCatalog` 傳說專屬物件表

**Files:**
- Create: `engine/inventory/unique_catalog.gd`
- Test: `tests/engine/inventory/test_unique_catalog.gd`

**Interfaces:**
- Produces：`UniqueCatalog.get(id)->Dictionary`、`UniqueCatalog.eligible(ilvl)->Array`。
- entry：`{name, base_id, min_ilvl, mods:{ItemStat.S:int}, on_hit:{}, resist:{}}`（mods 為固定值，非區間）。

- [ ] **Step 1: 寫失敗測試** `tests/engine/inventory/test_unique_catalog.gd`

```gdscript
extends GutTest

func test_eligible_by_ilvl():
	assert_true(UniqueCatalog.eligible(60).has("dawnblade"))
	assert_false(UniqueCatalog.eligible(5).has("dawnblade"))

func test_entry_shape():
	var e := UniqueCatalog.get("dawnblade")
	assert_eq(String(e["name"]), "弐光之刃")
	assert_eq(String(e["base_id"]), "steel_blade")
	assert_true((e["mods"] as Dictionary).has(ItemStat.S.ATTACK))
```

- [ ] **Step 2: 執行確認失敗** → FAIL

- [ ] **Step 3: 建立 `engine/inventory/unique_catalog.gd`**（V1 3 件；Phase 7 擴充/掛世界觀）

```gdscript
class_name UniqueCatalog
extends Object

# 傳說專屬物件。mods 為固定值。base_id 必須存在於 base 池。
const _U := {
	"dawnblade": {"name": "弐光之刃", "base_id": "steel_blade", "min_ilvl": 45,
		"mods": {ItemStat.S.ATTACK: 18, ItemStat.S.MIGHT: 8, ItemStat.S.LUCK: 5},
		"on_hit": {"kind": StatusEffect.Kind.BURN, "potency": 6, "duration": 3, "chance": 0.5}},
	"bulwark_of_ages": {"name": "萬古壁壘", "base_id": "plate_armor", "min_ilvl": 50,
		"mods": {ItemStat.S.ARMOR: 20, ItemStat.S.ENDURANCE: 8, ItemStat.S.HP_MAX: 30}},
	"whisperwind": {"name": "耳語之風", "base_id": "short_sword", "min_ilvl": 20,
		"mods": {ItemStat.S.ATTACK: 9, ItemStat.S.SPEED: 8, ItemStat.S.ACCURACY: 6}},
}

static func get(id: String) -> Dictionary:
	return _U.get(id, {})

static func eligible(ilvl: int) -> Array:
	var out: Array = []
	for id in _U:
		if int(_U[id]["min_ilvl"]) <= ilvl:
			out.append(id)
	return out
```

- [ ] **Step 4: 執行確認通過** → PASS
- [ ] **Step 5: Commit**

```bash
git add engine/inventory/unique_catalog.gd tests/engine/inventory/test_unique_catalog.gd && git commit -m "feat(equipment): UniqueCatalog 傳說專屬物件表

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: `ItemInstance` 物品實例

**Files:**
- Create: `engine/inventory/item_instance.gd`
- Test: `tests/engine/inventory/test_item_instance.gd`

**Interfaces:**
- Consumes：`ItemDef`（base 解析用一個注入 resolver `func(id)->ItemDef`，避免 engine 直接依賴 presentation ItemCatalog）、`Quality`、`AffixCatalog`、`UniqueCatalog`、`ItemStat`。
- Produces：欄位 `base_id/quality/ilvl/affixes/unique_id`；方法見「共用型別」。
- **base resolver 約定**：`ItemInstance.base_resolver` 為 static `Callable`，預設 `Callable()`（測試可注入假 def）。正式由 Task 16 的 `presentation/inventory/item_catalog.gd` 設定為 `ItemCatalog.get_item`。

- [ ] **Step 1: 寫失敗測試** `tests/engine/inventory/test_item_instance.gd`

```gdscript
extends GutTest

func _sword_def() -> ItemDef:
	var d := ItemDef.new()
	d.id = "iron_sword"; d.display_name = "鐵劍"; d.category = ItemDef.Category.WEAPON
	d.attack = 6; d.value = 30
	return d

func before_each():
	ItemInstance.base_resolver = func(id): return _sword_def() if id == "iron_sword" else null

func after_each():
	ItemInstance.base_resolver = Callable()

func _inst(quality: int, affixes: Array) -> ItemInstance:
	var it := ItemInstance.new()
	it.base_id = "iron_sword"; it.quality = quality; it.ilvl = 20; it.affixes = affixes
	return it

func test_common_uses_base_name_and_attack():
	var it := _inst(Quality.Q.COMMON, [])
	assert_eq(it.display_name(), "鐵劍")
	assert_eq(it.total_attack(), 6)

func test_affix_attack_and_stat_and_name():
	var it := _inst(Quality.Q.RARE, [
		{"id": "sharp", "kind": AffixCatalog.PREFIX, "mods": {ItemStat.S.ATTACK: 4}},
		{"id": "of_the_bear", "kind": AffixCatalog.SUFFIX, "mods": {ItemStat.S.MIGHT: 5}},
	])
	assert_eq(it.total_attack(), 10)                         # 6 + 4
	assert_eq(it.total_stat(ItemStat.S.MIGHT), 5)
	assert_eq(it.display_name(), "鋒利的鐵劍·熊之力")

func test_unique_uses_unique_name_and_mods():
	var it := ItemInstance.new()
	it.base_id = "iron_sword"; it.quality = Quality.Q.LEGENDARY; it.ilvl = 50; it.unique_id = "whisperwind"
	assert_eq(it.display_name(), "耳語之風")
	assert_eq(it.total_stat(ItemStat.S.SPEED), 8)

func test_to_from_dict_roundtrip():
	var it := _inst(Quality.Q.FINE, [{"id": "sharp", "kind": AffixCatalog.PREFIX, "mods": {ItemStat.S.ATTACK: 3}}])
	var back := ItemInstance.from_dict(it.to_dict())
	assert_eq(back.base_id, "iron_sword")
	assert_eq(back.quality, Quality.Q.FINE)
	assert_eq(back.total_attack(), 9)

func test_sell_value_scales_with_quality():
	assert_eq(_inst(Quality.Q.COMMON, []).sell_value(), 30)  # 30 * 1.0
	assert_eq(_inst(Quality.Q.RARE, []).sell_value(), 105)   # 30 * 3.5
```

- [ ] **Step 2: 執行確認失敗** → FAIL

- [ ] **Step 3: 建立 `engine/inventory/item_instance.gd`**

```gdscript
class_name ItemInstance
extends RefCounted

# 一件裝備實例：base_id + quality + ilvl + 已 roll 定的 affixes（或 unique_id）。
# base 解析走注入的 static resolver（避免 engine 直接依賴 presentation ItemCatalog）。
static var base_resolver: Callable = Callable()

var base_id: String = ""
var quality: int = Quality.Q.COMMON
var ilvl: int = 1
var affixes: Array = []      # [{id, kind, mods:{ItemStat.S:int}, on_hit?, resist?}, ...]
var unique_id: String = ""

func base_def() -> ItemDef:
	if base_resolver.is_valid():
		return base_resolver.call(base_id)
	return null

func _unique() -> Dictionary:
	return UniqueCatalog.get(unique_id) if unique_id != "" else {}

func display_name() -> String:
	var u := _unique()
	if not u.is_empty():
		return String(u["name"])
	var def := base_def()
	var base_name: String = def.display_name if def != null else base_id
	var pre := ""
	var suf := ""
	for a in affixes:
		if int(a["kind"]) == AffixCatalog.PREFIX and pre == "":
			pre = AffixCatalog.name_of(String(a["id"]))
		elif int(a["kind"]) == AffixCatalog.SUFFIX and suf == "":
			suf = AffixCatalog.name_of(String(a["id"]))
	return pre + base_name + suf

func total_attack() -> int:
	var def := base_def()
	var base_atk: int = def.attack if def != null else 0
	return base_atk + total_stat(ItemStat.S.ATTACK)

func total_armor() -> int:
	var def := base_def()
	var base_arm: int = def.armor if def != null else 0
	return base_arm + total_stat(ItemStat.S.ARMOR)

# 只加詞綴/unique 的 stat（不含 base attack/armor；後者已在 total_attack/armor 併入）
func total_stat(stat: int) -> int:
	var total := 0
	var u := _unique()
	if not u.is_empty():
		total += int((u["mods"] as Dictionary).get(stat, 0))
	for a in affixes:
		var mods: Dictionary = a.get("mods", {})
		total += int(mods.get(stat, 0))
	return total

func weapon_on_hit() -> Dictionary:
	var u := _unique()
	if not u.is_empty():
		return u.get("on_hit", {})
	for a in affixes:
		var oh: Dictionary = a.get("on_hit", {})
		if not oh.is_empty():
			return oh
	return {}

func resist_bonus() -> Dictionary:
	var out: Dictionary = {}
	var u := _unique()
	if not u.is_empty():
		_merge_resist(out, u.get("resist", {}))
	for a in affixes:
		_merge_resist(out, a.get("resist", {}))
	return out

func _merge_resist(acc: Dictionary, src: Dictionary) -> void:
	for elem in src:
		acc[elem] = int(acc.get(elem, 0)) + int(src[elem])

func sell_value() -> int:
	var def := base_def()
	var base_val: int = def.value if def != null else 0
	return int(round(base_val * Quality.value_mult(quality)))

func to_dict() -> Dictionary:
	# affix mods 的 int key 於 JSON 會變字串；存/讀都在 from_dict 統一還原
	var ax: Array = []
	for a in affixes:
		var mods_out: Dictionary = {}
		for stat in a.get("mods", {}):
			mods_out[str(stat)] = int(a["mods"][stat])
		var entry: Dictionary = {"id": a["id"], "kind": int(a["kind"]), "mods": mods_out}
		if not (a.get("on_hit", {}) as Dictionary).is_empty():
			entry["on_hit"] = a["on_hit"]
		if not (a.get("resist", {}) as Dictionary).is_empty():
			entry["resist"] = a["resist"]
		ax.append(entry)
	return {"base_id": base_id, "quality": quality, "ilvl": ilvl, "affixes": ax, "unique_id": unique_id}

static func from_dict(d: Dictionary) -> ItemInstance:
	var it := ItemInstance.new()
	it.base_id = String(d.get("base_id", ""))
	it.quality = int(d.get("quality", Quality.Q.COMMON))
	it.ilvl = int(d.get("ilvl", 1))
	it.unique_id = String(d.get("unique_id", ""))
	var ax: Array = []
	for a in d.get("affixes", []):
		var mods_in: Dictionary = {}
		for stat_key in a.get("mods", {}):
			mods_in[int(str(stat_key))] = int(a["mods"][stat_key])
		var entry: Dictionary = {"id": String(a["id"]), "kind": int(a["kind"]), "mods": mods_in}
		if a.has("on_hit"):
			entry["on_hit"] = a["on_hit"]
		if a.has("resist"):
			entry["resist"] = a["resist"]
		ax.append(entry)
	it.affixes = ax
	return it
```

- [ ] **Step 4: 執行確認通過** → PASS
- [ ] **Step 5: Commit**

```bash
git add engine/inventory/item_instance.gd tests/engine/inventory/test_item_instance.gd && git commit -m "feat(equipment): ItemInstance（名稱組字/屬性加總/售價/序列化）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

# Phase 2：屬性接通（詞綴真正改變戰鬥）

### Task 6: `Equipment` 改存 `ItemInstance` + `total_stat`

**Files:**
- Modify: `engine/inventory/equipment.gd`
- Test: `tests/engine/inventory/test_equipment.gd`（既有，改寫為實例）

**Interfaces:**
- Consumes：`ItemInstance`。
- Produces：`Equipment.total_stat(stat)->int`；`get_item/equip/unequip/can_equip/slot_for` 改吃 `ItemInstance`；`equipped()->Dictionary`（slot→ItemInstance，取代 `equipped_ids`）。

- [ ] **Step 1: 改寫既有測試** `tests/engine/inventory/test_equipment.gd` — 頂部 helper 改成產生實例：

```gdscript
extends GutTest

func before_all():
	ItemInstance.base_resolver = func(id):
		var d := ItemDef.new(); d.id = id
		if id == "sword": d.category = ItemDef.Category.WEAPON; d.attack = 6
		elif id == "axe": d.category = ItemDef.Category.WEAPON; d.attack = 9
		elif id == "leather": d.category = ItemDef.Category.ARMOR; d.armor = 3
		elif id == "charm": d.category = ItemDef.Category.ACCESSORY; d.armor = 1
		return d

func after_all():
	ItemInstance.base_resolver = Callable()

func _inst(id: String, affixes: Array = []) -> ItemInstance:
	var it := ItemInstance.new(); it.base_id = id; it.affixes = affixes; return it

func test_equip_weapon_sets_slot_and_attack():
	var e := Equipment.new()
	var displaced := e.equip(_inst("sword"))
	assert_null(displaced)
	assert_eq(e.total_attack(), 6)

func test_total_stat_sums_affixes_across_slots():
	var e := Equipment.new()
	e.equip(_inst("sword", [{"id": "of_the_bear", "kind": 1, "mods": {ItemStat.S.MIGHT: 5}}]))
	e.equip(_inst("charm", [{"id": "of_the_bear", "kind": 1, "mods": {ItemStat.S.MIGHT: 3}}]))
	assert_eq(e.total_stat(ItemStat.S.MIGHT), 8)

func test_total_armor_sums_across_slots():
	var e := Equipment.new()
	e.equip(_inst("leather"))
	e.equip(_inst("charm"))
	assert_eq(e.total_armor(), 4)
```
（其餘既有測試依同 pattern 調整；`total_attack/total_armor` 語意不變。）

- [ ] **Step 2: 執行確認失敗** — `...-gselect=test_equipment.gd -gexit` → FAIL

- [ ] **Step 3: 改寫 `engine/inventory/equipment.gd`** — `_slots` 改存 `ItemInstance`，`can_equip/slot_for` 用 `item.base_def().category`，新增 `total_stat`，`total_attack/total_armor` 走實例，`equipped_ids`→`equipped`：

```gdscript
func can_equip(item: ItemInstance) -> bool:
	if item == null:
		return false
	var def := item.base_def()
	return def != null and def.is_equippable()

func slot_for(item: ItemInstance) -> int:
	if not can_equip(item):
		return -1
	return item.base_def().category

func equip(item: ItemInstance) -> ItemInstance:
	var slot := slot_for(item)
	if slot == -1:
		return null
	var prev: ItemInstance = _slots.get(slot, null)
	_slots[slot] = item
	return prev

func total_attack() -> int:
	var t := 0
	for s in _slots:
		t += _slots[s].total_attack()
	return t

func total_armor() -> int:
	var t := 0
	for s in _slots:
		t += _slots[s].total_armor()
	return t

func total_stat(stat: int) -> int:
	var t := 0
	for s in _slots:
		t += _slots[s].total_stat(stat)
	return t

func equipped() -> Dictionary:
	# slot(int) -> ItemInstance，供序列化
	return _slots.duplicate()
```
（`get_item`/`is_equipped`/`unequip` 型別註記由 `ItemDef` 改 `ItemInstance`。）

- [ ] **Step 4: 執行確認通過** — `...-gselect=test_equipment.gd -gexit` → PASS
- [ ] **Step 5: Commit**

```bash
git add engine/inventory/equipment.gd tests/engine/inventory/test_equipment.gd && git commit -m "feat(equipment): Equipment 改存 ItemInstance + total_stat 跨欄加總

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: `Character` effective 屬性接通 + 戰鬥讀取點改用

**Files:**
- Modify: `engine/party/character.gd`, `engine/combat/combat_system.gd:57-59,85-87`, `engine/combat/turn_order.gd`
- Modify: `engine/combat/monster.gd`（加 `effective_speed()`）
- Test: `tests/engine/party/test_character.gd`（既有，追加）, `tests/engine/combat/test_turn_order.gd`（既有，若無則建立）

**Interfaces:**
- Produces：`Character.effective_attr(stat)->int`（含 equipment.total_stat + STAT_MOD status）、`effective_speed()->int`、`effective_luck()->int`、`effective_hp_max()->int`、`effective_sp_max()->int`；`attack_power/armor_value/effective_accuracy` 改用之。
- `Monster.effective_speed()->int`（= `speed`），供 turn_order/命中多型呼叫。

- [ ] **Step 1: 寫失敗測試** 追加到 `tests/engine/party/test_character.gd`

```gdscript
func test_affix_might_flows_into_attack_power():
	ItemInstance.base_resolver = func(id):
		var d := ItemDef.new(); d.id = id; d.category = ItemDef.Category.WEAPON; d.attack = 5; return d
	var c := Character.new()
	c.might = 10; c.speed = 8; c.luck = 3
	var it := ItemInstance.new(); it.base_id = "w"
	it.affixes = [{"id": "of_the_bear", "kind": 1, "mods": {ItemStat.S.MIGHT: 4}},
		{"id": "of_haste", "kind": 1, "mods": {ItemStat.S.SPEED: 2}}]
	c.equipment.equip(it)
	assert_eq(c.attack_power(), 19)          # (10+4 might) + 5 attack
	assert_eq(c.effective_speed(), 10)       # 8 + 2
	ItemInstance.base_resolver = Callable()
```

- [ ] **Step 2: 執行確認失敗** → FAIL

- [ ] **Step 3: 改 `engine/party/character.gd`** — 加 effective 系列，改寫 `attack_power/armor_value/effective_accuracy`：

```gdscript
# ItemStat.S → 對應的 Character base 欄位值
func _base_attr(stat: int) -> int:
	match stat:
		ItemStat.S.MIGHT: return might
		ItemStat.S.INTELLECT: return intellect
		ItemStat.S.PERSONALITY: return personality
		ItemStat.S.ENDURANCE: return endurance
		ItemStat.S.SPEED: return speed
		ItemStat.S.ACCURACY: return accuracy
		ItemStat.S.LUCK: return luck
		ItemStat.S.HP_MAX: return hp_max
		ItemStat.S.SP_MAX: return sp_max
		_: return 0

func effective_attr(stat: int) -> int:
	return _base_attr(stat) + equipment.total_stat(stat)

func effective_speed() -> int:
	return effective_attr(ItemStat.S.SPEED)

func effective_luck() -> int:
	return effective_attr(ItemStat.S.LUCK)

func effective_hp_max() -> int:
	return hp_max + equipment.total_stat(ItemStat.S.HP_MAX)

func effective_sp_max() -> int:
	return sp_max + equipment.total_stat(ItemStat.S.SP_MAX)

func attack_power() -> int:
	return effective_attr(ItemStat.S.MIGHT) + equipment.total_attack() + StatusRules.stat_total(statuses, StatusEffect.Stat.ATTACK)

func armor_value() -> int:
	return equipment.total_armor() + StatusRules.stat_total(statuses, StatusEffect.Stat.ARMOR) + CombatFormulas.defense_from_endurance(effective_attr(ItemStat.S.ENDURANCE))

func effective_accuracy() -> int:
	return effective_attr(ItemStat.S.ACCURACY) + StatusRules.stat_total(statuses, StatusEffect.Stat.ACCURACY)
```
> `equipment.total_attack()` 已含 base attack；`effective_attr(MIGHT)` 已含 MIGHT 詞綴，故不重複。

- [ ] **Step 4: 改 `engine/combat/monster.gd`** — 加（若無）：

```gdscript
func effective_speed() -> int:
	return speed
```

- [ ] **Step 5: 改戰鬥讀取點** —
  `engine/combat/combat_system.gd`：57、85 行 `target.speed` → `target.effective_speed()`；58、86 行 `actor.luck` → `actor.effective_luck()`。
  `engine/combat/turn_order.gd`：兩處 `a["c"].speed`/`b["c"].speed` → `a["c"].effective_speed()`/`b["c"].effective_speed()`。

- [ ] **Step 6: 執行確認通過** — 跑 `test_character.gd`、`test_turn_order.gd`、`test_combat_system.gd`（若存在）→ PASS
- [ ] **Step 7: Commit**

```bash
git add engine/party/character.gd engine/combat/monster.gd engine/combat/combat_system.gd engine/combat/turn_order.gd tests/engine/party/test_character.gd && git commit -m "feat(equipment): 詞綴屬性接通戰鬥（effective_attr/speed/luck，攻防命中先攻爆擊改用）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 8: 武器 on-hit 詞綴接進戰鬥 + hp/sp 上限夾點改 effective

**Files:**
- Modify: `engine/combat/combat_system.gd`（`party_attack` 命中後施加 on-hit）
- Modify: `engine/inventory/item_effects.gd:16,17,26,43,47`、`engine/spell/spell_effects.gd:13,23,28`、`engine/world/vendor_transaction.gd:57,64,65`、`engine/party/portrait_state.gd:14`（`hp_max`→`effective_hp_max()`、`sp_max`→`effective_sp_max()`）
- Test: `tests/engine/combat/test_combat_system.gd`（追加或建立）、`tests/engine/inventory/test_item_effects.gd`（既有，追加）

**Interfaces:**
- Consumes：`ItemInstance.weapon_on_hit()`、`Character.effective_hp_max/sp_max()`、`StatusCatalog.from_data`。
- Produces：隊員普攻命中後，依裝備武器 on-hit 機率對目標施加異常。

- [ ] **Step 1: 寫失敗測試** — 追加 `tests/engine/combat/test_combat_system.gd`

```gdscript
func test_party_attack_applies_weapon_on_hit_status():
	ItemInstance.base_resolver = func(id):
		var d := ItemDef.new(); d.id = id; d.category = ItemDef.Category.WEAPON; d.attack = 50; return d
	var hero := Character.new()
	hero.name = "H"; hero.might = 50; hero.accuracy = 99; hero.speed = 20; hero.hp = 30; hero.hp_max = 30; hero.condition = Character.Condition.OK
	var wep := ItemInstance.new(); wep.base_id = "w"
	wep.affixes = [{"id": "venom", "kind": 0, "mods": {}, "on_hit": {"kind": StatusEffect.Kind.POISON, "potency": 2, "duration": 3, "chance": 1.0}}]
	hero.equipment.equip(wep)
	var mon := Monster.new(); mon.name = "M"; mon.hp = 999; mon.hp_max = 999; mon.speed = 1
	var party := Party.new(); party.members = [hero]
	var rng := RandomNumberGenerator.new(); rng.seed = 1
	var cs := CombatSystem.new(party, [mon], rng)
	cs.party_attack(0)
	var has_poison := false
	for s in mon.statuses:
		if s.kind == StatusEffect.Kind.POISON: has_poison = true
	assert_true(has_poison, "命中後目標中毒")
	ItemInstance.base_resolver = Callable()
```
（Monster 建構若需特定方式，比照既有 `test_combat_system.gd` 內的 helper。）

- [ ] **Step 2: 執行確認失敗** → FAIL

- [ ] **Step 3: 改 `engine/combat/combat_system.gd` `party_attack`** — 在 `target.statuses = StatusRules.cleared_on_hit(...)` 後、`is_alive()` 判斷前插入：

```gdscript
		if actor is Character:
			var oh: Dictionary = _weapon_on_hit(actor)
			if not oh.is_empty() and _rng.randf() <= float(oh["chance"]):
				target.statuses.append(StatusCatalog.from_data(int(oh["kind"]), -1, 0, int(oh["potency"]), int(oh["duration"])))
				events.append("%s 的武器使 %s 陷入異常！" % [actor.name, target.name])
```
並加 helper：

```gdscript
func _weapon_on_hit(c: Character) -> Dictionary:
	var w = c.equipment.get_item(Equipment.Slot.WEAPON)
	return w.weapon_on_hit() if w != null else {}
```

- [ ] **Step 4: 上限夾點改 effective** — 逐檔替換（`target`/`c`/`t` 為 Character 時）：
  - `engine/inventory/item_effects.gd`：16 `target.hp_max`→`target.effective_hp_max()`；17 `target.sp_max`→`target.effective_sp_max()`；26 `target.hp_max`→`target.effective_hp_max()`；43 同；47 `target.sp_max`→`target.effective_sp_max()`。
  - `engine/spell/spell_effects.gd`：13、23、28 的 `target.hp_max`→`target.effective_hp_max()`。
  - `engine/world/vendor_transaction.gd`：57、64 `t.hp_max`→`t.effective_hp_max()`；65 `t.sp_max`→`t.effective_sp_max()`。
  - `engine/party/portrait_state.gd`：14 `c.hp_max`→`c.effective_hp_max()`（兩處）。

- [ ] **Step 5: 執行確認通過** — 跑 `test_combat_system.gd`、`test_item_effects.gd`、`test_spell_effects`（若存在）、`test_vendor_transaction.gd`（若存在）→ PASS
- [ ] **Step 6: Commit**

```bash
git add engine/combat/combat_system.gd engine/inventory/item_effects.gd engine/spell/spell_effects.gd engine/world/vendor_transaction.gd engine/party/portrait_state.gd tests/engine/combat/test_combat_system.gd && git commit -m "feat(equipment): 武器 on-hit 詞綴接戰鬥 + hp/sp 上限夾點改 effective

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

# Phase 3：背包混合化 + 存檔 v12

### Task 9: `Inventory` 加裝備實例集合

**Files:**
- Modify: `engine/inventory/inventory.gd`
- Test: `tests/engine/inventory/test_inventory.gd`（既有，追加）

**Interfaces:**
- Produces：`Inventory.add_instance(inst)`、`remove_instance(inst)->bool`、`instances()->Array`。消耗品 `_stacks` 與既有方法不動。

- [ ] **Step 1: 寫失敗測試** 追加 `tests/engine/inventory/test_inventory.gd`

```gdscript
func test_instances_add_remove():
	var inv := Inventory.new()
	var a := ItemInstance.new(); a.base_id = "iron_sword"
	var b := ItemInstance.new(); b.base_id = "leather"
	inv.add_instance(a); inv.add_instance(b)
	assert_eq(inv.instances().size(), 2)
	assert_true(inv.remove_instance(a))
	assert_eq(inv.instances().size(), 1)
	assert_false(inv.remove_instance(a))   # 已移除，回 false

func test_instances_independent_from_consumable_stacks():
	var inv := Inventory.new()
	inv.add("potion", 3)
	inv.add_instance(ItemInstance.new())
	assert_eq(inv.count_of("potion"), 3)
	assert_eq(inv.instances().size(), 1)
```

- [ ] **Step 2: 執行確認失敗** → FAIL

- [ ] **Step 3: 改 `engine/inventory/inventory.gd`** — 加欄位與方法：

```gdscript
var _instances: Array = []   # Array[ItemInstance]（裝備，不可堆疊）

func add_instance(inst: ItemInstance) -> void:
	if inst != null:
		_instances.append(inst)

func remove_instance(inst: ItemInstance) -> bool:
	var i := _instances.find(inst)
	if i == -1:
		return false
	_instances.remove_at(i)
	return true

func instances() -> Array:
	return _instances
```
（`is_empty()` 若要涵蓋裝備可改 `return _stacks.is_empty() and _instances.is_empty()`；先確認呼叫端語意，預設保持只看消耗品。）

- [ ] **Step 4: 執行確認通過** → PASS
- [ ] **Step 5: Commit**

```bash
git add engine/inventory/inventory.gd tests/engine/inventory/test_inventory.gd && git commit -m "feat(equipment): Inventory 混合化——加裝備實例集合

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 10: 存檔 v12——實例 inline 序列化

**Files:**
- Modify: `engine/save/save_serializer.gd`（`VERSION`、`_inventory_to/from_array`、`_char_to/from_dict` 的 equipment、`_apply_equipment`）
- Test: `tests/engine/save/test_save_serializer_items.gd`（既有，改寫）、`tests/autoload/test_save_system_items.gd`（既有，視需要調整）

**Interfaces:**
- Consumes：`ItemInstance.to_dict/from_dict`、`Inventory.instances/add_instance`、`Equipment.equipped/equip`。
- Produces：save `version = 12`；inventory dict 加 `"instances"`；character dict 的 `"equipment"` 改為 slot→instance-dict。

- [ ] **Step 1: 改寫測試** `tests/engine/save/test_save_serializer_items.gd`

```gdscript
extends GutTest

func before_all():
	ItemInstance.base_resolver = func(id):
		var d := ItemDef.new(); d.id = id; d.category = ItemDef.Category.WEAPON; d.attack = 6; return d

func after_all():
	ItemInstance.base_resolver = Callable()

func test_inventory_instances_roundtrip():
	var data := SaveData.new()
	data.party = Party.new(); data.party.members = []
	data.inventory = Inventory.new()
	data.inventory.add("potion", 2)
	var it := ItemInstance.new(); it.base_id = "iron_sword"; it.quality = Quality.Q.FINE
	it.affixes = [{"id": "sharp", "kind": 0, "mods": {ItemStat.S.ATTACK: 3}}]
	data.inventory.add_instance(it)
	var raw := SaveSerializer.to_dict(data)
	assert_eq(int(raw["version"]), 12)
	var back := SaveSerializer.from_dict(raw)
	assert_eq(back.inventory.count_of("potion"), 2)
	assert_eq(back.inventory.instances().size(), 1)
	assert_eq((back.inventory.instances()[0] as ItemInstance).total_attack(), 9)

func test_equipped_instance_roundtrip():
	var c := Character.new(); c.name = "H"; c.char_class = "knight"; c.level = 1
	var it := ItemInstance.new(); it.base_id = "iron_sword"; it.quality = Quality.Q.RARE
	c.equipment.equip(it)
	var data := SaveData.new(); data.party = Party.new(); data.party.members = [c]; data.inventory = Inventory.new()
	var back := SaveSerializer.from_dict(SaveSerializer.to_dict(data))
	var w = back.party.members[0].equipment.get_item(Equipment.Slot.WEAPON)
	assert_not_null(w)
	assert_eq(w.quality, Quality.Q.RARE)
```

- [ ] **Step 2: 執行確認失敗** → FAIL（version 仍 11 / instances 不存在）

- [ ] **Step 3: 改 `engine/save/save_serializer.gd`**：
  - `const VERSION := 12`
  - `_inventory_to_array` 改名/擴充為同時輸出 stacks + instances。最省事：把 inventory 序列化改成 dict：

```gdscript
static func _inventory_to_dict(inv: Inventory) -> Dictionary:
	if inv == null:
		return {"stacks": [], "instances": []}
	var insts: Array = []
	for it in inv.instances():
		insts.append(it.to_dict())
	return {"stacks": inv.stacks(), "instances": insts}

static func _inventory_from_dict(raw) -> Inventory:
	var inv := Inventory.new()
	if typeof(raw) == TYPE_DICTIONARY:
		inv.load_stacks(raw.get("stacks", []))
		for d in raw.get("instances", []):
			inv.add_instance(ItemInstance.from_dict(d))
	return inv
```
  並把 `to_dict`/`from_dict` 內 `"inventory": _inventory_to_array(...)` 改叫 `_inventory_to_dict`、`_inventory_from_array` 改叫 `_inventory_from_dict`（刪除舊兩個函式）。
  - `_char_to_dict`：`"equipment": c.equipment.equipped_ids()` → 改成 inline 實例：

```gdscript
	var equip_out: Dictionary = {}
	for slot in c.equipment.equipped():
		equip_out[slot] = c.equipment.equipped()[slot].to_dict()
	# 上面兩次 equipped() 呼叫可先存一個 var eq := c.equipment.equipped()
```
  在回傳 dict 用 `"equipment": equip_out`。
  - `_apply_equipment`：改成用 ItemInstance.from_dict + equip（不再需要 resolver 解 id，但 base 顯示仍靠 `ItemInstance.base_resolver`）：

```gdscript
static func _apply_equipment(c: Character, raw, _resolver: Callable) -> void:
	if typeof(raw) != TYPE_DICTIONARY:
		return
	for slot_key in raw:
		var inst := ItemInstance.from_dict(raw[slot_key])
		if c.equipment.can_equip(inst):
			c.equipment.equip(inst)
```
  > `can_equip` 需 `base_resolver` 已設；純序列化測試裡 `before_all` 已注入。

- [ ] **Step 4: 執行確認通過** — 跑 `test_save_serializer_items.gd`、`test_save_system_items.gd` → PASS（後者若因舊格式壞掉，依「不需向後相容」更新其測試資料）
- [ ] **Step 5: Commit**

```bash
git add engine/save/save_serializer.gd tests/engine/save/test_save_serializer_items.gd tests/autoload/test_save_system_items.gd && git commit -m "feat(equipment): save v12——背包/裝備實例 inline 序列化

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 11: 裝備/卸下移動實例 + 卸下夾 hp/sp

**Files:**
- Modify: `engine/inventory/equipment.gd`（或呼叫端）加一個「換裝流程」純函式；建議建立 `engine/inventory/equip_flow.gd`
- Create: `engine/inventory/equip_flow.gd`
- Test: `tests/engine/inventory/test_equip_flow.gd`

**Interfaces:**
- Produces：`EquipFlow.equip(character, inventory, inst)`（把 inst 從 inventory 移到欄位、被換下者回 inventory、夾 hp/sp）、`EquipFlow.unequip(character, inventory, slot)`。

- [ ] **Step 1: 寫失敗測試** `tests/engine/inventory/test_equip_flow.gd`

```gdscript
extends GutTest

func before_all():
	ItemInstance.base_resolver = func(id):
		var d := ItemDef.new(); d.id = id; d.category = ItemDef.Category.ARMOR
		d.affix_hp = 0  # 佔位，勿用
		return d

func after_all():
	ItemInstance.base_resolver = Callable()

func _armor_with_hp(bonus: int) -> ItemInstance:
	var it := ItemInstance.new(); it.base_id = "armor"
	it.affixes = [{"id": "of_vigor", "kind": 1, "mods": {ItemStat.S.HP_MAX: bonus}}]
	return it

func test_equip_moves_instance_from_inventory():
	var c := Character.new(); c.hp = 20; c.hp_max = 20
	var inv := Inventory.new()
	var it := _armor_with_hp(10)
	inv.add_instance(it)
	EquipFlow.equip(c, inv, it)
	assert_eq(inv.instances().size(), 0)
	assert_eq(c.equipment.get_item(Equipment.Slot.ARMOR), it)
	assert_eq(c.effective_hp_max(), 30)

func test_unequip_returns_instance_and_clamps_hp():
	var c := Character.new(); c.hp = 20; c.hp_max = 20
	var inv := Inventory.new()
	var it := _armor_with_hp(10)
	inv.add_instance(it)
	EquipFlow.equip(c, inv, it)
	c.hp = 30                     # 靠裝備上限補到 30
	EquipFlow.unequip(c, inv, Equipment.Slot.ARMOR)
	assert_eq(inv.instances().size(), 1)
	assert_eq(c.hp, 20)          # 卸下後夾回 20
```
> 註：測試 helper 內 `d.affix_hp = 0` 那行是誤植，實作前刪掉——base_resolver 只需回一個 ARMOR 類 ItemDef。

- [ ] **Step 2: 執行確認失敗** → FAIL

- [ ] **Step 3: 建立 `engine/inventory/equip_flow.gd`**

```gdscript
class_name EquipFlow
extends Object

# 換裝流程（純邏輯）：在 Inventory 實例集合與 Character 裝備欄間搬移，並夾 hp/sp。
static func equip(c: Character, inv: Inventory, inst: ItemInstance) -> void:
	if not c.equipment.can_equip(inst):
		return
	inv.remove_instance(inst)
	var prev: ItemInstance = c.equipment.equip(inst)
	if prev != null:
		inv.add_instance(prev)
	_clamp(c)

static func unequip(c: Character, inv: Inventory, slot: int) -> void:
	var prev: ItemInstance = c.equipment.unequip(slot)
	if prev != null:
		inv.add_instance(prev)
	_clamp(c)

static func _clamp(c: Character) -> void:
	c.hp = mini(c.hp, c.effective_hp_max())
	c.sp = mini(c.sp, c.effective_sp_max())
```

- [ ] **Step 4: 執行確認通過** → PASS
- [ ] **Step 5: Commit**

```bash
git add engine/inventory/equip_flow.gd tests/engine/inventory/test_equip_flow.gd && git commit -m "feat(equipment): EquipFlow 換裝流程（搬移實例 + 卸下夾 hp/sp）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

# Phase 4：LootRoller + 掉落整合

### Task 12: `LootRoller` 程序掉落核心

**Files:**
- Create: `engine/inventory/loot_roller.gd`
- Test: `tests/engine/inventory/test_loot_roller.gd`

**Interfaces:**
- Consumes：注入的 `bases: Array[ItemDef]`（帶 min_level/max_level/drop_weight/category）、`Quality`、`AffixCatalog`、`UniqueCatalog`。
- Produces：`LootRoller.Source`、`LootRoller.roll(bases, monster_level, source, rng)->ItemInstance`（無合格 base 回 null）、`LootRoller.tier_for(level)->int`、`LootRoller.roll_quality(tier, source, rng)->int`。

- [ ] **Step 1: 寫失敗測試** `tests/engine/inventory/test_loot_roller.gd`

```gdscript
extends GutTest

func before_all():
	ItemInstance.base_resolver = func(id):
		var d := ItemDef.new(); d.id = id; d.category = ItemDef.Category.WEAPON; d.attack = 6; return d

func after_all():
	ItemInstance.base_resolver = Callable()

func _base(id: String, lo: int, hi: int, w: int) -> ItemDef:
	var d := ItemDef.new()
	d.id = id; d.category = ItemDef.Category.WEAPON; d.attack = 6
	d.min_level = lo; d.max_level = hi; d.drop_weight = w
	return d

func _rng(seed: int) -> RandomNumberGenerator:
	var r := RandomNumberGenerator.new(); r.seed = seed; return r

func test_only_bases_in_level_band_are_picked():
	var bases := [_base("low", 1, 10, 10), _base("high", 50, 60, 10)]
	# lv55 只可能抽到 high
	for s in range(20):
		var it := LootRoller.roll(bases, 55, LootRoller.Source.OVERWORLD, _rng(s))
		if it != null:
			assert_eq(it.base_id, "high")

func test_tier_for():
	assert_eq(LootRoller.tier_for(1), 1)
	assert_eq(LootRoller.tier_for(10), 1)
	assert_eq(LootRoller.tier_for(11), 2)
	assert_eq(LootRoller.tier_for(55), 6)
	assert_eq(LootRoller.tier_for(999), 10)

func test_affix_count_matches_quality():
	var bases := [_base("w", 1, 100, 10)]
	# 用固定 quality 的直路：直接檢查 roll 出來的實例詞綴數不超過品質上限
	var it := LootRoller.roll(bases, 30, LootRoller.Source.OVERWORLD, _rng(3))
	assert_not_null(it)
	if it.quality == Quality.Q.COMMON:
		assert_eq(it.affixes.size(), 0)
	assert_true(it.ilvl == 30)

func test_returns_null_when_no_eligible_base():
	var bases := [_base("high", 50, 60, 10)]
	assert_null(LootRoller.roll(bases, 5, LootRoller.Source.OVERWORLD, _rng(1)))
```

- [ ] **Step 2: 執行確認失敗** → FAIL

- [ ] **Step 3: 建立 `engine/inventory/loot_roller.gd`**

```gdscript
class_name LootRoller
extends Object

# 程序掉落核心（純函式，RNG 注入）。bases 由呼叫端（presentation/LootPool）注入。
enum Source { OVERWORLD, DUNGEON }

# tier(1..10) × 品質權重 [common, fine, rare, legendary]。source=DUNGEON 額外上移。
const _QW := [
	[85, 14, 1, 0],    # T1
	[78, 19, 3, 0],    # T2
	[70, 24, 6, 0],    # T3
	[62, 29, 8, 1],    # T4
	[54, 33, 11, 2],   # T5
	[46, 37, 15, 2],   # T6
	[38, 40, 19, 3],   # T7
	[30, 42, 24, 4],   # T8
	[22, 43, 30, 5],   # T9
	[15, 42, 36, 7],   # T10
]
const MAX_PREFIX := 3
const MAX_SUFFIX := 3

static func tier_for(level: int) -> int:
	return clampi(int(ceil(level / 10.0)), 1, 10)

static func roll_quality(tier: int, source: int, rng: RandomNumberGenerator) -> int:
	var w: Array = (_QW[tier - 1]).duplicate()
	if source == Source.DUNGEON:
		# 地城：把權重往高品質搬（common→fine、fine→rare、rare→legendary 各挪一部分）
		var shift_c: int = w[0] / 3
		var shift_f: int = w[1] / 4
		var shift_r: int = w[2] / 4
		w[0] -= shift_c; w[1] += shift_c - shift_f
		w[2] += shift_f - shift_r; w[3] += shift_r
	var total := 0
	for x in w: total += int(x)
	var pick := rng.randi_range(1, maxi(1, total))
	var acc := 0
	for q in w.size():
		acc += int(w[q])
		if pick <= acc:
			return q
	return Quality.Q.COMMON

static func _pick_base(bases: Array, level: int, rng: RandomNumberGenerator) -> ItemDef:
	var pool: Array = []
	var total := 0
	for d in bases:
		if d.drop_weight > 0 and d.min_level <= level and level <= d.max_level:
			pool.append(d); total += d.drop_weight
	if pool.is_empty():
		return null
	var pick := rng.randi_range(1, total)
	var acc := 0
	for d in pool:
		acc += d.drop_weight
		if pick <= acc:
			return d
	return pool[0]

static func roll(bases: Array, monster_level: int, source: int, rng: RandomNumberGenerator) -> ItemInstance:
	var def := _pick_base(bases, monster_level, rng)
	if def == null:
		return null
	var ilvl := clampi(monster_level, 1, 100)
	var tier := tier_for(monster_level)
	var quality := roll_quality(tier, source, rng)
	var it := ItemInstance.new()
	it.base_id = def.id
	it.ilvl = ilvl
	it.quality = quality
	if quality == Quality.Q.LEGENDARY:
		var uids: Array = UniqueCatalog.eligible(ilvl)
		if not uids.is_empty():
			it.unique_id = uids[rng.randi_range(0, uids.size() - 1)]
			return it
		quality = Quality.Q.RARE   # 無合格 unique → 降級為稀有
		it.quality = quality
	it.affixes = _roll_affixes(def.category, ilvl, Quality.affix_count(quality, rng), rng)
	return it

static func _roll_affixes(category: int, ilvl: int, count: int, rng: RandomNumberGenerator) -> Array:
	var out: Array = []
	var used: Dictionary = {}
	var n_pre := 0
	var n_suf := 0
	var attempts := 0
	while out.size() < count and attempts < count * 8:
		attempts += 1
		var kind := AffixCatalog.PREFIX if rng.randi_range(0, 1) == 0 else AffixCatalog.SUFFIX
		if kind == AffixCatalog.PREFIX and n_pre >= MAX_PREFIX:
			kind = AffixCatalog.SUFFIX
		if kind == AffixCatalog.SUFFIX and n_suf >= MAX_SUFFIX:
			kind = AffixCatalog.PREFIX
		var ids: Array = AffixCatalog.eligible(category, ilvl, kind)
		# 過濾已用
		var fresh: Array = []
		for id in ids:
			if not used.has(id): fresh.append(id)
		if fresh.is_empty():
			continue
		var chosen: String = fresh[rng.randi_range(0, fresh.size() - 1)]
		used[chosen] = true
		var e: Dictionary = AffixCatalog.get(chosen)
		var entry: Dictionary = {"id": chosen, "kind": kind, "mods": AffixCatalog.roll_mods(chosen, rng)}
		if not (e.get("on_hit", {}) as Dictionary).is_empty():
			entry["on_hit"] = e["on_hit"]
		if not (e.get("resist", {}) as Dictionary).is_empty():
			entry["resist"] = e["resist"]
		out.append(entry)
		if kind == AffixCatalog.PREFIX: n_pre += 1
		else: n_suf += 1
	return out
```

- [ ] **Step 4: 執行確認通過** → PASS
- [ ] **Step 5: Commit**

```bash
git add engine/inventory/loot_roller.gd tests/engine/inventory/test_loot_roller.gd && git commit -m "feat(equipment): LootRoller 程序掉落核心（等級帶 base + tier 品質權重 + 詞綴 roll）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 13: `LootPool` base 池 + `MonsterDef.gear_drop_chance` + `LootSystem` 擴充

**Files:**
- Create: `presentation/inventory/loot_pool.gd`
- Modify: `resources/monster_def.gd`（加 `gear_drop_chance`）
- Modify: `engine/combat/loot_system.gd`
- Test: `tests/presentation/test_loot_pool.gd`、`tests/engine/combat/test_loot_system.gd`（既有，改寫）

**Interfaces:**
- Produces：`LootPool.equipment_bases()->Array[ItemDef]`（列舉 ItemCatalog 中 drop_weight>0 的 base）、`LootPool.gear_drop_chance(monster)->float`；`MonsterDef.gear_drop_chance:float=-1`；`LootSystem.roll_drops(monsters, source, bases, rng)->Dictionary{item_ids, instances}`。

- [ ] **Step 1: 寫失敗測試** `tests/engine/combat/test_loot_system.gd`（改寫）

```gdscript
extends GutTest

func before_all():
	ItemInstance.base_resolver = func(id):
		var d := ItemDef.new(); d.id = id; d.category = ItemDef.Category.WEAPON; d.attack = 6; return d

func after_all():
	ItemInstance.base_resolver = Callable()

func _mon(level: int, drop_id: String, drop_chance: float, gear: float) -> Monster:
	var d := MonsterDef.new()
	d.id = "m"; d.level = level; d.drop_item_id = drop_id; d.drop_chance = drop_chance; d.gear_drop_chance = gear
	return Monster.from_def(d)

func _base(id: String, w: int) -> ItemDef:
	var d := ItemDef.new(); d.id = id; d.category = ItemDef.Category.WEAPON
	d.min_level = 1; d.max_level = 100; d.drop_weight = w; d.attack = 6
	return d

func test_fixed_consumable_drop_still_works():
	var rng := RandomNumberGenerator.new(); rng.seed = 1
	var res := LootSystem.roll_drops([_mon(5, "potion", 1.0, 0.0)], LootRoller.Source.OVERWORLD, [], rng)
	assert_true((res["item_ids"] as Array).has("potion"))
	assert_eq((res["instances"] as Array).size(), 0)

func test_gear_drop_produces_instance():
	var rng := RandomNumberGenerator.new(); rng.seed = 2
	var res := LootSystem.roll_drops([_mon(30, "", 0.0, 1.0)], LootRoller.Source.OVERWORLD, [_base("w", 10)], rng)
	assert_eq((res["instances"] as Array).size(), 1)
```

- [ ] **Step 2: 執行確認失敗** → FAIL

- [ ] **Step 3: 改 `resources/monster_def.gd`** — 加：

```gdscript
@export var gear_drop_chance: float = -1.0   # -1 = 用 tier 預設
```

- [ ] **Step 4: 改 `engine/combat/loot_system.gd`**

```gdscript
class_name LootSystem
extends Object

# tier → 未指定時的裝備掉落機率預設（由模擬器調校）
const _GEAR_DEFAULT := [0.06, 0.07, 0.08, 0.09, 0.10, 0.11, 0.12, 0.13, 0.14, 0.15]

static func _gear_chance(m) -> float:
	if m.gear_drop_chance >= 0.0:
		return m.gear_drop_chance
	return _GEAR_DEFAULT[LootRoller.tier_for(m.level) - 1]

# 回傳 {item_ids: Array[String], instances: Array[ItemInstance]}
static func roll_drops(monsters: Array, source: int, bases: Array, rng: RandomNumberGenerator) -> Dictionary:
	var ids: Array = []
	var insts: Array = []
	for m in monsters:
		if m.drop_item_id != "" and rng.randf() < m.drop_chance:
			ids.append(m.drop_item_id)
		if rng.randf() < _gear_chance(m):
			var it := LootRoller.roll(bases, m.level, source, rng)
			if it != null:
				insts.append(it)
	return {"item_ids": ids, "instances": insts}
```
> `Monster` 需能讀 `gear_drop_chance`/`level`。確認 `Monster.from_def` 有帶 `level` 與 `gear_drop_chance`；若無，於 `engine/combat/monster.gd` 的 `from_def` 補上 `m.level = def.level` / `m.gear_drop_chance = def.gear_drop_chance`（並在 Monster 加對應欄位）。

- [ ] **Step 5: 建立 `presentation/inventory/loot_pool.gd`**

```gdscript
class_name LootPool
extends Object

# 列舉 ItemCatalog 中可程序掉落（drop_weight>0）的 base ItemDef。
static func equipment_bases() -> Array:
	var out: Array = []
	for id in ItemCatalog.all_ids():
		var d: ItemDef = ItemCatalog.get_item(id)
		if d != null and d.drop_weight > 0:
			out.append(d)
	return out
```

- [ ] **Step 6: 寫 `tests/presentation/test_loot_pool.gd`**

```gdscript
extends GutTest

func test_equipment_bases_only_droppable():
	var bases := LootPool.equipment_bases()
	for d in bases:
		assert_true(d.drop_weight > 0)
```
（Phase 7 上內容後此測試才有非空資料；此處僅驗證篩選條件。）

- [ ] **Step 7: 執行確認通過** → PASS
- [ ] **Step 8: Commit**

```bash
git add resources/monster_def.gd engine/combat/loot_system.gd engine/combat/monster.gd presentation/inventory/loot_pool.gd tests/engine/combat/test_loot_system.gd tests/presentation/test_loot_pool.gd && git commit -m "feat(equipment): LootSystem 擴充固定+程序掉落 + MonsterDef.gear_drop_chance + LootPool

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 14: `main.gd` 掉落接線 + 來源旗標

**Files:**
- Modify: `presentation/world/main.gd`（`_grant_drops`、combat 起始帶 source）
- Modify: `presentation/inventory/item_catalog.gd`（設定 `ItemInstance.base_resolver`）
- Test: 手動 gate（`./run.sh`）；純邏輯已由 Task 12/13 覆蓋。

**Interfaces:**
- Consumes：`LootSystem.roll_drops`、`LootPool.equipment_bases`、`GameState.inventory.add_instance`、`ItemInstance.display_name`、`Quality`.
- Produces：勝利後掉落裝備進背包 + 品質色 toast；`ItemInstance.base_resolver` 全域就緒。

- [ ] **Step 1: 設定 base_resolver** — 在 `presentation/inventory/item_catalog.gd` 末尾加一個靜態初始化入口，並於 `autoload/game_state.gd` 的 `_ready()` 首行呼叫：

  `item_catalog.gd` 加：
```gdscript
static func install_resolver() -> void:
	ItemInstance.base_resolver = func(id): return ItemCatalog.get_item(id)
```
  `game_state.gd` `_ready()` 最前面加：`ItemCatalog.install_resolver()`

- [ ] **Step 2: 改 `main.gd` 判定 source** — 加 helper：

```gdscript
func _combat_source() -> int:
	var theme: String = MapManager.current_map.theme_id if MapManager.current_map != null else ""
	return LootRoller.Source.DUNGEON if theme == "dungeon" else LootRoller.Source.OVERWORLD
```

- [ ] **Step 3: 改 `main.gd::_grant_drops`**

```gdscript
func _grant_drops() -> void:
	var rng := RandomNumberGenerator.new()
	rng.randomize()
	var res: Dictionary = LootSystem.roll_drops(_combat.monsters, _combat_source(), LootPool.equipment_bases(), rng)
	for id in res["item_ids"]:
		GameState.inventory.add(id, 1)
		var item := ItemCatalog.get_item(id)
		var label: String = item.display_name if item != null else String(id)
		GameState.message_log.push("獲得道具：%s" % label)
	for inst in res["instances"]:
		GameState.inventory.add_instance(inst)
		GameState.message_log.push("獲得裝備：[%s] %s (ilvl%d)" % [Quality.display_name(inst.quality), inst.display_name(), inst.ilvl])
```

- [ ] **Step 4: 啟動煙霧測試**
Run: `./run.sh --headless`
Expected: 乾淨啟動、無 parse/load 錯誤。

- [ ] **Step 5: Commit**

```bash
git add presentation/world/main.gd presentation/inventory/item_catalog.gd autoload/game_state.gd && git commit -m "feat(equipment): 掉落接線——勝利掉裝進背包 + 品質色 toast + 地城來源旗標

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

# Phase 5：UI

### Task 15: `ItemDisplay` 呈現輔助（名稱色/tooltip 文字）

**Files:**
- Create: `presentation/inventory/item_display.gd`
- Test: `tests/presentation/test_item_display.gd`

**Interfaces:**
- Produces：`ItemDisplay.colored_name(inst)->String`（BBCode，品質色）、`ItemDisplay.detail_lines(inst)->Array[String]`（base 數值 + 逐條詞綴 + ilvl + 售價）、`ItemDisplay.stat_label(stat)->String`。

- [ ] **Step 1: 寫失敗測試** `tests/presentation/test_item_display.gd`

```gdscript
extends GutTest

func before_all():
	ItemInstance.base_resolver = func(id):
		var d := ItemDef.new(); d.id = id; d.display_name = "鐵劍"; d.category = ItemDef.Category.WEAPON; d.attack = 6; d.value = 30; return d

func after_all():
	ItemInstance.base_resolver = Callable()

func test_colored_name_wraps_bbcode_color():
	var it := ItemInstance.new(); it.base_id = "iron_sword"; it.quality = Quality.Q.RARE
	var s := ItemDisplay.colored_name(it)
	assert_true(s.begins_with("[color=#"))
	assert_true(s.find("鐵劍") != -1)

func test_detail_lines_list_affixes():
	var it := ItemInstance.new(); it.base_id = "iron_sword"; it.quality = Quality.Q.FINE
	it.ilvl = 20
	it.affixes = [{"id": "of_the_bear", "kind": 1, "mods": {ItemStat.S.MIGHT: 5}}]
	var lines := ItemDisplay.detail_lines(it)
	var joined := "\n".join(lines)
	assert_true(joined.find("力量 +5") != -1)
	assert_true(joined.find("ilvl 20") != -1)
```

- [ ] **Step 2: 執行確認失敗** → FAIL

- [ ] **Step 3: 建立 `presentation/inventory/item_display.gd`**

```gdscript
class_name ItemDisplay
extends Object

const _STAT_LABEL := {
	ItemStat.S.ATTACK: "攻擊", ItemStat.S.ARMOR: "防禦", ItemStat.S.MIGHT: "力量",
	ItemStat.S.INTELLECT: "智力", ItemStat.S.PERSONALITY: "魅力", ItemStat.S.ENDURANCE: "耐力",
	ItemStat.S.SPEED: "速度", ItemStat.S.ACCURACY: "命中", ItemStat.S.LUCK: "幸運",
	ItemStat.S.HP_MAX: "HP上限", ItemStat.S.SP_MAX: "SP上限",
}

static func stat_label(stat: int) -> String:
	return String(_STAT_LABEL.get(stat, ItemStat.to_name(stat)))

static func colored_name(inst: ItemInstance) -> String:
	return "[color=%s]%s[/color]" % [Quality.color(inst.quality).to_html(false), inst.display_name()]

static func detail_lines(inst: ItemInstance) -> Array:
	var lines: Array = []
	var def := inst.base_def()
	if def != null and def.attack != 0:
		lines.append("基礎攻擊 %d" % def.attack)
	if def != null and def.armor != 0:
		lines.append("基礎防禦 %d" % def.armor)
	# 逐條詞綴（unique 直接列 mods）
	for a in inst.affixes:
		for stat in a.get("mods", {}):
			lines.append("%s +%d" % [stat_label(stat), int(a["mods"][stat])])
		var oh: Dictionary = a.get("on_hit", {})
		if not oh.is_empty():
			lines.append("命中時：%d%% 施加異常" % int(round(float(oh["chance"]) * 100.0)))
	if inst.unique_id != "":
		var u := UniqueCatalog.get(inst.unique_id)
		for stat in (u.get("mods", {}) as Dictionary):
			lines.append("%s +%d" % [stat_label(stat), int(u["mods"][stat])])
	lines.append("ilvl %d　售價 %d" % [inst.ilvl, inst.sell_value()])
	return lines
```

- [ ] **Step 4: 執行確認通過** → PASS
- [ ] **Step 5: Commit**

```bash
git add presentation/inventory/item_display.gd tests/presentation/test_item_display.gd && git commit -m "feat(equipment): ItemDisplay 呈現輔助（品質色名稱 + 詞綴明細行）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 16: 角色/背包畫面顯示裝備實例 + 換裝 + 比較

**Files:**
- Modify: `presentation/ui/character/items_view.gd`、`items_tab.gd`、`items_confirm_dialog.gd`（依實際結構）
- Test: `tests/presentation/character/test_items_view.gd`（既有，追加）

**Interfaces:**
- Consumes：`GameState.inventory.instances()`、`ItemDisplay`、`EquipFlow`、`Equipment.get_item`。
- Produces：背包列出裝備實例（品質色名稱）、選取顯示明細與「與當前裝備的 stat delta」、確認後 `EquipFlow.equip` 換裝。

> **執行提示**：先 Read 這三個檔案掌握既有清單/選取/確認流程，再把「資料源」從 `ItemCatalog.get_item(id)` 一路換成 `GameState.inventory.instances()` 的 `ItemInstance`，名稱經 `ItemDisplay.colored_name`、明細經 `ItemDisplay.detail_lines`、確認動作改叫 `EquipFlow.equip(character, GameState.inventory, inst)`。

- [ ] **Step 1: 寫/追加測試** — 針對可純邏輯驗證的部分（如「背包實例對應到列表項數」與「換裝後欄位更新」），追加 `tests/presentation/character/test_items_view.gd`：

```gdscript
func test_equip_from_inventory_updates_slot_and_removes_from_bag():
	ItemInstance.base_resolver = func(id):
		var d := ItemDef.new(); d.id = id; d.category = ItemDef.Category.WEAPON; d.attack = 7; return d
	var c := Character.new(); c.hp = 10; c.hp_max = 10
	var inv := Inventory.new()
	var it := ItemInstance.new(); it.base_id = "iron_sword"
	inv.add_instance(it)
	EquipFlow.equip(c, inv, it)          # 模擬確認換裝路徑
	assert_eq(c.equipment.total_attack(), 7)
	assert_eq(inv.instances().size(), 0)
	ItemInstance.base_resolver = Callable()
```
（若既有測試以 harness 驗證 UI 節點，遵其 pattern；核心換裝邏輯已由 `EquipFlow` 測試覆蓋，此處僅補接線。）

- [ ] **Step 2: 執行確認失敗**（若為新斷言）→ FAIL

- [ ] **Step 3: 改三個 UI 檔** — 依上「執行提示」把資料源與動作改為實例流：
  - 清單項文字：`RichTextLabel`/`bbcode_enabled=true` + `ItemDisplay.colored_name(inst)`（比例式版面不動）。
  - 明細面板：`ItemDisplay.detail_lines(inst)`。
  - 比較：對選取 inst 與角色當前同 slot 裝備，逐 stat 顯示 `+/-`（用 `inst.total_stat` / `total_attack` / `total_armor` 對比 `current`；current 為 null 時全部視為 0）。
  - 確認：`EquipFlow.equip(character, GameState.inventory, inst)`；換下者自動回背包。

- [ ] **Step 4: 執行確認通過** + `./run.sh --headless` 乾淨啟動 → PASS
- [ ] **Step 5: Commit**

```bash
git add presentation/ui/character/ tests/presentation/character/test_items_view.gd && git commit -m "feat(equipment): 角色/背包畫面顯示裝備實例 + 換裝 + stat 比較

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 17: 隊伍 HUD / 掉落 toast 品質色

**Files:**
- Modify: 掉落 toast 已於 Task 14 帶品質名；此 task 讓 toast/HUD 顯示品質**色**（若 message_log/HUD 支援 BBCode）。
- Modify: `presentation/world/main.gd`（toast 用 `ItemDisplay.colored_name`）、HUD 顯示裝備處（若有）
- Test: 手動 gate。

- [ ] **Step 1: 改 toast** — `main.gd::_grant_drops` 裝備那行改：

```gdscript
		GameState.message_log.push("獲得裝備：%s (ilvl%d)" % [ItemDisplay.colored_name(inst), inst.ilvl])
```
  確認 message_log 呈現端 `bbcode_enabled = true`；若否，在該 Label 開啟。

- [ ] **Step 2: `./run.sh --headless`** 乾淨啟動；`./run.sh` 人工確認掉落訊息帶色。
- [ ] **Step 3: Commit**

```bash
git add presentation/world/main.gd presentation/ && git commit -m "feat(equipment): 掉落 toast/HUD 顯示品質色

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

# Phase 6：商店實例化

### Task 18: 商店買賣裝備實例

**Files:**
- Modify: `engine/world/vendor_transaction.gd`、`presentation/world/vendor_catalog.gd`、`presentation/ui/vendor_overlay.gd`
- Test: `tests/engine/world/test_vendor_transaction.gd`（既有或新建，追加）

**Interfaces:**
- Consumes：`ItemInstance`、`EquipFlow`（不需）、`Inventory.add_instance/instances`、`sell_value()`.
- Produces：買基礎裝備→生成 COMMON 實例進背包；賣玩家實例→依 `sell_value()` 給金幣並移出背包。

> **執行提示**：先 Read `vendor_transaction.gd` 掌握既有 buy/sell 對消耗品的處理，再加「裝備」分支。買基礎裝備＝`var it := ItemInstance.new(); it.base_id = base_id; it.quality = Quality.Q.COMMON; it.ilvl = 1; inv.add_instance(it)`。賣＝找到該 instance、`gold += it.sell_value()`、`inv.remove_instance(it)`。

- [ ] **Step 1: 寫失敗測試** `tests/engine/world/test_vendor_transaction.gd`（追加）

```gdscript
func test_buy_base_equipment_adds_common_instance():
	ItemInstance.base_resolver = func(id):
		var d := ItemDef.new(); d.id = id; d.category = ItemDef.Category.WEAPON; d.attack = 6; d.value = 30; return d
	var inv := Inventory.new()
	var it := VendorTransaction.make_base_instance("iron_sword")
	inv.add_instance(it)
	assert_eq(it.quality, Quality.Q.COMMON)
	assert_eq(inv.instances().size(), 1)
	ItemInstance.base_resolver = Callable()

func test_sell_instance_gives_quality_scaled_gold():
	ItemInstance.base_resolver = func(id):
		var d := ItemDef.new(); d.id = id; d.category = ItemDef.Category.WEAPON; d.value = 30; return d
	var it := ItemInstance.new(); it.base_id = "iron_sword"; it.quality = Quality.Q.RARE
	assert_eq(it.sell_value(), 105)   # 30 * 3.5
	ItemInstance.base_resolver = Callable()
```

- [ ] **Step 2: 執行確認失敗** → FAIL

- [ ] **Step 3: 加 `engine/world/vendor_transaction.gd`** 靜態工廠 + sell 分支：

```gdscript
static func make_base_instance(base_id: String) -> ItemInstance:
	var it := ItemInstance.new()
	it.base_id = base_id
	it.quality = Quality.Q.COMMON
	it.ilvl = 1
	return it
```
  並在既有 buy/sell 流程加裝備分支（買呼叫 `make_base_instance` 並 `inv.add_instance`；賣用 `it.sell_value()` + `inv.remove_instance`）。

- [ ] **Step 4: 改 `vendor_catalog.gd` / `vendor_overlay.gd`** — 上架的裝備品項改以 base_id 呈現（買價＝base value），玩家出售列表列出 `inv.instances()`（品質色名 + `sell_value()`）。

- [ ] **Step 5: 執行確認通過** + `./run.sh --headless` → PASS
- [ ] **Step 6: Commit**

```bash
git add engine/world/vendor_transaction.gd presentation/world/vendor_catalog.gd presentation/ui/vendor_overlay.gd tests/engine/world/test_vendor_transaction.gd && git commit -m "feat(equipment): 商店買基礎裝備生成 COMMON 實例 + 賣依品質售價

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

# Phase 7：內容 + 平衡

### Task 19: base 裝備池內容（.tres）

**Files:**
- Create：新武器/防具/飾品 `.tres`（見下）；補既有 6 件的 `min_level/max_level/drop_weight`
- Modify: `presentation/inventory/item_catalog.gd`（登錄新 id）
- Test: `tests/presentation/test_item_catalog.gd`（既有，追加覆蓋斷言）

**Interfaces:**
- Produces：跨等級帶的可掉落 base 池（武器 ~8–10、防具 ~6–8、飾品 ~3–5），每件標 `min_level/max_level/drop_weight`。

- [ ] **Step 1: 補既有 6 件掉落欄位** — 於各 `.tres` `[resource]` 區塊加（示例 `content/items/short_sword.tres`）：
```
min_level = 1
max_level = 20
drop_weight = 10
```
  依強度給不同帶：iron_sword `min_level=8 max_level=35 drop_weight=8`；leather_armor `1/18/10`；chain_mail `12/40/8`；buckler `1/25/8`；lucky_charm `1/60/5`。

- [ ] **Step 2: 新增 base `.tres`** — 每個仿 `short_sword.tres` 格式（`script_class="ItemDef"`）。範例 `content/items/steel_blade.tres`：
```
[gd_resource type="Resource" script_class="ItemDef" load_steps=2 format=3]
[ext_resource type="Script" path="res://resources/item_def.gd" id="1_item"]
[resource]
script = ExtResource("1_item")
id = "steel_blade"
display_name = "鋼刃劍"
category = 0
attack = 14
value = 120
min_level = 20
max_level = 55
drop_weight = 7
```
  依此建立（欄位隨強度遞增）：
  - 武器：`wood_club`(atk3,1/10,10)、`hand_axe`(atk8,5/25,8)、`steel_blade`(14,20/55,7)、`war_hammer`(20,35/70,6)、`mithril_edge`(26,50/80,4)、`dragonbone_sword`(34,70/100,3)、`plate_armor` 不是武器→歸防具。
  - 防具：`cloth_robe`(arm1,1/12,10)、`plate_armor`(arm12,30/70,6)、`mithril_mail`(arm18,55/90,4)、`dragon_scale`(arm24,75/100,3)。
  - 飾品：`iron_ring`(1/40,5)、`amulet_of_power`(20/80,4)。
  （數值可再由模擬器調；`plate_armor`/`steel_blade`/`short_sword` 為 UniqueCatalog base_id，必須存在。）

- [ ] **Step 3: 登錄 `presentation/inventory/item_catalog.gd`** — 於 `_ITEMS` 加各新 id → 路徑。

- [ ] **Step 4: 追加測試** `tests/presentation/test_item_catalog.gd`

```gdscript
func test_unique_base_ids_exist():
	for uid in UniqueCatalog.eligible(100):
		var base_id: String = String(UniqueCatalog.get(uid)["base_id"])
		assert_true(ItemCatalog.has_item(base_id), "unique base 必須存在：%s" % base_id)

func test_loot_pool_nonempty_and_all_droppable():
	var bases := LootPool.equipment_bases()
	assert_true(bases.size() >= 10)
	for d in bases:
		assert_true(d.drop_weight > 0)
```

- [ ] **Step 5: 執行確認通過** — `...-gselect=test_item_catalog.gd -gexit` → PASS
- [ ] **Step 6: Commit**

```bash
git add content/items/ presentation/inventory/item_catalog.gd tests/presentation/test_item_catalog.gd && git commit -m "content(equipment): 跨等級帶 base 裝備池（武器/防具/飾品）+ 掉落欄位

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 20: 內容 lint（掉落池覆蓋 + 詞綴/unique 一致性）

**Files:**
- Create: `tools/loot_lint.gd`、`tools/loot_lint_cli.gd`（仿 `tools/quest_lint.gd`/`quest_lint_cli.gd`）
- Create: `.claude/skills`? 否——僅工具。
- Test: `tests/tools/test_loot_lint.gd`

**Interfaces:**
- Produces：`LootLint.check(bases, )->Array[String]`（回問題清單，空＝通過）：每 10 級帶至少一個 base 覆蓋、無 drop_weight>0 卻缺欄位、affix `min_ilvl>=1`、unique `base_id` 存在於 bases。

- [ ] **Step 1: 寫失敗測試** `tests/tools/test_loot_lint.gd`

```gdscript
extends GutTest

func _base(id: String, lo: int, hi: int, w: int) -> ItemDef:
	var d := ItemDef.new(); d.id = id; d.min_level = lo; d.max_level = hi; d.drop_weight = w; return d

func test_reports_gap_when_band_uncovered():
	# 只有 1..10 有覆蓋，11..100 無 → 應回報缺口
	var issues := LootLint.check([_base("a", 1, 10, 5)])
	assert_true(issues.size() > 0)

func test_ok_when_full_coverage():
	var bases := [_base("a", 1, 100, 5)]
	var issues := LootLint.check(bases)
	# 覆蓋滿；unique base 檢查另計（此處 bases 不含 unique base 會回報）— 用寬鬆斷言
	for s in issues:
		assert_false(s.find("覆蓋") != -1, "不應有覆蓋缺口：%s" % s)
```

- [ ] **Step 2: 執行確認失敗** → FAIL

- [ ] **Step 3: 建立 `tools/loot_lint.gd`**

```gdscript
class_name LootLint
extends Object

# 掉落內容一致性檢查（純函式）。回問題字串清單，空＝通過。
static func check(bases: Array) -> Array:
	var issues: Array = []
	# 1) 每 10 級帶 (1-10,11-20,...,91-100) 至少一個 droppable base 覆蓋
	for band in range(10):
		var lo := band * 10 + 1
		var hi := lo + 9
		var covered := false
		for d in bases:
			if d.drop_weight > 0 and d.min_level <= hi and d.max_level >= lo:
				covered = true; break
		if not covered:
			issues.append("等級帶 %d-%d 無 base 覆蓋" % [lo, hi])
	# 2) unique base_id 必須存在於 bases
	var ids: Dictionary = {}
	for d in bases: ids[d.id] = true
	for uid in UniqueCatalog.eligible(100):
		var bid: String = String(UniqueCatalog.get(uid)["base_id"])
		if not ids.has(bid):
			issues.append("unique %s 的 base_id %s 不在 base 池" % [uid, bid])
	return issues
```

- [ ] **Step 4: 建立 `tools/loot_lint_cli.gd`**（仿 `tools/quest_lint_cli.gd` 的 headless 入口，載入 `LootPool.equipment_bases()` 跑 `LootLint.check` 並印結果、有問題 `OS.set_exit_code(1)`）。

```gdscript
extends SceneTree

func _init() -> void:
	var issues := LootLint.check(LootPool.equipment_bases())
	if issues.is_empty():
		print("[loot_lint] OK：掉落內容一致")
	else:
		for s in issues:
			printerr("[loot_lint] ", s)
		OS.set_exit_code(1)
	quit()
```

- [ ] **Step 5: 執行確認通過** — 跑 `test_loot_lint.gd` → PASS；
  跑 CLI：`godot --headless --path . -s res://tools/loot_lint_cli.gd`，Expected：`[loot_lint] OK`（Task 19 內容已滿覆蓋）。

- [ ] **Step 6: 更新 `tools/README.md`** 加 `loot_lint_cli.gd` 一行索引。
- [ ] **Step 7: Commit**

```bash
git add tools/loot_lint.gd tools/loot_lint_cli.gd tools/README.md tests/tools/test_loot_lint.gd && git commit -m "tools(equipment): loot_lint 掉落內容一致性檢查（等級帶覆蓋 + unique base）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 21: 平衡校準 + 全套回歸 + 最終人工 gate

**Files:**
- Modify: `engine/inventory/loot_roller.gd`（`_QW`）、`engine/combat/loot_system.gd`（`_GEAR_DEFAULT`）、各 `.tres` 數值——依模擬結果調
- 可選 Modify: `engine/sim/progression_sim.gd`（讓模擬隊伍可選帶等級對應裝備，量測強度曲線）
- Test: 全套 GUT + 人工 `./run.sh`

- [ ] **Step 1: 跑全套 GUT**
Run: `godot --headless --path . -s res://addons/gut/gut_cmdln.gd -gdir=res://tests -ginclude_subdirs -gexit`
Expected: 全綠（新舊測試皆通過）。若有紅，回對應 task 修正。

- [ ] **Step 2: 跑掉落分佈快檢** — 建一次性檢查（或用既有 sim CLI）：對 lv30 overworld roll 1000 次，統計品質比例，確認落在 `_QW[3]` 附近（common≈62% / fine≈29% / rare≈8% / legendary≈1%）。偏差過大就調 `_QW`。

- [ ] **Step 3: 平衡校準** — 依模擬把 `_GEAR_DEFAULT`、`_QW`、base `attack/armor/drop_weight`、affix `mods` 區間調到：穿上「等級相稱」裝備後隊伍勝率曲線不破壞 `docs/balance/combat-matrix`（不暴力碾壓 / 不無效）。記錄調整於 commit 訊息。

- [ ] **Step 4: 人工視覺 gate**
Run: `./run.sh`
確認：打怪掉裝出現品質色 toast → 開背包看到品質色名稱與詞綴明細 → 換裝後角色面板攻防/命中數值變化 → 存讀檔後裝備與背包實例保留 → 進「dungeon 主題」地圖時掉落偏高品質 → 商店買白裝/賣裝金幣依品質。

- [ ] **Step 5: Commit（平衡）**

```bash
git add engine/inventory/loot_roller.gd engine/combat/loot_system.gd content/items/ engine/sim/ && git commit -m "balance(equipment): 掉落率/品質權重/裝備數值校準（全套回歸綠 + 人工 gate 通過）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

- [ ] **Step 6: 收尾** — 使用 `superpowers:finishing-a-development-branch` 決定 merge/PR。

---

## Self-Review（作者對照 spec）

**Spec 覆蓋**
- §2 資料模型：Task 1（ItemDef+ItemStat）/2（Quality）/3（AffixCatalog）/4（UniqueCatalog）/5（ItemInstance）✓
- §3 掉落生成：Task 12（LootRoller）/13（LootSystem+gear_drop_chance+LootPool）/14（main 接線+source）✓
- §4 屬性接通：Task 6（Equipment.total_stat）/7（effective_*）/8（on-hit + hp/sp 夾點）✓
- §5 背包/裝備/存檔：Task 9（Inventory）/10（Save v12）/11（EquipFlow）✓
- §6 UI：Task 15（ItemDisplay）/16（角色背包畫面+比較）/17（toast/HUD 色）✓
- §7 商店：Task 18 ✓
- §8 內容：Task 19（base 池）/詞綴於 Task 3、unique 於 Task 4（V1 起始集，Task 19 補齊 base 使其可掉）✓
- §9 平衡與測試：各 task TDD + Task 20（lint）+ Task 21（模擬校準+回歸）✓
- §11 不做：計畫未引入 set/寶石/耐久/uid/相容層 ✓

**Placeholder 掃描**：無 TBD/TODO；每個新模組都有完整程式碼與測試。UI 三檔（Task 16/18）與既有結構耦合，給了「先 Read 再依提示改資料源/動作」的精確指示，核心邏輯已由 `EquipFlow`/`ItemDisplay`/`VendorTransaction` 純單元覆蓋，不靠 UI 檔猜測。

**型別一致性**：`total_stat(stat:int)`、`ItemStat.S`、`ItemInstance.base_resolver`、`Quality.Q`、`AffixCatalog.PREFIX/SUFFIX`、`LootRoller.Source`、`LootSystem.roll_drops(monsters, source, bases, rng)->{item_ids,instances}` 跨 task 一致。`Equipment.equipped()`（取代 `equipped_ids`）於 Task 6 定義、Task 10 使用一致。

**已知需執行時確認的既有結構**（非 placeholder，含精確指示）：
- Task 7/13：`Monster` 是否已有 `level`/`effective_speed`/`gear_drop_chance` 欄位 → 若無，依指示於 `monster.gd`/`from_def` 補。
- Task 16/18：三個 UI 檔的既有清單/選取/確認流程 → 先 Read 再改資料源。
