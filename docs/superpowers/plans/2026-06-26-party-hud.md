# 隊伍 HUD（頭像/HP/MP/buff-debuff）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把左下角純文字隊伍格升級成 6 張隊友卡，每張顯示 placeholder 頭像（依狀態換臉：正常/重傷/受擊/暈倒/死亡）、HP（紅）/MP（藍）條與數字、以及 buff/debuff 色片。

**Architecture:** 純邏輯的臉狀態映射放 `engine/party/portrait_state.gd`（可測）；`Character` 新增 `damaged` 訊號 + `take_damage()`，戰鬥 `monster_act` 改走它，受擊得以即時觸發。UI 拆成 `PartyMemberCard`（單卡）與 `PartyPanel`（6 卡列 + 把 `member.damaged` 接到該卡），`hud.gd` 用 `PartyPanel` 取代舊文字列；讀檔換 Party 實例時由 `PartyPanel.sync()` 重建卡。戰鬥中即時刷新靠 `CombatLayer` 新增 `turn_resolved` 訊號，`main.gd` 接到就 `_hud.refresh()`。

**Tech Stack:** Godot 4.7 + GDScript；GUT 測試（`extends GutTest`）；純程式建構 UI（無 .tscn、無美術），沿用既有 HUD 慣例。

## Global Constraints

- 引擎二進位不在 PATH：所有 godot 指令用 `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"`。
- 全套測試：`"$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gexit`。聚焦單檔：加 `-gselect=<script_name.gd>`。
- 新增帶 `class_name` 的檔案後，若測試報「class 未定義」，先跑一次編輯器 import 再重試：`"$GODOT" --headless --editor --quit`（`.godot/` 已 gitignore）。import 會生成同名 `.gd.uid`，commit 時一併加入（用 glob `path.gd*` 涵蓋）。
- 三層架構：`engine/` 純邏輯、`autoload/` 全域單例、`presentation/` Godot 節點。
- 重用既有資料：`Character`（`hp/hp_max`、`sp/sp_max`、`condition`、`statuses`）、`StatusEffect {stat, amount, remaining}`、`Party.members`。
- MP 條讀的是 `Character.sp/sp_max`，但 **UI 標籤一律寫「MP」**（使用者用詞）。
- buff/debuff 文字：`amount>0` 綠 `↑`、`amount<0` 紅 `↓`；stat 映射 ATTACK→`ATK`、ARMOR→`DEF`、ACCURACY→`ACC`。
- 存檔格式不動（buff/debuff 不入檔、頭像為推導值、無新持久欄位）。
- commit 訊息結尾固定一行：`Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`。
- 工作分支已建：`feat/party-hud`（spec 已 commit 於此）。

---

## File Structure

- **Create** `engine/party/portrait_state.gd` — `PortraitState`：持久臉狀態純映射。
- **Create** `tests/engine/party/test_portrait_state.gd` — 映射/邊界測試。
- **Modify** `engine/party/character.gd` — 加 `signal damaged(amount)` + `take_damage()`。
- **Modify** `tests/engine/party/test_character.gd` — 加 take_damage/訊號測試。
- **Modify** `engine/combat/combat_system.gd` — `monster_act` 扣血改走 `take_damage`。
- **Modify** `tests/engine/combat/test_combat_system.gd` — 加 monster_act 發 `damaged` + 昏迷不變測試。
- **Create** `presentation/ui/party_member_card.gd` — `PartyMemberCard`：單卡。
- **Create** `tests/presentation/test_party_member_card.gd` — 條/臉/buff 測試。
- **Create** `presentation/ui/party_panel.gd` — `PartyPanel`：6 卡列 + sync/閃臉接線。
- **Create** `tests/presentation/test_party_panel.gd` — 建卡/受擊/refresh/sync 測試。
- **Modify** `presentation/ui/hud.gd` — 用 `PartyPanel` 取代舊 `_member_labels` 與 `_format_member`。
- **Modify** `presentation/combat/combat_layer.gd` — 加 `signal turn_resolved`，`_apply` 後 emit。
- **Modify** `tests/presentation/` 新增 `test_combat_layer.gd` — 行動後發 `turn_resolved`。
- **Modify** `presentation/world/main.gd` — 接 `_combat_layer.turn_resolved -> _hud.refresh`。

---

### Task 1: PortraitState（持久臉純映射）

**Files:**
- Create: `engine/party/portrait_state.gd`
- Test: `tests/engine/party/test_portrait_state.gd` (create)

**Interfaces:**
- Consumes: `Character`（`condition`、`hp`、`hp_max`）、`Character.Condition`。
- Produces:
  - `class_name PortraitState`
  - `enum Face { OK, HURT, UNCONSCIOUS, DEAD }`
  - `const HURT_RATIO := 0.25`
  - `static func for_character(c: Character) -> int`（回 `Face.*`）

- [ ] **Step 1: 寫失敗測試**

Create `tests/engine/party/test_portrait_state.gd`:

```gdscript
extends GutTest

func _char(hp: int, hp_max: int, cond := Character.Condition.OK) -> Character:
	var c := Character.new()
	c.hp = hp
	c.hp_max = hp_max
	c.condition = cond
	return c

func test_dead_takes_priority_over_hp():
	assert_eq(PortraitState.for_character(_char(0, 28, Character.Condition.DEAD)), PortraitState.Face.DEAD)

func test_unconscious_when_ko():
	assert_eq(PortraitState.for_character(_char(0, 28, Character.Condition.UNCONSCIOUS)), PortraitState.Face.UNCONSCIOUS)

func test_ok_when_healthy():
	assert_eq(PortraitState.for_character(_char(28, 28)), PortraitState.Face.OK)

func test_hurt_at_or_below_quarter():
	# 28 * 0.25 = 7 → hp 7 為邊界，算重傷
	assert_eq(PortraitState.for_character(_char(7, 28)), PortraitState.Face.HURT)
	assert_eq(PortraitState.for_character(_char(8, 28)), PortraitState.Face.OK)

func test_zero_max_is_ok_not_hurt():
	assert_eq(PortraitState.for_character(_char(0, 0)), PortraitState.Face.OK)
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gselect=test_portrait_state.gd -gexit`
Expected: FAIL（`PortraitState` 未定義）。若報 class 未定義，先 `"$GODOT" --headless --editor --quit` 再重試。

- [ ] **Step 3: 實作**

Create `engine/party/portrait_state.gd`:

```gdscript
class_name PortraitState
extends RefCounted
# 由角色「持久狀態」推導頭像臉（不含戰鬥中瞬間的受擊閃臉）。純邏輯、可測。

enum Face { OK, HURT, UNCONSCIOUS, DEAD }

const HURT_RATIO := 0.25

static func for_character(c: Character) -> int:
	if c.condition == Character.Condition.DEAD:
		return Face.DEAD
	if c.condition == Character.Condition.UNCONSCIOUS:
		return Face.UNCONSCIOUS
	if c.hp_max > 0 and float(c.hp) <= float(c.hp_max) * HURT_RATIO:
		return Face.HURT
	return Face.OK
```

- [ ] **Step 4: 跑測試確認通過**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gselect=test_portrait_state.gd -gexit`
Expected: PASS（5 測試綠）。

- [ ] **Step 5: Commit**

```bash
git add engine/party/portrait_state.gd* tests/engine/party/test_portrait_state.gd*
git commit -m "feat(party): PortraitState 持久臉狀態純映射（OK/重傷/暈倒/死亡）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: Character 受擊訊號 + take_damage

**Files:**
- Modify: `engine/party/character.gd`
- Test: `tests/engine/party/test_character.gd` (append)

**Interfaces:**
- Consumes: 既有 `Character` 欄位。
- Produces:
  - `signal damaged(amount: int)`（Character 上）
  - `func take_damage(amount: int) -> void`（`hp = maxi(hp - amount, 0)` 後 `damaged.emit(amount)`）

- [ ] **Step 1: 寫失敗測試**

Append to `tests/engine/party/test_character.gd`:

```gdscript
func test_take_damage_reduces_hp():
	var c := Character.new()
	c.hp = 20
	c.hp_max = 20
	c.take_damage(5)
	assert_eq(c.hp, 15)

func test_take_damage_clamps_at_zero():
	var c := Character.new()
	c.hp = 3
	c.hp_max = 20
	c.take_damage(10)
	assert_eq(c.hp, 0)

func test_take_damage_emits_damaged_signal():
	var c := Character.new()
	c.hp = 20
	c.hp_max = 20
	watch_signals(c)
	c.take_damage(7)
	assert_signal_emitted(c, "damaged")
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gselect=test_character.gd -gexit`
Expected: FAIL（`take_damage` 不存在 / signal `damaged` 不存在）。

- [ ] **Step 3: 實作**

In `engine/party/character.gd`, add the signal right after the variable block (after `var statuses: Array[StatusEffect] = []`):

```gdscript
var statuses: Array[StatusEffect] = []

signal damaged(amount: int)
```

And add the method right after `is_conscious()`:

```gdscript
func is_conscious() -> bool:
	return condition == Condition.OK

func take_damage(amount: int) -> void:
	hp = maxi(hp - amount, 0)
	damaged.emit(amount)
```

- [ ] **Step 4: 跑測試確認通過**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gselect=test_character.gd -gexit`
Expected: PASS（既有 + 3 新測試綠）。

- [ ] **Step 5: Commit**

```bash
git add engine/party/character.gd tests/engine/party/test_character.gd
git commit -m "feat(party): Character 新增 damaged 訊號 + take_damage（扣血不破 0 並通知）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: monster_act 扣隊員血改走 take_damage

**Files:**
- Modify: `engine/combat/combat_system.gd`（`monster_act`）
- Test: `tests/engine/combat/test_combat_system.gd` (append)

**Interfaces:**
- Consumes: `Character.take_damage` / `Character.damaged`（Task 2）。
- Produces: 行為不變（仍夾 0、仍在 hp<=0 設 UNCONSCIOUS），但隊員被打時會發 `damaged`。

- [ ] **Step 1: 寫失敗測試**

Append to `tests/engine/combat/test_combat_system.gd`:

```gdscript
func test_monster_act_emits_damaged_on_target():
	var hero := _char("Hero", 50, 1, 1, 1)        # 慢 → 怪物先動
	var mon := _monster("Mon", 100, 5, 1000, 20)  # 高命中必中
	var cs := CombatSystem.new(_party([hero]), _monsters([mon]), _rng(1))
	assert_false(cs.is_party_turn())
	watch_signals(hero)
	cs.monster_act()
	assert_signal_emitted(hero, "damaged")
	assert_lt(hero.hp, 50)

func test_monster_act_kos_member_at_zero_hp():
	var hero := _char("Hero", 3, 1, 1, 1)
	var mon := _monster("Mon", 100, 50, 1000, 20)
	var cs := CombatSystem.new(_party([hero]), _monsters([mon]), _rng(1))
	cs.monster_act()
	assert_eq(hero.hp, 0)
	assert_eq(hero.condition, Character.Condition.UNCONSCIOUS)
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gselect=test_combat_system.gd -gexit`
Expected: FAIL（`test_monster_act_emits_damaged_on_target` 失敗，因為現在是 `target.hp -= dmg`，沒發訊號）。

- [ ] **Step 3: 實作**

In `engine/combat/combat_system.gd`, inside `monster_act()`, change the damage line:

```gdscript
		var dmg := CombatFormulas.roll_damage(actor.effective_attack(), target.armor_value(), defending, _rng)
		target.take_damage(dmg)
		events.append("%s 攻擊 %s，造成 %d 傷害。" % [actor.name, target.name, dmg])
		if target.hp <= 0:
			target.hp = 0
			target.condition = Character.Condition.UNCONSCIOUS
			events.append("%s 倒下了！" % target.name)
```

（只把原本 `target.hp -= dmg` 換成 `target.take_damage(dmg)`；其餘維持。）

- [ ] **Step 4: 跑測試確認通過**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gselect=test_combat_system.gd -gexit`
Expected: PASS（既有 + 2 新測試綠）。

- [ ] **Step 5: Commit**

```bash
git add engine/combat/combat_system.gd tests/engine/combat/test_combat_system.gd
git commit -m "feat(combat): monster_act 扣隊員血改走 take_damage（觸發受擊訊號，行為不變）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: PartyMemberCard（單卡：頭像/條/buff）

**Files:**
- Create: `presentation/ui/party_member_card.gd`
- Test: `tests/presentation/test_party_member_card.gd` (create)

**Interfaces:**
- Consumes: `PortraitState`（Task 1）、`Character`、`StatusEffect`、`Character.damaged`（Task 2）。
- Produces:
  - `class_name PartyMemberCard extends VBoxContainer`
  - `enum FaceVisual { OK, HURT, HIT, UNCONSCIOUS, DEAD }`
  - `const HIT_MS := 400`
  - `func setup(character: Character) -> void`
  - `func refresh() -> void`
  - `func flash_hit() -> void`
  - `func is_hit_active() -> bool`
  - `func current_visual() -> int`（回 `FaceVisual.*`）
  - `func character() -> Character`
  - `func _on_self_damaged(amount: int) -> void`（給 PartyPanel 接 `damaged`）
  - `static func bar_ratio(value: int, max_value: int) -> float`
  - `static func status_text(s: StatusEffect) -> String`
  - `static func status_color(s: StatusEffect) -> Color`
  - 內部成員（測試會讀）：`var _hp_label: Label`、`var _mp_label: Label`、`var _buff_row: HBoxContainer`

- [ ] **Step 1: 寫失敗測試**

Create `tests/presentation/test_party_member_card.gd`:

```gdscript
extends GutTest

func _char(cls := "Knight", hp := 28, hp_max := 28, sp := 8, sp_max := 8) -> Character:
	var c := Character.new()
	c.name = "Hero"
	c.char_class = cls
	c.level = 3
	c.hp = hp
	c.hp_max = hp_max
	c.sp = sp
	c.sp_max = sp_max
	c.condition = Character.Condition.OK
	return c

func _card(c: Character) -> PartyMemberCard:
	var card := PartyMemberCard.new()
	add_child_autofree(card)
	card.setup(c)
	return card

func test_hp_label_shows_tag_and_values():
	assert_eq(_card(_char("Knight", 24, 28))._hp_label.text, "HP 24/28")

func test_mp_label_uses_mp_tag_from_sp_fields():
	assert_eq(_card(_char("Cleric", 28, 28, 4, 8))._mp_label.text, "MP 4/8")

func test_bar_ratio_quarter():
	assert_almost_eq(PartyMemberCard.bar_ratio(7, 28), 0.25, 0.0001)

func test_bar_ratio_zero_max_is_zero():
	assert_eq(PartyMemberCard.bar_ratio(5, 0), 0.0)

func test_bar_ratio_clamped_to_one():
	assert_eq(PartyMemberCard.bar_ratio(40, 20), 1.0)

func test_visual_ok_when_healthy():
	assert_eq(_card(_char("Knight", 28, 28)).current_visual(), PartyMemberCard.FaceVisual.OK)

func test_visual_hurt_when_low_hp():
	assert_eq(_card(_char("Knight", 7, 28)).current_visual(), PartyMemberCard.FaceVisual.HURT)

func test_visual_unconscious():
	var c := _char("Knight", 0, 28)
	c.condition = Character.Condition.UNCONSCIOUS
	assert_eq(_card(c).current_visual(), PartyMemberCard.FaceVisual.UNCONSCIOUS)

func test_visual_dead():
	var c := _char("Knight", 0, 28)
	c.condition = Character.Condition.DEAD
	assert_eq(_card(c).current_visual(), PartyMemberCard.FaceVisual.DEAD)

func test_flash_hit_overrides_to_hit_visual():
	var card := _card(_char("Knight", 28, 28))
	card.flash_hit()
	assert_true(card.is_hit_active())
	assert_eq(card.current_visual(), PartyMemberCard.FaceVisual.HIT)

func test_status_text_buff_and_debuff():
	assert_eq(PartyMemberCard.status_text(StatusEffect.new(StatusEffect.Stat.ATTACK, 5, 2)), "↑ATK")
	assert_eq(PartyMemberCard.status_text(StatusEffect.new(StatusEffect.Stat.ARMOR, -3, 2)), "↓DEF")
	assert_eq(PartyMemberCard.status_text(StatusEffect.new(StatusEffect.Stat.ACCURACY, 4, 2)), "↑ACC")

func test_buff_row_renders_one_chip_per_status():
	var c := _char("Sorcerer")
	c.statuses.append(StatusEffect.new(StatusEffect.Stat.ATTACK, 5, 2))
	c.statuses.append(StatusEffect.new(StatusEffect.Stat.ARMOR, -3, 2))
	var card := _card(c)
	assert_eq(card._buff_row.get_child_count(), 2)
	assert_eq((card._buff_row.get_child(0) as Label).text, "↑ATK")

func test_character_accessor_returns_bound_character():
	var c := _char("Knight")
	assert_eq(_card(c).character(), c)
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gselect=test_party_member_card.gd -gexit`
Expected: FAIL（`PartyMemberCard` 未定義）。若報 class 未定義，先 `"$GODOT" --headless --editor --quit` 再重試。

- [ ] **Step 3: 實作**

Create `presentation/ui/party_member_card.gd`:

```gdscript
class_name PartyMemberCard
extends VBoxContainer
# 單一隊友卡：placeholder 頭像（依狀態換臉）＋ HP/MP 條 ＋ buff/debuff 列。
# 全程式建構、無美術；尺寸/座標為 placeholder（預設視窗 1152x648）。

const HIT_MS := 400                       # 受擊閃臉持續毫秒
const _CARD_SIZE := Vector2(150, 110)
const _PORTRAIT_SIZE := Vector2(56, 40)
const _BAR_SIZE := Vector2(132, 12)

enum FaceVisual { OK, HURT, HIT, UNCONSCIOUS, DEAD }

const _GLYPH := {
	FaceVisual.OK: ":)",
	FaceVisual.HURT: ":(",
	FaceVisual.HIT: "><",
	FaceVisual.UNCONSCIOUS: "x_x",
	FaceVisual.DEAD: "✝",
}
const _TINT := {
	FaceVisual.OK: Color(1, 1, 1),
	FaceVisual.HURT: Color(0.85, 0.55, 0.55),
	FaceVisual.HIT: Color(1.0, 0.45, 0.45),
	FaceVisual.UNCONSCIOUS: Color(0.55, 0.55, 0.55),
	FaceVisual.DEAD: Color(0.3, 0.3, 0.3),
}

var _character: Character
var _hit_until_msec: int = 0

var _portrait: ColorRect
var _portrait_glyph: Label
var _name_label: Label
var _hp_fill: ColorRect
var _hp_label: Label
var _mp_fill: ColorRect
var _mp_label: Label
var _buff_row: HBoxContainer

func setup(character: Character) -> void:
	_character = character
	custom_minimum_size = _CARD_SIZE
	add_theme_constant_override("separation", 2)
	_build()
	refresh()

func character() -> Character:
	return _character

func _build() -> void:
	_portrait = ColorRect.new()
	_portrait.custom_minimum_size = _PORTRAIT_SIZE
	_portrait.size_flags_horizontal = Control.SIZE_SHRINK_CENTER
	add_child(_portrait)
	_portrait_glyph = Label.new()
	_portrait_glyph.set_anchors_preset(Control.PRESET_FULL_RECT)
	_portrait_glyph.horizontal_alignment = HORIZONTAL_ALIGNMENT_CENTER
	_portrait_glyph.vertical_alignment = VERTICAL_ALIGNMENT_CENTER
	_portrait.add_child(_portrait_glyph)

	_name_label = Label.new()
	_name_label.add_theme_font_size_override("font_size", 12)
	add_child(_name_label)

	_hp_fill = _make_bar(Color(0.8, 0.2, 0.2))
	_hp_label = _bar_label(_hp_fill)
	_mp_fill = _make_bar(Color(0.25, 0.45, 0.9))
	_mp_label = _bar_label(_mp_fill)

	_buff_row = HBoxContainer.new()
	_buff_row.add_theme_constant_override("separation", 3)
	add_child(_buff_row)

func _make_bar(fill_color: Color) -> ColorRect:
	var bg := ColorRect.new()
	bg.color = Color(0.1, 0.1, 0.1)
	bg.custom_minimum_size = _BAR_SIZE
	add_child(bg)
	var fill := ColorRect.new()
	fill.color = fill_color
	fill.set_anchors_preset(Control.PRESET_TOP_LEFT)
	fill.size = Vector2(0, _BAR_SIZE.y)
	bg.add_child(fill)
	return fill

func _bar_label(fill: ColorRect) -> Label:
	var lbl := Label.new()
	lbl.set_anchors_preset(Control.PRESET_FULL_RECT)
	lbl.horizontal_alignment = HORIZONTAL_ALIGNMENT_CENTER
	lbl.vertical_alignment = VERTICAL_ALIGNMENT_CENTER
	lbl.add_theme_font_size_override("font_size", 10)
	(fill.get_parent() as Control).add_child(lbl)   # 疊在 bar 背景上
	return lbl

func refresh() -> void:
	if _character == null:
		return
	_name_label.text = "%s L%d" % [_character.name, _character.level]
	_apply_bar(_hp_fill, _hp_label, "HP", _character.hp, _character.hp_max)
	_apply_bar(_mp_fill, _mp_label, "MP", _character.sp, _character.sp_max)
	_apply_face()
	_apply_buffs()

func _apply_bar(fill: ColorRect, label: Label, tag: String, value: int, max_value: int) -> void:
	fill.size = Vector2(bar_ratio(value, max_value) * _BAR_SIZE.x, _BAR_SIZE.y)
	label.text = "%s %d/%d" % [tag, value, max_value]

static func bar_ratio(value: int, max_value: int) -> float:
	if max_value <= 0:
		return 0.0
	return clampf(float(value) / float(max_value), 0.0, 1.0)

# --- 頭像換臉 ---

func flash_hit() -> void:
	_hit_until_msec = Time.get_ticks_msec() + HIT_MS
	set_process(true)
	_apply_face()

func is_hit_active() -> bool:
	return Time.get_ticks_msec() < _hit_until_msec

func current_visual() -> int:
	if is_hit_active():
		return FaceVisual.HIT
	match PortraitState.for_character(_character):
		PortraitState.Face.DEAD:
			return FaceVisual.DEAD
		PortraitState.Face.UNCONSCIOUS:
			return FaceVisual.UNCONSCIOUS
		PortraitState.Face.HURT:
			return FaceVisual.HURT
		_:
			return FaceVisual.OK

func _apply_face() -> void:
	var v := current_visual()
	_portrait_glyph.text = _GLYPH[v]
	_portrait.color = _class_color(_character.char_class)
	_portrait.modulate = _TINT[v]

func _process(_delta: float) -> void:
	if not is_hit_active():
		set_process(false)
		_apply_face()

func _on_self_damaged(_amount: int) -> void:
	flash_hit()

# --- buff/debuff ---

func _apply_buffs() -> void:
	for child in _buff_row.get_children():
		_buff_row.remove_child(child)
		child.free()
	for s in _character.statuses:
		var chip := Label.new()
		chip.add_theme_font_size_override("font_size", 10)
		chip.text = status_text(s)
		chip.add_theme_color_override("font_color", status_color(s))
		_buff_row.add_child(chip)

static func status_text(s: StatusEffect) -> String:
	var arrow := "↑" if s.amount > 0 else "↓"
	return arrow + _stat_abbrev(s.stat)

static func status_color(s: StatusEffect) -> Color:
	return Color(0.4, 0.9, 0.4) if s.amount > 0 else Color(0.95, 0.4, 0.4)

static func _stat_abbrev(stat: int) -> String:
	match stat:
		StatusEffect.Stat.ATTACK:
			return "ATK"
		StatusEffect.Stat.ARMOR:
			return "DEF"
		StatusEffect.Stat.ACCURACY:
			return "ACC"
		_:
			return "?"

static func _class_color(char_class: String) -> Color:
	match char_class:
		"Knight":
			return Color(0.55, 0.35, 0.30)
		"Paladin":
			return Color(0.85, 0.78, 0.45)
		"Archer":
			return Color(0.35, 0.6, 0.35)
		"Cleric":
			return Color(0.8, 0.8, 0.85)
		"Sorcerer":
			return Color(0.45, 0.4, 0.75)
		"Robber":
			return Color(0.4, 0.4, 0.45)
		_:
			return Color(0.5, 0.5, 0.5)
```

- [ ] **Step 4: 跑測試確認通過**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gselect=test_party_member_card.gd -gexit`
Expected: PASS（13 測試綠）。

- [ ] **Step 5: Commit**

```bash
git add presentation/ui/party_member_card.gd* tests/presentation/test_party_member_card.gd*
git commit -m "feat(ui): PartyMemberCard 單卡（placeholder 頭像換臉 + HP/MP 條 + buff/debuff）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: PartyPanel + hud.gd 接線

**Files:**
- Create: `presentation/ui/party_panel.gd`
- Test: `tests/presentation/test_party_panel.gd` (create)
- Modify: `presentation/ui/hud.gd`

**Interfaces:**
- Consumes: `PartyMemberCard`（Task 4）、`Party`、`Character.damaged`（Task 2）。
- Produces:
  - `class_name PartyPanel extends HBoxContainer`
  - `func setup(party: Party) -> void`（建卡、接 `member.damaged -> card._on_self_damaged`）
  - `func refresh() -> void`
  - `func sync(party: Party) -> void`（成員實例變了就 setup 重建，否則 refresh）
- hud：`refresh()` 改呼叫 `_party_panel.sync(GameState.party)`。

- [ ] **Step 1: 寫失敗測試**

Create `tests/presentation/test_party_panel.gd`:

```gdscript
extends GutTest

func _party_of(n: int) -> Party:
	var p := Party.new()
	var ms: Array[Character] = []
	for i in n:
		var c := Character.new()
		c.name = "C%d" % i
		c.char_class = "Knight"
		c.level = 1
		c.hp = 20
		c.hp_max = 20
		c.sp = 5
		c.sp_max = 5
		c.condition = Character.Condition.OK
		ms.append(c)
	p.members = ms
	return p

func _panel(party: Party) -> PartyPanel:
	var panel := PartyPanel.new()
	add_child_autofree(panel)
	panel.setup(party)
	return panel

func test_one_card_per_member():
	var panel := _panel(_party_of(3))
	assert_eq(panel.get_child_count(), 3)
	assert_true(panel.get_child(0) is PartyMemberCard)

func test_member_damage_flashes_only_its_card():
	var party := _party_of(2)
	var panel := _panel(party)
	party.members[1].take_damage(3)
	assert_true((panel.get_child(1) as PartyMemberCard).is_hit_active())
	assert_false((panel.get_child(0) as PartyMemberCard).is_hit_active())

func test_refresh_updates_card_labels():
	var party := _party_of(1)
	var panel := _panel(party)
	party.members[0].hp = 5
	party.members[0].hp_max = 20
	panel.refresh()
	assert_eq((panel.get_child(0) as PartyMemberCard)._hp_label.text, "HP 5/20")

func test_sync_keeps_cards_when_same_party():
	var party := _party_of(2)
	var panel := _panel(party)
	var card0 := panel.get_child(0)
	panel.sync(party)
	assert_eq(panel.get_child(0), card0)

func test_sync_rebuilds_when_party_replaced():
	var panel := _panel(_party_of(2))
	var party_b := _party_of(2)
	panel.sync(party_b)
	assert_eq(panel.get_child_count(), 2)
	assert_eq((panel.get_child(0) as PartyMemberCard).character(), party_b.members[0])
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gselect=test_party_panel.gd -gexit`
Expected: FAIL（`PartyPanel` 未定義）。若報 class 未定義，先 `"$GODOT" --headless --editor --quit` 再重試。

- [ ] **Step 3: 實作 PartyPanel**

Create `presentation/ui/party_panel.gd`:

```gdscript
class_name PartyPanel
extends HBoxContainer
# 左下隊伍列：每位 member 一張 PartyMemberCard，並把 member.damaged 接到該卡（受擊閃臉）。
# 讀檔會把 GameState.party 換成新實例 → sync() 偵測成員實例改變時整列重建。

var _cards: Array[PartyMemberCard] = []

func setup(party: Party) -> void:
	for c in _cards:
		remove_child(c)
		c.free()
	_cards.clear()
	add_theme_constant_override("separation", 8)
	for m in party.members:
		var card := PartyMemberCard.new()
		add_child(card)
		card.setup(m)
		_cards.append(card)
		m.damaged.connect(card._on_self_damaged)

func refresh() -> void:
	for card in _cards:
		card.refresh()

func sync(party: Party) -> void:
	if _same_members(party):
		refresh()
	else:
		setup(party)

func _same_members(party: Party) -> bool:
	if party.members.size() != _cards.size():
		return false
	for i in _cards.size():
		if _cards[i].character() != party.members[i]:
			return false
	return true
```

- [ ] **Step 4: 跑 PartyPanel 測試確認通過**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gselect=test_party_panel.gd -gexit`
Expected: PASS（5 測試綠）。

- [ ] **Step 5: 接上 hud.gd**

Replace the full contents of `presentation/ui/hud.gd` with:

```gdscript
class_name Hud
extends CanvasLayer

# 程式建構的 placeholder HUD：左上指北針、左下訊息列 + 隊伍卡列（PartyPanel）。
# 版面錨定左上/左下，解析度無關（預設視窗 1152x648）。

const _DIR_NAMES := ["N", "E", "S", "W"]  # 以 GridDirection.Dir 索引

var _compass_label: Label
var _message_label: Label
var _party_panel: PartyPanel
var _bound_party: Party
var _message_log: MessageLog

func setup(game_state: Node, player: PlayerController) -> void:
	_build_ui(game_state.party)
	_bound_party = game_state.party
	_message_log = game_state.message_log
	_message_log.changed.connect(_on_message_changed)
	player.facing_changed.connect(_on_facing_changed)

func _build_ui(party: Party) -> void:
	_compass_label = Label.new()
	_compass_label.position = Vector2(20, 12)
	_compass_label.add_theme_font_size_override("font_size", 22)
	add_child(_compass_label)

	var bottom := VBoxContainer.new()
	bottom.set_anchors_preset(Control.PRESET_BOTTOM_LEFT)
	bottom.offset_left = 20
	bottom.offset_bottom = -20
	bottom.grow_horizontal = Control.GROW_DIRECTION_END
	bottom.grow_vertical = Control.GROW_DIRECTION_BEGIN
	bottom.add_theme_constant_override("separation", 8)
	add_child(bottom)

	_message_label = Label.new()
	_message_label.add_theme_font_size_override("font_size", 18)
	bottom.add_child(_message_label)

	_party_panel = PartyPanel.new()
	bottom.add_child(_party_panel)
	_party_panel.setup(party)

func refresh() -> void:
	# 讀檔可能整個換掉 GameState.party 實例 → sync 偵測並在需要時重建卡。
	_bound_party = GameState.party
	_party_panel.sync(GameState.party)

func _on_facing_changed(facing: int) -> void:
	_compass_label.text = "面向: %s" % _DIR_NAMES[facing]

func _on_message_changed() -> void:
	var lines := _message_log.recent(1)
	_message_label.text = ("> " + lines[0]) if lines.size() > 0 else ""
```

- [ ] **Step 6: 跑全套測試確認沒打壞既有**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gexit`
Expected: 全綠（既有 + 本計畫新測試；無 hud.gd 既有測試會被破壞）。

- [ ] **Step 7: Commit**

```bash
git add presentation/ui/party_panel.gd* presentation/ui/hud.gd tests/presentation/test_party_panel.gd*
git commit -m "feat(ui): PartyPanel 隊伍卡列（受擊接線 + 讀檔重建）並接上 hud

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: 戰鬥中即時刷新（CombatLayer.turn_resolved → hud）

**Files:**
- Modify: `presentation/combat/combat_layer.gd`（加 `signal turn_resolved`，`_apply` 後 emit）
- Test: `tests/presentation/test_combat_layer.gd` (create)
- Modify: `presentation/world/main.gd`（接訊號）

**Interfaces:**
- Consumes: `CombatSystem`、`Character.take_damage`（Task 3）、`Hud.refresh`（Task 5）。
- Produces: `signal turn_resolved`（CombatLayer 每次行動結算後 emit）。

- [ ] **Step 1: 寫失敗測試**

Create `tests/presentation/test_combat_layer.gd`:

```gdscript
extends GutTest

func _key(code: int) -> InputEventKey:
	var ev := InputEventKey.new()
	ev.keycode = code
	ev.pressed = true
	return ev

func _char(name: String, hp: int, might: int, acc: int, speed: int) -> Character:
	var c := Character.new()
	c.name = name
	c.hp = hp
	c.hp_max = hp
	c.might = might
	c.accuracy = acc
	c.speed = speed
	c.condition = Character.Condition.OK
	return c

func _monster(name: String, hp: int, might: int, acc: int, speed: int) -> Monster:
	var m := Monster.new()
	m.name = name
	m.hp = hp
	m.hp_max = hp
	m.might = might
	m.armor = 0
	m.accuracy = acc
	m.speed = speed
	m.xp_reward = 10
	m.gold_reward = 5
	return m

func _party(c: Character) -> Party:
	var p := Party.new()
	var ms: Array[Character] = [c]
	p.members = ms
	return p

func _monsters(m: Monster) -> Array[Monster]:
	var out: Array[Monster] = [m]
	return out

func _rng(s: int) -> RandomNumberGenerator:
	var r := RandomNumberGenerator.new()
	r.seed = s
	return r

func test_turn_resolved_emitted_after_party_action():
	var cam := Camera3D.new()
	add_child_autofree(cam)
	var layer := CombatLayer.new()
	add_child_autofree(layer)
	var hero := _char("Hero", 100, 5, 1000, 50)   # 快、必中
	var mon := _monster("Mon", 100, 1, 1, 1)       # 撐得住一擊 → 戰鬥未結束
	var cs := CombatSystem.new(_party(hero), _monsters(mon), _rng(1))
	layer.begin(cs, cam)
	assert_true(cs.is_party_turn())
	watch_signals(layer)
	layer._unhandled_input(_key(KEY_1))            # 攻擊 1 號敵人
	assert_signal_emitted(layer, "turn_resolved")
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gselect=test_combat_layer.gd -gexit`
Expected: FAIL（signal `turn_resolved` 不存在）。

- [ ] **Step 3: 實作 CombatLayer**

In `presentation/combat/combat_layer.gd`, add the signal right after the existing one:

```gdscript
signal combat_finished(result: int)
signal turn_resolved
```

And modify `_apply()` to emit after resolving:

```gdscript
func _apply(events: Array) -> void:
	for e in events:
		_push_log(e)
	_resolve()
	turn_resolved.emit()
```

- [ ] **Step 4: 跑測試確認通過**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gselect=test_combat_layer.gd -gexit`
Expected: PASS（1 測試綠）。

- [ ] **Step 5: 接上 main.gd**

In `presentation/world/main.gd`, find the `_combat_layer` wiring in `_ready()`:

```gdscript
	_combat_layer = CombatLayer.new()
	add_child(_combat_layer)
	_combat_layer.combat_finished.connect(_on_combat_finished)
```

Add the new connection right after `combat_finished.connect`:

```gdscript
	_combat_layer = CombatLayer.new()
	add_child(_combat_layer)
	_combat_layer.combat_finished.connect(_on_combat_finished)
	_combat_layer.turn_resolved.connect(_hud.refresh)
```

（`_hud` 在此之前已建立並 setup，故 `_hud.refresh` 為有效 Callable。）

- [ ] **Step 6: 跑全套測試**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gexit`
Expected: 全綠。

- [ ] **Step 7: 人工視覺 gate**

Run: `./run.sh`
目視確認：
- 開場地圖左下出現 6 張隊友卡（頭像色塊 + 名字 L# + HP 紅條/數字 + MP 藍條/數字）。預設隊伍中 Marcus 應顯示暈倒臉（`x_x`、灰）。
- 觸發戰鬥：被怪物打到的隊員頭像會「受擊閃臉」(`><` 紅閃) 約 0.4s 後恢復；HP 條在戰鬥中每個行動後即時下降；隊員倒下時臉變暈倒。
- 施放 buff 法術（如有）後，受影響隊員的 buff/debuff 色片出現於卡片下方（`↑ATK`/`↓DEF` 等），戰鬥結束清空。
- 存讀檔後隊伍卡仍正確對應載入的隊伍狀態。

- [ ] **Step 8: Commit**

```bash
git add presentation/combat/combat_layer.gd presentation/world/main.gd tests/presentation/test_combat_layer.gd*
git commit -m "feat(combat): CombatLayer turn_resolved 訊號讓隊伍 HUD 戰鬥中即時刷新

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Self-Review

**1. Spec coverage**（逐項對 spec）：
- 頭像 placeholder + 依狀態換臉（OK/重傷/暈倒/死亡） → Task 1（純映射）+ Task 4（FaceVisual 視覺）。✅
- 受擊瞬間換臉 → Task 2（`damaged`/`take_damage`）+ Task 3（monster_act 觸發）+ Task 4（`flash_hit`/HIT）+ Task 5（PartyPanel 接線）。✅
- HP 紅條 / MP 藍條 + 數字、MP 標籤 → Task 4（`_apply_bar`、`bar_ratio`、"HP/MP" 標籤）。✅
- buff/debuff 色片（↑/↓ + ATK/DEF/ACC、綠/紅） → Task 4（`status_text`/`status_color`/`_apply_buffs`）。✅
- 元件拆分 PartyMemberCard / PartyPanel / hud 接線 → Task 4 / Task 5。✅
- 戰鬥中即時刷新 → Task 6（`turn_resolved` + main 接線）。✅
- 讀檔換 Party 實例的重建 → Task 5（`PartyPanel.sync` + hud `refresh`）。✅
- 存檔不升版 → 全程未動存檔程式。✅

**2. Placeholder scan**：無 TBD/TODO；每個程式步驟皆附完整程式碼；每個測試步驟附實際 assert。✅

**3. Type consistency**：
- `PortraitState.for_character` 回 `int`（`Face.*`）；Task 4 `current_visual` 以 `match PortraitState.Face.*` 對應到 `FaceVisual.*`。一致。
- `Character.take_damage(int)` / `damaged(amount:int)`：Task 2 定義、Task 3 呼叫、Task 4 `_on_self_damaged(_amount:int)`、Task 5 `m.damaged.connect(card._on_self_damaged)`。簽章一致。
- `PartyMemberCard.character()`、`_hp_label`/`_mp_label`/`_buff_row`：Task 4 定義、Task 5 測試與 `_same_members` 使用。一致。
- `PartyPanel.setup/refresh/sync`、hud `refresh()` 呼叫 `sync`。一致。
- `CombatLayer.turn_resolved`（無參數）：Task 6 定義、main `connect(_hud.refresh)`（refresh 無參數）。一致。

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-06-26-party-hud.md`.
