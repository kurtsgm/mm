# A4 世界拓撲與尺度 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** LOCAL_MAP_SIZE 10→16（含既有 10 張地圖遷移）＋大陸間旅行系統（旅行點＋目的地選單）＋世界地圖總覽畫面（M 鍵）。

**Architecture:** 引擎的 stitch/grid/movement 已尺寸無關，遷移只動 `MapData.LOCAL_SIZE` 常數＋content 重畫＋座標測試。旅行＝新 `travel` entity → `TravelOverlay` 選單 → 復用 `main._enter_via_link()` 跨叢集傳送；解鎖復用既有 `GameState.flags`（**不加存檔欄位、不升 save 版**）。世界地圖＝新 CanvasLayer 全螢幕面板，資料來自新 `continents.json` ＋ map JSON 新 `continent` 欄位。

**Tech Stack:** Godot 4.7 / GDScript / GUT。

## Global Constraints

- Spec：`docs/superpowers/specs/2026-07-05-a4-世界拓撲與尺度-design.md`。
- pre-release **不需向後相容**：舊存檔壞掉沒關係，禁止寫相容/fallback 層（CLAUDE.md）。
- UI 版面一律**比例式 anchor**，不寫死像素（字級/小邊距 offset 可固定）。
- 遊戲內文案 zh-TW。
- 測試指令（全套）：`godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests -ginclude_subdirs -gexit`
- 測試指令（單檔）：上式加 `-gselect=<file.gd>`。
- quest lint：`godot --headless --path . --script res://tools/quest_lint_cli.gd`；loot lint：`godot --headless --path . --script res://tools/loot_lint_cli.gd`。
- 每個 Task 結尾 commit；sub-agent 一律繼承主對話模型。
- GDScript 注意：`:=` 遇 Variant 右值在 4.7 不編譯，用 `=`；Catalog 類查詢方法命名避開 `get()`（撞 `Object.get`）。

---

### Task 1: LOCAL_SIZE 16 ＋ 既有地圖遷移（置中 +3 padding）

**Files:**
- Modify: `resources/map_data.gd:6`（LOCAL_SIZE）
- Modify: `content/maps/*.json`（全部 10 張，由一次性腳本改寫）
- Modify: `content/quests/goblin_menace.json:7`（reach pos）
- Modify: `tests/resources/test_map_data.gd:88`
- Modify: `tests/content/test_world_maps.gd`、`tests/content/test_quest_content.gd`、`tests/content/test_int_oak_*.gd`（5 檔）、其餘 suite 失敗點（見 Step 6）
- Create（scratchpad，不進 repo）: `<scratchpad>/migrate_maps_16.py`

**Interfaces:**
- Produces: 全部 content 地圖為 16×16；既有內容整體平移 (+3,+3)。後續 Task 在 16×16 座標系上加 entity。

- [ ] **Step 1: 改常數與其測試**

`resources/map_data.gd:6` 改為：
```gdscript
const LOCAL_SIZE := 16   # 每張 local map 一律 LOCAL_SIZE × LOCAL_SIZE（無縫拼接需等邊長）
```
`tests/resources/test_map_data.gd` 的 `test_local_size_constant_is_10` 改名 `test_local_size_constant_is_16`、斷言 `assert_eq(MapData.LOCAL_SIZE, 16, ...)`。

- [ ] **Step 2: 跑內容尺寸測試，確認現在會 FAIL**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests -ginclude_subdirs -gselect=test_world_maps.gd -gexit`
Expected: FAIL（每張 content map 仍是 10×10）。

- [ ] **Step 3: 寫並執行一次性遷移腳本（Python，放 scratchpad、不進 repo）**

規則：有 `neighbors` 的圖（野外）補 `.`、無 `neighbors` 的圖（城鎮/室內）補 `#`；grid 四周各補 3 圈；所有 `pos`/`rect(x,y)`/`door`/`entries.*.pos` 座標 +3；保持欄位順序與「entities 一行一項」的既有排版。

```python
#!/usr/bin/env python3
import json
from pathlib import Path

MAPS = Path("content/maps")
PAD = 3
NEW = 16

def j(v):
    return json.dumps(v, ensure_ascii=False, separators=(", ", ": "))

def pad_grid(grid, ch):
    w = len(grid[0])
    full = ch * (w + 2 * PAD)
    out = [full] * PAD + [ch * PAD + r + ch * PAD for r in grid] + [full] * PAD
    assert len(out) == NEW and all(len(r) == NEW for r in out), "尺寸不對"
    return out

def shift(p):
    return [p[0] + PAD, p[1] + PAD]

def dump_map(root):
    parts = []
    for k, v in root.items():
        if k == "grid":
            rows = ",\n".join('    "%s"' % r for r in v)
            parts.append('  "grid": [\n%s\n  ]' % rows)
        elif k == "entities":
            rows = ",\n".join("    " + j(e) for e in v)
            parts.append('  "entities": [\n%s\n  ]' % rows)
        elif k == "entries":
            rows = ",\n".join('    "%s": %s' % (n, j(e)) for n, e in v.items())
            parts.append('  "entries": {\n%s\n  }' % rows)
        else:
            parts.append('  "%s": %s' % (k, j(v)))
    return "{\n" + ",\n".join(parts) + "\n}\n"

for path in sorted(MAPS.glob("*.json")):
    root = json.loads(path.read_text(encoding="utf-8"))
    ch = "." if root.get("neighbors") else "#"
    root["grid"] = pad_grid(root["grid"], ch)
    for e in root.get("entities", []):
        if "pos" in e:
            e["pos"] = shift(e["pos"])
        if "rect" in e:
            e["rect"] = [e["rect"][0] + PAD, e["rect"][1] + PAD, e["rect"][2], e["rect"][3]]
        if "door" in e:
            e["door"] = shift(e["door"])
    for entry in root.get("entries", {}).values():
        entry["pos"] = shift(entry["pos"])
    path.write_text(dump_map(root), encoding="utf-8")
    print("migrated", path.name)
```

Run: `python3 <scratchpad>/migrate_maps_16.py`（於 repo 根目錄）
Expected: 10 行 `migrated …`。抽查 `town_oak.json`：grid 16 行 × 16 字、四周 `#`；portal `[7, 11]`、gate entry `[7, 10]`。抽查 `wild_nw.json`：四周 `.`、town portal/decoration `[9, 9]`。

- [ ] **Step 4: 更新 quest reach 座標**

`content/quests/goblin_menace.json` 的 `{"type":"reach","map":"wild_ne","pos":[6,6],...}` 改 `pos: [9, 9]`（全 content/quests 僅此一處引用格座標）。

- [ ] **Step 5: 跑 quest lint**

Run: `godot --headless --path . --script res://tools/quest_lint_cli.gd`
Expected: 通過（exit 0）。若報 reach 格不可達，檢查 wild_ne 遷移結果。

- [ ] **Step 6: 跑全 suite，機械式修座標測試**

Run: 全套 GUT 指令。
規則：**用合成 MapData 的引擎/呈現層測試一律不動**（它們尺寸無關；若失敗＝真回歸，回頭查）。要改的是寫死 content 座標的內容層測試，全部 +3：
- `tests/content/test_world_maps.gd`：town link `(6,6)→(9,9)`、`(4,6)→(7,9)`、`(4,7)→(7,10)`、`(4,8)→(7,11)`；流浪商人 `(4,4)→(7,7)`；五店門/footprint `(1,1)→(4,4)`、`(2,1)→(5,4)`、`(1,2)→(4,5)`、`(4,4)→(7,7)`；questgiver `(3,7)→(6,10)`、`(5,5)→(8,8)`；遭遇 `(2,2)→(5,5)`；decoration `(6,6)→(9,9)`。
- `tests/content/test_quest_content.gd`：`(2,2)→(5,5)`、`(6,2)→(9,5)`、`(3,7)→(6,10)`、`(2,4)→(5,7)`、`(2,6)→(5,9)`。
- `tests/content/test_int_oak_general/inn/mage/smithy/temple.gd`、`test_town_interiors_integration.gd`、`test_oak_antidote_scenes.gd`、`test_quest_flows.gd`、`test_quest_lint.gd`、`tests/autoload/test_save_system_integration.gd`、`test_map_manager.gd`：凡引用實際 content 座標處 +3。
Expected（修完重跑）: 全綠。

- [ ] **Step 7: Commit**

```bash
git add -A && git commit -m "feat(world): LOCAL_MAP_SIZE 16×16，既有地圖置中遷移（+3,+3）"
```

---

### Task 2: MapData／MapImporter——`travel` entity 與 `continent` 欄位

**Files:**
- Modify: `resources/map_data.gd`（欄位 + 查詢函式）
- Modify: `engine/map/map_importer.gd`（`parse()` 頂層欄位 + `_parse_entities` match）
- Test: `tests/engine/map/test_map_importer.gd`（追加）

**Interfaces:**
- Produces: `MapData.continent: String`、`MapData.travels: Array[{pos:Vector2i, node:String}]`、`has_travel(pos)->bool`、`get_travel(pos)->Dictionary`；map JSON 頂層 `"continent"`、entity `{"type":"travel","pos":[x,y],"node":"<id>"}`。

- [ ] **Step 1: 寫失敗測試**（追加到 `tests/engine/map/test_map_importer.gd`，沿用該檔既有最小 JSON 風格）

```gdscript
func test_parses_continent_field() -> void:
	var m := MapImporter.parse('{"grid":["@."],"continent":"oak"}')
	assert_eq(m.continent, "oak")

func test_continent_defaults_to_empty() -> void:
	var m := MapImporter.parse('{"grid":["@."]}')
	assert_eq(m.continent, "")

func test_parses_travel_entity() -> void:
	var m := MapImporter.parse('{"grid":["@..."],"entities":[{"type":"travel","pos":[2,0],"node":"oak_caravan"}]}')
	assert_true(m.has_travel(Vector2i(2, 0)))
	assert_eq(String(m.get_travel(Vector2i(2, 0))["node"]), "oak_caravan")
	assert_false(m.has_travel(Vector2i(1, 0)))

func test_travel_entity_without_node_fails_parse() -> void:
	assert_null(MapImporter.parse('{"grid":["@."],"entities":[{"type":"travel","pos":[1,0]}]}'))
```

- [ ] **Step 2: 跑單檔確認 FAIL**

Run: `-gselect=test_map_importer.gd`。Expected: 新測試 FAIL（`continent`/`has_travel` 不存在）。

- [ ] **Step 3: 實作**

`resources/map_data.gd`：在 `vendors`（現 :24 附近）後加欄位，在 `has_vendor/get_vendor` 後加查詢（同樣式線性掃）：
```gdscript
@export var continent: String = ""         # 所屬大陸 id（content/world/continents.json）
@export var travels: Array = []            # [{ pos:Vector2i, node:String }]
```
```gdscript
func has_travel(pos: Vector2i) -> bool:
	for t in travels:
		if t["pos"] == pos:
			return true
	return false

func get_travel(pos: Vector2i) -> Dictionary:
	for t in travels:
		if t["pos"] == pos:
			return t
	return {}
```
`engine/map/map_importer.gd`：
1. `parse()` 讀 `theme/name/neighbors` 的區塊（:34-39）加：`map.continent = String(root.get("continent", ""))`。
2. `_parse_entities()`：結果集合加 `travels`（比照 `vendors` 初始化與回傳 dict），match（:121）加 case：
```gdscript
"travel":
	if not e.has("node"):
		return null
	travels.append({ "pos": pos, "node": String(e["node"]) })
```
3. `parse()` 指派區（比照 `map.vendors = entities["vendors"]`，:51）加 `map.travels = entities["travels"]`。

- [ ] **Step 4: 跑單檔轉綠、跑全 suite**

Run: `-gselect=test_map_importer.gd` → PASS；全套 → 全綠。

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(map): travel entity 與 continent 欄位（importer+MapData）"
```

---

### Task 3: 旅行網資料層（TravelCatalog＋content 節點＋車隊長對話）

**Files:**
- Create: `content/world/travel_network.json`
- Create: `presentation/world/travel_catalog.gd`
- Create: `content/dialogues/oak_caravan_master.json`
- Modify: `content/maps/town_oak.json`、`content/maps/wild_se.json`（16×16 座標系）
- Modify（如需）: 對話 require 評估器（flag 條件支援，見 Step 3）
- Test: `tests/presentation/test_travel_catalog.gd`、`tests/content/test_travel_network.gd`

**Interfaces:**
- Consumes: Task 2 的 `MapData.travels`。
- Produces: `TravelCatalog.load_network() -> Array`、`TravelCatalog.node_entry(node_id:String) -> Dictionary`、`TravelCatalog.unlocked_destinations(state, current_node_id:String) -> Array`。節點 schema：`{id, name, continent, map, entry, unlock}`（`unlock` 空字串＝預設解鎖，否則為 `GameState.flags` 旗標名）。

- [ ] **Step 1: 寫失敗測試**

`tests/presentation/test_travel_catalog.gd`：
```gdscript
extends GutTest

class FakeState:
	var flags := {}
	func has_flag(f: String) -> bool:
		return flags.has(f)

func test_network_loads_nodes() -> void:
	assert_gt(TravelCatalog.load_network().size(), 0)

func test_node_entry_finds_by_id() -> void:
	var n := TravelCatalog.node_entry("oak_caravan")
	assert_eq(String(n["map"]), "town_oak")

func test_unlocked_excludes_current_and_locked() -> void:
	var s := FakeState.new()
	var dests := TravelCatalog.unlocked_destinations(s, "oak_caravan")
	for d in dests:
		assert_ne(String(d["id"]), "oak_caravan")
		assert_ne(String(d["id"]), "oak_se_camp")  # 未解鎖不出現

func test_unlocked_includes_after_flag() -> void:
	var s := FakeState.new()
	s.flags["travel_se_camp"] = true
	var ids := []
	for d in TravelCatalog.unlocked_destinations(s, "oak_caravan"):
		ids.append(String(d["id"]))
	assert_has(ids, "oak_se_camp")
```
`tests/content/test_travel_network.gd`（沿用 `test_world_maps.gd` 載地圖的方式）：
```gdscript
extends GutTest

func _load_map(id: String) -> MapData:
	var f := FileAccess.open("res://content/maps/%s.json" % id, FileAccess.READ)
	return MapImporter.parse(f.get_as_text())

func test_nodes_reference_existing_maps_and_entries() -> void:
	var seen := {}
	for n in TravelCatalog.load_network():
		var id := String(n["id"])
		assert_false(seen.has(id), "node id 重複: " + id)
		seen[id] = true
		var m := _load_map(String(n["map"]))
		assert_not_null(m, "map 不存在: " + String(n["map"]))
		assert_true(m.has_entry(String(n["entry"])), "entry 不存在: " + String(n["entry"]))

func test_travel_tiles_reference_known_nodes() -> void:
	var known := {}
	for n in TravelCatalog.load_network():
		known[String(n["id"])] = true
	for path in DirAccess.get_files_at("res://content/maps"):
		if not path.ends_with(".json"):
			continue
		var m := _load_map(path.trim_suffix(".json"))
		for t in m.travels:
			assert_true(known.has(String(t["node"])), "未知 travel node: " + String(t["node"]))
```

- [ ] **Step 2: 跑兩單檔確認 FAIL**（`TravelCatalog` 不存在）

- [ ] **Step 3: 實作資料層與 content**

`presentation/world/travel_catalog.gd`（比照 `vendor_catalog.gd`；查詢名用 `node_entry` 避開 `Object.get`）：
```gdscript
class_name TravelCatalog
extends Object

const PATH := "res://content/world/travel_network.json"

static func load_network() -> Array:
	var f := FileAccess.open(PATH, FileAccess.READ)
	if f == null:
		return []
	var root: Variant = JSON.parse_string(f.get_as_text())
	if typeof(root) != TYPE_DICTIONARY:
		return []
	return root.get("nodes", [])

static func node_entry(node_id: String) -> Dictionary:
	for n in load_network():
		if String(n.get("id", "")) == node_id:
			return n
	return {}

# 已解鎖且非目前節點的目的地。unlock == "" ＝ 預設解鎖。
static func unlocked_destinations(state, current_node_id: String) -> Array:
	var out: Array = []
	for n in load_network():
		if String(n.get("id", "")) == current_node_id:
			continue
		var unlock := String(n.get("unlock", ""))
		if unlock == "" or state.has_flag(unlock):
			out.append(n)
	return out
```
`content/world/travel_network.json`：
```json
{
  "nodes": [
    { "id": "oak_caravan", "name": "橡鎮車隊點", "continent": "oak", "map": "town_oak", "entry": "caravan", "unlock": "" },
    { "id": "oak_se_camp", "name": "毒澤營地", "continent": "oak", "map": "wild_se", "entry": "camp", "unlock": "travel_se_camp" }
  ]
}
```
`content/maps/town_oak.json`（16×16 座標；(9..11,11) 為空地板，先驗證 grid 再放）entities 追加、entries 追加：
```json
{ "type": "travel", "pos": [10, 11], "node": "oak_caravan" },
{ "type": "questgiver", "pos": [11, 11], "dialogue": "oak_caravan_master", "sprite": "oak_guard" }
```
```json
"caravan": { "pos": [9, 11], "facing": "E" }
```
（sprite 暫借既有 `oak_guard`，M-B 美術批次再換。）
`content/maps/wild_se.json` entities 追加 travel、頂層追加 entries：
```json
{ "type": "travel", "pos": [11, 7], "node": "oak_se_camp" }
```
```json
"entries": { "camp": { "pos": [10, 7], "facing": "E" } }
```
`content/dialogues/oak_caravan_master.json`：
```json
{
  "id": "oak_caravan_master",
  "start": "root",
  "nodes": {
    "root": {
      "text": "車隊長靠在貨車旁，用炭筆在木板上勾著路線。「要搭車？橡境這幾條道，我閉著眼都走得。」",
      "choices": [
        { "text": "「毒澤那頭有落腳處嗎？」", "require": {"flag_unset": "travel_se_camp"},
          "effects": [{"op": "set_flag", "flag": "travel_se_camp"}], "goto": "unlocked" },
        { "text": "「沒事，看看。」", "goto": null }
      ]
    },
    "unlocked": {
      "text": "車隊長在木板上多畫了一個叉：「毒澤東緣有個獵人營地，車能到。想去，就到旁邊的車隊點跟夥計說一聲。」",
      "choices": [ { "text": "…", "goto": null } ]
    }
  }
}
```
**require 的 flag 條件**：先查對話 require 評估器（`engine/dialogue/` 下，`quest_inactive` 等 key 的處理處）。若已有 flag 條件 key 就沿用其名；若無，**加 `flag_set`／`flag_unset` 兩個 key**（TDD：在該評估器的測試檔加最小測試——`flag_unset` 在旗標存在時隱藏選項、`flag_set` 反之），實作各一行查 `ctx.has_flag(...)`。

- [ ] **Step 4: 跑單檔轉綠、跑 quest lint（對話引用交叉檢查）、跑全 suite**

Expected: 全綠、lint exit 0。

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(travel): 旅行網資料層（TravelCatalog+橡境雙節點+車隊長解鎖對話）"
```

---

### Task 4: TravelOverlay 選單＋main 接線（旅行流程可玩）

**Files:**
- Create: `presentation/ui/travel_overlay.gd`
- Modify: `presentation/world/main.gd`（`_try_travel`、overlay 建立、選單守衛）
- Test: `tests/presentation/test_travel_overlay.gd`

**Interfaces:**
- Consumes: `TravelCatalog.unlocked_destinations`、`MapData.has_travel/get_travel`、`main._enter_via_link(map_id, entry_name)`（既有，main.gd:207）。
- Produces: `TravelOverlay.open(nodes: Array)`、`close()`、`is_open() -> bool`、`signal travel_chosen(node: Dictionary)`、`signal finished`。

- [ ] **Step 1: 寫失敗測試** `tests/presentation/test_travel_overlay.gd`：

```gdscript
extends GutTest

func _key(code: int) -> InputEventKey:
	var e := InputEventKey.new()
	e.keycode = code
	e.pressed = true
	return e

func _nodes() -> Array:
	return [
		{ "id": "a", "name": "甲地" },
		{ "id": "b", "name": "乙地" },
	]

func test_open_renders_names() -> void:
	var o := TravelOverlay.new()
	add_child_autofree(o)
	o.open(_nodes())
	assert_true(o.is_open())
	assert_string_contains(o.list_text(), "甲地")
	assert_string_contains(o.list_text(), "乙地")

func test_enter_emits_chosen_with_cursor_node() -> void:
	var o := TravelOverlay.new()
	add_child_autofree(o)
	o.open(_nodes())
	watch_signals(o)
	o._unhandled_input(_key(KEY_DOWN))
	o._unhandled_input(_key(KEY_ENTER))
	assert_signal_emitted(o, "travel_chosen")
	var n: Dictionary = get_signal_parameters(o, "travel_chosen")[0]
	assert_eq(String(n["id"]), "b")
	assert_false(o.is_open())

func test_escape_closes_and_finishes() -> void:
	var o := TravelOverlay.new()
	add_child_autofree(o)
	o.open(_nodes())
	watch_signals(o)
	o._unhandled_input(_key(KEY_ESCAPE))
	assert_signal_emitted(o, "finished")
	assert_false(o.is_open())

func test_empty_list_shows_placeholder_and_enter_noop() -> void:
	var o := TravelOverlay.new()
	add_child_autofree(o)
	o.open([])
	watch_signals(o)
	o._unhandled_input(_key(KEY_ENTER))
	assert_signal_not_emitted(o, "travel_chosen")
	assert_string_contains(o.list_text(), "沒有")
```

- [ ] **Step 2: 跑單檔確認 FAIL**

- [ ] **Step 3: 實作 `presentation/ui/travel_overlay.gd`**（比照 VendorOverlay：CanvasLayer、layer 11、純程式建構、比例 anchor）

```gdscript
class_name TravelOverlay
extends CanvasLayer

signal travel_chosen(node: Dictionary)
signal finished

var _nodes: Array = []
var _cursor := 0
var _title: Label
var _list: Label

func _ready() -> void:
	layer = 11
	visible = false
	var panel := Panel.new()
	panel.anchor_left = 0.32
	panel.anchor_right = 0.68
	panel.anchor_top = 0.30
	panel.anchor_bottom = 0.70
	add_child(panel)
	var box := VBoxContainer.new()
	box.set_anchors_preset(Control.PRESET_FULL_RECT)
	box.offset_left = 16
	box.offset_top = 12
	box.offset_right = -16
	box.offset_bottom = -12
	panel.add_child(box)
	_title = Label.new()
	_title.text = "旅行——選擇目的地"
	box.add_child(_title)
	_list = Label.new()
	_list.size_flags_vertical = Control.SIZE_EXPAND_FILL
	box.add_child(_list)
	var hint := Label.new()
	hint.text = "↑↓ 選擇　Enter 出發　Esc 離開"
	box.add_child(hint)
	set_process_unhandled_input(false)

func is_open() -> bool:
	return visible

func list_text() -> String:
	return _list.text

func open(nodes: Array) -> void:
	_nodes = nodes
	_cursor = 0
	visible = true
	set_process_unhandled_input(true)
	_render()

func close() -> void:
	visible = false
	set_process_unhandled_input(false)

func _render() -> void:
	if _nodes.is_empty():
		_list.text = "（目前沒有可前往的目的地）"
		return
	var lines: Array = []
	for i in _nodes.size():
		var n: Dictionary = _nodes[i]
		var mark = "▶ " if i == _cursor else "　 "
		lines.append("%s%s" % [mark, String(n.get("name", n.get("id", "?")))])
	_list.text = "\n".join(lines)

func _unhandled_input(event: InputEvent) -> void:
	if not visible or not (event is InputEventKey) or not event.pressed or event.echo:
		return
	get_viewport().set_input_as_handled()
	match event.keycode:
		KEY_ESCAPE:
			close()
			finished.emit()
		KEY_UP:
			if _nodes.size() > 0:
				_cursor = (_cursor - 1 + _nodes.size()) % _nodes.size()
				_render()
		KEY_DOWN:
			if _nodes.size() > 0:
				_cursor = (_cursor + 1) % _nodes.size()
				_render()
		KEY_ENTER, KEY_KP_ENTER:
			if _nodes.size() > 0:
				var n: Dictionary = _nodes[_cursor]
				close()
				travel_chosen.emit(n)
```
（測試在 tree 外呼叫 `_unhandled_input` 時 `get_viewport()` 為 null 會炸——`add_child_autofree` 已入 tree，安全。）

- [ ] **Step 4: main.gd 接線**

1. 宣告 `var _travel_overlay: TravelOverlay`，在建立 vendor overlay 處（main.gd:99 附近）比照建立：
```gdscript
_travel_overlay = TravelOverlay.new()
add_child(_travel_overlay)
_travel_overlay.travel_chosen.connect(_on_travel_chosen)
_travel_overlay.finished.connect(_on_travel_finished)
```
2. `_on_entered_cell()` 內、`_try_vendor(local)` 呼叫點（:173）旁，同型加：踩到 travel 格開選單。
```gdscript
func _try_travel(pos: Vector2i) -> bool:
	var map := MapManager.current_map
	if not map.has_travel(pos):
		return false
	var node_id := String(map.get_travel(pos)["node"])
	_player.set_enabled(false)
	_travel_overlay.open(TravelCatalog.unlocked_destinations(GameState, node_id))
	return true

func _on_travel_finished() -> void:
	_player.set_enabled(true)

func _on_travel_chosen(node: Dictionary) -> void:
	await _enter_via_link(String(node["map"]), String(node["entry"]))
	# _enter_via_link 已推「你來到…」訊息並重新啟用玩家
```
3. `_unhandled_input` 的選單守衛（:472-477，「對話或商店開啟中 return」處）把 `_travel_overlay.is_open()` 加入同一守衛。

- [ ] **Step 5: 跑單檔轉綠、全 suite 綠**

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "feat(travel): TravelOverlay 目的地選單＋踩格觸發＋跨叢集傳送"
```

---

### Task 5: 世界地圖總覽畫面（continents.json＋M 鍵）

**Files:**
- Create: `content/world/continents.json`
- Create: `presentation/world/continent_catalog.gd`
- Create: `presentation/ui/world_map_screen.gd`
- Modify: `content/maps/*.json`（全部補 `"continent": "oak"`）
- Modify: `presentation/world/main.gd`（KEY_B 換法術、KEY_M 開世界地圖、`_menus`）
- Modify: `presentation/ui/character_panel.gd:505`（分頁標籤）
- Test: `tests/presentation/test_continent_catalog.gd`、`tests/content/test_continents.gd`

**Interfaces:**
- Consumes: `MapData.continent`（Task 2）、`TravelCatalog.load_network`（Task 3）、`MapManager.current_map`、`GameState.has_flag`。
- Produces: `ContinentCatalog.load_all() -> Array`、`ContinentCatalog.find(id:String) -> Dictionary`；`WorldMapScreen.open()/close()/is_open()`＋`signal closed`（與 QuestLog 同介面，掛進 `main._toggle_menu`）。

- [ ] **Step 1: 寫失敗測試**

`tests/presentation/test_continent_catalog.gd`：
```gdscript
extends GutTest

func test_loads_six_continents() -> void:
	assert_eq(ContinentCatalog.load_all().size(), 6)

func test_find_by_id() -> void:
	assert_eq(String(ContinentCatalog.find("oak")["name"]), "橡境")
	assert_true(ContinentCatalog.find("nope").is_empty())
```
`tests/content/test_continents.gd`：
```gdscript
extends GutTest

func test_registry_rects_are_ratio_coords() -> void:
	for c in ContinentCatalog.load_all():
		var r: Array = c["rect"]
		assert_eq(r.size(), 4)
		for v in r:
			assert_between(float(v), 0.0, 1.0)

func test_every_map_tagged_with_known_continent() -> void:
	var known := {}
	for c in ContinentCatalog.load_all():
		known[String(c["id"])] = true
	for path in DirAccess.get_files_at("res://content/maps"):
		if not path.ends_with(".json"):
			continue
		var f := FileAccess.open("res://content/maps/" + path, FileAccess.READ)
		var m := MapImporter.parse(f.get_as_text())
		assert_ne(m.continent, "", path + " 缺 continent")
		assert_true(known.has(m.continent), path + " continent 未註冊")

func test_travel_nodes_use_known_continents() -> void:
	var known := {}
	for c in ContinentCatalog.load_all():
		known[String(c["id"])] = true
	for n in TravelCatalog.load_network():
		assert_true(known.has(String(n["continent"])))
```

- [ ] **Step 2: 跑單檔確認 FAIL**

- [ ] **Step 3: 實作資料與 content**

`content/world/continents.json`（rect＝世界地圖上的比例座標 [x,y,w,h]；佈局依 ref §B 向心結構：帝心居中、橡境/霜垠/碎島海外環、赤沙/翠淵內環）：
```json
{
  "continents": [
    { "id": "oak", "name": "橡境", "rect": [0.10, 0.60, 0.17, 0.20] },
    { "id": "fro", "name": "霜垠", "rect": [0.28, 0.08, 0.22, 0.18] },
    { "id": "ver", "name": "赤沙", "rect": [0.62, 0.56, 0.24, 0.22] },
    { "id": "dee", "name": "翠淵", "rect": [0.64, 0.14, 0.22, 0.24] },
    { "id": "imp", "name": "帝心", "rect": [0.38, 0.36, 0.22, 0.22] },
    { "id": "isl", "name": "碎島海", "rect": [0.06, 0.14, 0.16, 0.30] }
  ]
}
```
`presentation/world/continent_catalog.gd`：
```gdscript
class_name ContinentCatalog
extends Object

const PATH := "res://content/world/continents.json"

static func load_all() -> Array:
	var f := FileAccess.open(PATH, FileAccess.READ)
	if f == null:
		return []
	var root: Variant = JSON.parse_string(f.get_as_text())
	if typeof(root) != TYPE_DICTIONARY:
		return []
	return root.get("continents", [])

static func find(id: String) -> Dictionary:
	for c in load_all():
		if String(c.get("id", "")) == id:
			return c
	return {}
```
全部 `content/maps/*.json` 頂層（`"name"` 之後）加 `"continent": "oak"`。

- [ ] **Step 4: 實作 `presentation/ui/world_map_screen.gd`**（QuestLog 開關介面＋自繪 canvas；全部比例式）

```gdscript
class_name WorldMapScreen
extends CanvasLayer

signal closed

var _canvas: _MapCanvas

func _ready() -> void:
	layer = 10
	visible = false
	var dim := ColorRect.new()
	dim.color = Color(0, 0, 0, 0.55)
	dim.set_anchors_preset(Control.PRESET_FULL_RECT)
	add_child(dim)
	_canvas = _MapCanvas.new()
	_canvas.anchor_left = 0.08
	_canvas.anchor_right = 0.92
	_canvas.anchor_top = 0.08
	_canvas.anchor_bottom = 0.92
	add_child(_canvas)
	set_process_unhandled_input(false)

func is_open() -> bool:
	return visible

func open() -> void:
	_canvas.current_continent = MapManager.current_map.continent if MapManager.current_map else ""
	_canvas.unlocked_nodes = TravelCatalog.unlocked_destinations(GameState, "")
	visible = true
	set_process_unhandled_input(true)
	_canvas.queue_redraw()

func close() -> void:
	visible = false
	set_process_unhandled_input(false)
	closed.emit()

func _unhandled_input(event: InputEvent) -> void:
	if not visible or not (event is InputEventKey) or not event.pressed or event.echo:
		return
	if event.keycode == KEY_ESCAPE:
		get_viewport().set_input_as_handled()
		close()

class _MapCanvas:
	extends Control

	const SEA := Color(0.13, 0.17, 0.24)
	const LAND := Color(0.45, 0.38, 0.26)
	const LAND_HERE := Color(0.72, 0.58, 0.30)
	const EDGE := Color(0.85, 0.78, 0.60)

	var current_continent := ""
	var unlocked_nodes: Array = []

	func _draw() -> void:
		var font := ThemeDB.fallback_font
		draw_rect(Rect2(Vector2.ZERO, size), SEA)
		for c in ContinentCatalog.load_all():
			var r: Array = c["rect"]
			var rect := Rect2(size.x * float(r[0]), size.y * float(r[1]), size.x * float(r[2]), size.y * float(r[3]))
			var here: bool = String(c["id"]) == current_continent
			draw_rect(rect, LAND_HERE if here else LAND)
			draw_rect(rect, EDGE, false, 3.0 if here else 1.0)
			var fs := int(size.y * 0.035)
			draw_string(font, rect.position + Vector2(8, fs + 6), String(c["name"]), HORIZONTAL_ALIGNMENT_LEFT, -1, fs, EDGE)
			if here:
				draw_string(font, rect.position + Vector2(8, fs * 2 + 10), "◆ 目前位置", HORIZONTAL_ALIGNMENT_LEFT, -1, int(fs * 0.7), Color.WHITE)
			# 已解鎖旅行節點：列在該大陸色塊下緣
			var line := 0
			for n in unlocked_nodes:
				if String(n.get("continent", "")) != String(c["id"]):
					continue
				line += 1
				var y := rect.position.y + rect.size.y - 8 - (line - 1) * fs * 0.8
				draw_circle(Vector2(rect.position.x + 10, y - fs * 0.25), 4.0, Color(0.95, 0.85, 0.4))
				draw_string(font, Vector2(rect.position.x + 20, y), String(n["name"]), HORIZONTAL_ALIGNMENT_LEFT, -1, int(fs * 0.65), Color(0.95, 0.9, 0.75))
		var hint_fs := int(size.y * 0.03)
		draw_string(font, Vector2(8, size.y - 8), "M／Esc 關閉", HORIZONTAL_ALIGNMENT_LEFT, -1, hint_fs, EDGE)
```
註：`open()` 對 `unlocked_destinations` 傳 `""` 當 current（顯示全部已解鎖節點，含所在地）。

- [ ] **Step 5: main.gd 鍵位與接線**

1. `main.gd:484` 法術分頁 `KEY_M` → `KEY_B`；`presentation/ui/character_panel.gd:505` 標籤 `"法術 (M)"` → `"法術 (B)"`。
2. 建立 `_world_map = WorldMapScreen.new()`（比照 `_quest_log`，main.gd:102-104）、`add_child`、`closed.connect(_on_menu_closed)`、加入 `_menus`（:113）。
3. `_unhandled_input` 加分支：`elif event.keycode == KEY_M: _toggle_menu(_world_map)`。

- [ ] **Step 6: 跑兩單檔轉綠、全 suite 綠**

- [ ] **Step 7: Commit**

```bash
git add -A && git commit -m "feat(worldmap): 世界地圖總覽（六大陸+目前位置+旅行節點；M 鍵，法術改 B）"
```

---

### Task 6: 收尾驗證與 spec 修訂

**Files:**
- Modify: `docs/superpowers/specs/2026-07-05-a4-世界拓撲與尺度-design.md`（落地修訂記錄）

- [ ] **Step 1: 全套驗證**

Run（依序）:
```bash
godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests -ginclude_subdirs -gexit
godot --headless --path . --script res://tools/quest_lint_cli.gd
godot --headless --path . --script res://tools/loot_lint_cli.gd
```
Expected: 全綠、兩 lint exit 0。

- [ ] **Step 2: Boot 煙霧測試（16×16 效能/載入驗證）**

Run: `godot --headless --path . --quit-after 120 2>&1 | grep -iE "error|warning" | head -30`
Expected: 無 ERROR（既有無害 warning 可忽略；出現新 ERROR 即回歸，須查明）。

- [ ] **Step 3: spec 落地修訂**

在 spec 檔尾追加「落地修訂（2026-07-05）」小節：①解鎖機制簡化＝復用 `GameState.flags`（`travel_*` 慣例＋既有 `set_flag` effect），不新增存檔欄位、save 版本維持 12；②世界地圖鍵＝M（法術分頁改 B）；③demo 內容＝橡鎮車隊點⇄毒澤營地＋車隊長解鎖對話。Commit：
```bash
git add -A && git commit -m "docs(spec): A4 落地修訂（flags 解鎖簡化、M/B 鍵、demo 節點）"
```

- [ ] **Step 4: 人工 gate 清單（回報總監，不自動執行）**

`./run.sh` 走查：①四野漫遊無縫、16×16 視野與效能體感；②進橡鎮找車隊長對話解鎖→踩車隊點→旅行選單→抵達毒澤營地→旅行回程；③存檔/讀檔後解鎖仍在；④M 開世界地圖（目前大陸高亮、節點標記）、B 開法術分頁；⑤迷你地圖/任務追蹤在新座標下正常。
