# A1 音訊框架 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** AudioManager autoload（BGM 依地圖切換＋crossfade、戰鬥曲進出、SFX 池、音量持久化）＋資料驅動曲目/音效 registry＋程序化佔位音，全部掛進既有遊戲流程。

**Architecture:** `AudioCatalog`（engine 層 static registry 查詢）＋`AudioManager`（autoload，雙 player crossfade 狀態機＋SFX 輪播池＋ConfigFile 音量）；map JSON 新頂層 `bgm` 欄位（照 continent 模式）；main.gd 在 4 個「地圖就緒」點與戰鬥進出點呼叫。未知 id／缺檔＝靜音略過。

**Tech Stack:** Godot 4.7 / GDScript / GUT / Python（一次性佔位音生成，scratchpad）。

## Global Constraints

- Spec：`docs/superpowers/specs/2026-07-05-a1-音訊框架-design.md`。
- 測試（全套）：`godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests -ginclude_subdirs -gexit`；單檔加 `-gselect=<file.gd>`。
- headless＝dummy audio driver：測試**只驗狀態邏輯**（track id、volumes、註冊表），不 assert 發聲。
- autoload 檔**不給 class_name**（沿 MapManager/GameState/SaveSystem 慣例）；`AudioCatalog` 是一般 class_name static。
- GDScript 4.7：`:=` 遇 Variant 右值用 `=`；catalog 查詢命名避開 `get`（用 `track_entry`/`sfx_entry`）。
- 新 class_name 檔案建立後跑 `godot --headless --path . --import`；`git add` 對應 `.gd.uid` 與音檔 `.import`。
- 音量**不進 SaveData**（save 版本維持 12）；用 `user://settings.cfg`。
- pre-release：不寫相容層。每 Task 結尾 commit（branch 見執行時建立）。

---

### Task 1: MapData `bgm` 欄位＋10 張地圖配曲

**Files:**
- Modify: `resources/map_data.gd`（continent 欄位旁）
- Modify: `engine/map/map_importer.gd`（parse() 頂層欄位區）
- Modify: `content/maps/*.json`（全部 10 張）
- Test: `tests/engine/map/test_map_importer.gd`（追加）

**Interfaces:**
- Produces: `MapData.bgm: String`（default `""`）；map JSON 頂層 `"bgm": "<track_id>"`。

- [ ] **Step 1: 失敗測試**（追加到 test_map_importer.gd，照 continent 測試樣式）

```gdscript
func test_parses_bgm_field() -> void:
	var m := MapImporter.parse('{"grid":["@."],"bgm":"oak_wild"}')
	assert_eq(m.bgm, "oak_wild")

func test_bgm_defaults_to_empty() -> void:
	var m := MapImporter.parse('{"grid":["@."]}')
	assert_eq(m.bgm, "")
```

- [ ] **Step 2: 跑單檔確認 FAIL**（`-gselect=test_map_importer.gd`）

- [ ] **Step 3: 實作**

`resources/map_data.gd`（`continent` 欄位下一行）：
```gdscript
@export var bgm: String = ""               # 地圖 BGM track id（content/audio/tracks.json）
```
`engine/map/map_importer.gd`（`map.continent = ...` 下一行）：
```gdscript
map.bgm = String(root.get("bgm", ""))
```
10 張地圖頂層（`"continent"` 之後）加 `"bgm"`：`wild_ne/nw/se/sw` → `"oak_wild"`；`town_oak` → `"oak_town"`；`int_oak_general/inn/mage/smithy/temple` → `"interior"`。

- [ ] **Step 4: 單檔綠、全 suite 綠**
- [ ] **Step 5: Commit** `feat(audio): map bgm 欄位＋既有地圖配曲`

---

### Task 2: AudioCatalog＋registry 內容＋佔位音

**Files:**
- Create: `engine/audio/audio_catalog.gd`
- Create: `content/audio/tracks.json`、`content/audio/sfx.json`
- Create: `content/audio/placeholder/*.wav`（12 個，Python 生成）
- Create（scratchpad，不進 repo）: `<scratchpad>/gen_placeholder_audio.py`
- Test: `tests/engine/audio/test_audio_catalog.gd`、`tests/content/test_audio_content.gd`

**Interfaces:**
- Produces: `AudioCatalog.track_entry(id: String) -> Dictionary`（`{path}`；未知回 `{}`）、`AudioCatalog.sfx_entry(id: String) -> Dictionary`。track ids：`oak_wild/oak_town/interior/combat`；sfx ids：`hit/victory/defeat/chest/menu_open/menu_close/dialogue/levelup`。

- [ ] **Step 1: 失敗測試**

`tests/engine/audio/test_audio_catalog.gd`：
```gdscript
extends GutTest

func test_track_entry_known() -> void:
	var e := AudioCatalog.track_entry("combat")
	assert_true(String(e["path"]).begins_with("res://content/audio/"))

func test_track_entry_unknown_returns_empty() -> void:
	assert_true(AudioCatalog.track_entry("nope").is_empty())

func test_sfx_entry_known_and_unknown() -> void:
	assert_false(AudioCatalog.sfx_entry("hit").is_empty())
	assert_true(AudioCatalog.sfx_entry("nope").is_empty())
```
`tests/content/test_audio_content.gd`：
```gdscript
extends GutTest

const TRACK_IDS := ["oak_wild", "oak_town", "interior", "combat"]
const SFX_IDS := ["hit", "victory", "defeat", "chest", "menu_open", "menu_close", "dialogue", "levelup"]

func test_all_track_paths_exist() -> void:
	for id in TRACK_IDS:
		var e := AudioCatalog.track_entry(id)
		assert_false(e.is_empty(), "缺 track: " + id)
		assert_true(ResourceLoader.exists(String(e["path"])), "track 檔不存在: " + id)

func test_all_sfx_paths_exist() -> void:
	for id in SFX_IDS:
		var e := AudioCatalog.sfx_entry(id)
		assert_false(e.is_empty(), "缺 sfx: " + id)
		assert_true(ResourceLoader.exists(String(e["path"])), "sfx 檔不存在: " + id)

func test_every_map_bgm_registered() -> void:
	for path in DirAccess.get_files_at("res://content/maps"):
		if not path.ends_with(".json"):
			continue
		var f := FileAccess.open("res://content/maps/" + path, FileAccess.READ)
		var m := MapImporter.parse(f.get_as_text())
		assert_ne(m.bgm, "", path + " 缺 bgm")
		assert_false(AudioCatalog.track_entry(m.bgm).is_empty(), path + " bgm 未註冊: " + m.bgm)
```

- [ ] **Step 2: 跑兩單檔確認 FAIL**

- [ ] **Step 3: 實作 `engine/audio/audio_catalog.gd`**

```gdscript
class_name AudioCatalog
extends Object

const TRACKS_PATH := "res://content/audio/tracks.json"
const SFX_PATH := "res://content/audio/sfx.json"

static func _load_registry(path: String) -> Dictionary:
	var f := FileAccess.open(path, FileAccess.READ)
	if f == null:
		return {}
	var root: Variant = JSON.parse_string(f.get_as_text())
	if typeof(root) != TYPE_DICTIONARY:
		return {}
	return root

static func track_entry(id: String) -> Dictionary:
	var e: Variant = _load_registry(TRACKS_PATH).get(id)
	return e if typeof(e) == TYPE_DICTIONARY else {}

static func sfx_entry(id: String) -> Dictionary:
	var e: Variant = _load_registry(SFX_PATH).get(id)
	return e if typeof(e) == TYPE_DICTIONARY else {}
```

- [ ] **Step 4: 佔位音生成（scratchpad Python）並產 registry**

`gen_placeholder_audio.py`（於 repo 根執行；22050Hz 16-bit mono PCM）：
```python
#!/usr/bin/env python3
import math, os, struct, wave

OUT = "content/audio/placeholder"
os.makedirs(OUT, exist_ok=True)
SR = 22050

def write_wav(name, samples):
    with wave.open(os.path.join(OUT, name), "wb") as w:
        w.setnchannels(1); w.setsampwidth(2); w.setframerate(SR)
        w.writeframes(b"".join(struct.pack("<h", max(-32767, min(32767, int(s * 32767)))) for s in samples))

def bed(freqs, secs=4.0, amp=0.10):
    n = int(SR * secs)
    return [amp * sum(math.sin(2 * math.pi * f * i / SR) for f in freqs) / len(freqs)
            * min(1.0, i / (SR * 0.05), (n - i) / (SR * 0.05)) for i in range(n)]

def blip(freq, secs=0.12, amp=0.30, slide=0.0):
    n = int(SR * secs)
    return [amp * math.sin(2 * math.pi * (freq + slide * i / n) * i / SR) * (1 - i / n) for i in range(n)]

write_wav("bgm_oak_wild.wav", bed([220, 277, 330]))
write_wav("bgm_oak_town.wav", bed([262, 330, 392]))
write_wav("bgm_interior.wav", bed([196, 247]))
write_wav("bgm_combat.wav",  bed([147, 220, 294], secs=2.0, amp=0.14))
write_wav("sfx_hit.wav",       blip(180, 0.08, 0.35))
write_wav("sfx_victory.wav",   blip(523, 0.30, 0.25, slide=523))
write_wav("sfx_defeat.wav",    blip(220, 0.40, 0.25, slide=-110))
write_wav("sfx_chest.wav",     blip(660, 0.15, 0.25, slide=220))
write_wav("sfx_menu_open.wav", blip(440, 0.06, 0.20, slide=120))
write_wav("sfx_menu_close.wav",blip(440, 0.06, 0.20, slide=-120))
write_wav("sfx_dialogue.wav",  blip(880, 0.04, 0.15))
write_wav("sfx_levelup.wav",   blip(392, 0.35, 0.25, slide=392))
print("done: 12 wav")
```
`content/audio/tracks.json`：
```json
{
  "oak_wild": { "path": "res://content/audio/placeholder/bgm_oak_wild.wav" },
  "oak_town": { "path": "res://content/audio/placeholder/bgm_oak_town.wav" },
  "interior": { "path": "res://content/audio/placeholder/bgm_interior.wav" },
  "combat":   { "path": "res://content/audio/placeholder/bgm_combat.wav" }
}
```
`content/audio/sfx.json`：
```json
{
  "hit":        { "path": "res://content/audio/placeholder/sfx_hit.wav" },
  "victory":    { "path": "res://content/audio/placeholder/sfx_victory.wav" },
  "defeat":     { "path": "res://content/audio/placeholder/sfx_defeat.wav" },
  "chest":      { "path": "res://content/audio/placeholder/sfx_chest.wav" },
  "menu_open":  { "path": "res://content/audio/placeholder/sfx_menu_open.wav" },
  "menu_close": { "path": "res://content/audio/placeholder/sfx_menu_close.wav" },
  "dialogue":   { "path": "res://content/audio/placeholder/sfx_dialogue.wav" },
  "levelup":    { "path": "res://content/audio/placeholder/sfx_levelup.wav" }
}
```
跑 `godot --headless --path . --import`（產 `.wav.import`），`git add content/audio`（含 `.import`）。

- [ ] **Step 5: 單檔綠、全 suite 綠**
- [ ] **Step 6: Commit** `feat(audio): AudioCatalog+曲目/音效 registry+程序化佔位音`

---

### Task 3: AudioManager core（crossfade／戰鬥曲／SFX 池）

**Files:**
- Create: `autoload/audio_manager.gd`
- Modify: `project.godot`（autoload 區塊 SaveSystem 後加 `AudioManager="*res://autoload/audio_manager.gd"`）
- Test: `tests/autoload/test_audio_manager.gd`

**Interfaces:**
- Consumes: `AudioCatalog.track_entry/sfx_entry`。
- Produces: `play_map_bgm(track_id: String)`、`push_combat_bgm()`、`pop_combat_bgm()`、`stop_music()`、`play_sfx(id: String)`、`current_track_id() -> String`、`is_combat_music() -> bool`（音量 API 在 Task 4）。

- [ ] **Step 1: 失敗測試** `tests/autoload/test_audio_manager.gd`（preload+new 模式，照 test_map_manager）：

```gdscript
extends GutTest

const AM := preload("res://autoload/audio_manager.gd")

func _mgr():
	var m = AM.new()
	add_child_autofree(m)
	return m

func test_play_map_bgm_sets_track() -> void:
	var m = _mgr()
	m.play_map_bgm("oak_wild")
	assert_eq(m.current_track_id(), "oak_wild")

func test_same_track_noop_keeps_playing_state() -> void:
	var m = _mgr()
	m.play_map_bgm("oak_wild")
	m.play_map_bgm("oak_wild")
	assert_eq(m.current_track_id(), "oak_wild")

func test_combat_push_pop_restores_map_track() -> void:
	var m = _mgr()
	m.play_map_bgm("oak_town")
	m.push_combat_bgm()
	assert_true(m.is_combat_music())
	assert_eq(m.current_track_id(), "combat")
	m.pop_combat_bgm()
	assert_false(m.is_combat_music())
	assert_eq(m.current_track_id(), "oak_town")

func test_map_bgm_during_combat_defers_until_pop() -> void:
	var m = _mgr()
	m.play_map_bgm("oak_town")
	m.push_combat_bgm()
	m.play_map_bgm("oak_wild")
	assert_eq(m.current_track_id(), "combat")
	m.pop_combat_bgm()
	assert_eq(m.current_track_id(), "oak_wild")

func test_stop_music_clears_state() -> void:
	var m = _mgr()
	m.play_map_bgm("oak_wild")
	m.push_combat_bgm()
	m.stop_music()
	assert_eq(m.current_track_id(), "")
	assert_false(m.is_combat_music())

func test_unknown_ids_do_not_crash() -> void:
	var m = _mgr()
	m.play_map_bgm("nope")
	m.play_sfx("nope")
	assert_eq(m.current_track_id(), "nope")
```

- [ ] **Step 2: 跑單檔確認 FAIL**

- [ ] **Step 3: 實作 `autoload/audio_manager.gd`**

```gdscript
extends Node
# Autoload：AudioManager（刻意不給 class_name，避免與 autoload 名衝突）。
# BGM 雙 player crossfade；戰鬥曲 push/pop；SFX 輪播池；未知 id/缺檔＝靜音略過。

const CROSSFADE := 1.0
const SFX_POOL_SIZE := 8
const SILENT_DB := -60.0

var _music_a: AudioStreamPlayer
var _music_b: AudioStreamPlayer
var _active: AudioStreamPlayer
var _current_track := ""
var _map_track := ""
var _in_combat := false
var _sfx_pool: Array = []
var _sfx_next := 0

func _ready() -> void:
	_ensure_buses()
	_music_a = _make_player("Music")
	_music_b = _make_player("Music")
	_music_a.finished.connect(_on_music_finished.bind(_music_a))
	_music_b.finished.connect(_on_music_finished.bind(_music_b))
	_active = _music_a
	for i in SFX_POOL_SIZE:
		_sfx_pool.append(_make_player("SFX"))

func _make_player(bus_name: String) -> AudioStreamPlayer:
	var p := AudioStreamPlayer.new()
	p.bus = bus_name
	add_child(p)
	return p

func _ensure_buses() -> void:
	for bus_name in ["Music", "SFX"]:
		if AudioServer.get_bus_index(bus_name) == -1:
			var idx := AudioServer.bus_count
			AudioServer.add_bus(idx)
			AudioServer.set_bus_name(idx, bus_name)
			AudioServer.set_bus_send(idx, "Master")

func current_track_id() -> String:
	return _current_track

func is_combat_music() -> bool:
	return _in_combat

func play_map_bgm(track_id: String) -> void:
	_map_track = track_id
	if _in_combat:
		return
	_switch_to(track_id)

func push_combat_bgm() -> void:
	if _in_combat:
		return
	_in_combat = true
	_switch_to("combat")

func pop_combat_bgm() -> void:
	if not _in_combat:
		return
	_in_combat = false
	_switch_to(_map_track)

func stop_music() -> void:
	_in_combat = false
	_current_track = ""
	_music_a.stop()
	_music_b.stop()

func play_sfx(id: String) -> void:
	var stream := _load_stream(AudioCatalog.sfx_entry(id))
	if stream == null:
		return
	var p: AudioStreamPlayer = _sfx_pool[_sfx_next]
	_sfx_next = (_sfx_next + 1) % SFX_POOL_SIZE
	p.stream = stream
	p.play()

func _switch_to(track_id: String) -> void:
	if track_id == _current_track:
		return
	_current_track = track_id
	var stream := _load_stream(AudioCatalog.track_entry(track_id))
	var outgoing := _active
	var incoming := _music_b if _active == _music_a else _music_a
	_active = incoming
	incoming.stop()
	incoming.stream = stream
	if stream != null:
		incoming.volume_db = SILENT_DB
		incoming.play()
		var t := create_tween()
		t.tween_property(incoming, "volume_db", 0.0, CROSSFADE)
	if outgoing.playing:
		var t2 := create_tween()
		t2.tween_property(outgoing, "volume_db", SILENT_DB, CROSSFADE)
		t2.tween_callback(outgoing.stop)

func _on_music_finished(p: AudioStreamPlayer) -> void:
	# 佔位/一般曲用 finished 重播成 loop（不依賴 import loop 參數）
	if p == _active and p.stream != null:
		p.play()

func _load_stream(entry: Dictionary) -> AudioStream:
	var path := String(entry.get("path", ""))
	if path == "" or not ResourceLoader.exists(path):
		return null
	return load(path) as AudioStream
```
`project.godot` autoload 區塊末尾加：`AudioManager="*res://autoload/audio_manager.gd"`。

- [ ] **Step 4: 單檔綠、全 suite 綠**
- [ ] **Step 5: Commit** `feat(audio): AudioManager（crossfade+戰鬥曲 push/pop+SFX 池）`

---

### Task 4: 音量 API＋settings.cfg 持久化

**Files:**
- Modify: `autoload/audio_manager.gd`
- Test: `tests/autoload/test_audio_manager.gd`（追加）

**Interfaces:**
- Produces: `set_volume(bus_name: String, v: float)`（clamp 0..1、寫 AudioServer、存檔）、`volume(bus_name: String) -> float`；`user://settings.cfg` `[audio]` 區。`_settings_path` 為 var 供測試注入。

- [ ] **Step 1: 失敗測試**（追加；after_each 清 temp 檔）

```gdscript
const TEST_CFG := "user://test_audio_settings.cfg"

func after_each() -> void:
	var abs := ProjectSettings.globalize_path(TEST_CFG)
	if FileAccess.file_exists(TEST_CFG):
		DirAccess.remove_absolute(abs)

func _mgr_with_cfg():
	var m = AM.new()
	m._settings_path = TEST_CFG
	add_child_autofree(m)
	return m

func test_volume_default_and_set_clamped() -> void:
	var m = _mgr_with_cfg()
	assert_eq(m.volume("Music"), 1.0)
	m.set_volume("Music", 1.5)
	assert_eq(m.volume("Music"), 1.0)
	m.set_volume("Music", -0.2)
	assert_eq(m.volume("Music"), 0.0)

func test_volume_persists_across_instances() -> void:
	var m = _mgr_with_cfg()
	m.set_volume("SFX", 0.25)
	var m2 = _mgr_with_cfg()
	assert_almost_eq(m2.volume("SFX"), 0.25, 0.001)
```

- [ ] **Step 2: 跑單檔確認 FAIL**

- [ ] **Step 3: 實作**（audio_manager.gd 追加；`_ready` 末尾呼叫 `_load_settings()`）

```gdscript
var _settings_path := "user://settings.cfg"
var _volumes := { "Master": 1.0, "Music": 1.0, "SFX": 1.0 }

func set_volume(bus_name: String, v: float) -> void:
	_apply_volume(bus_name, clampf(v, 0.0, 1.0))
	_save_settings()

func volume(bus_name: String) -> float:
	return float(_volumes.get(bus_name, 1.0))

func _apply_volume(bus_name: String, v: float) -> void:
	if not _volumes.has(bus_name):
		return
	_volumes[bus_name] = v
	var idx := AudioServer.get_bus_index(bus_name)
	if idx != -1:
		AudioServer.set_bus_volume_db(idx, linear_to_db(maxf(v, 0.0001)))
		AudioServer.set_bus_mute(idx, v <= 0.0)

func _load_settings() -> void:
	var cfg := ConfigFile.new()
	if cfg.load(_settings_path) != OK:
		return
	for b in _volumes.keys():
		_apply_volume(b, clampf(float(cfg.get_value("audio", b, _volumes[b])), 0.0, 1.0))

func _save_settings() -> void:
	var cfg := ConfigFile.new()
	cfg.load(_settings_path)  # 保留他區段；失敗＝新檔
	for b in _volumes.keys():
		cfg.set_value("audio", b, _volumes[b])
	cfg.save(_settings_path)
```
注意：`_ready` 順序＝`_ensure_buses` → players → `_load_settings()`（載入即套 bus）。

- [ ] **Step 4: 單檔綠、全 suite 綠**
- [ ] **Step 5: Commit** `feat(audio): 音量 API＋user://settings.cfg 持久化`

---

### Task 5: 遊戲流程掛點接線

**Files:**
- Modify: `presentation/world/main.gd`
- Modify: `presentation/combat/combat_layer.gd`

**Interfaces:**
- Consumes: Task 3/4 全部 API＋`MapData.bgm`。

- [ ] **Step 1: main.gd 地圖 BGM 四點**（行號以現檔為準，語意定位）

在下列四處「新地圖就緒」後各加 `AudioManager.play_map_bgm(MapManager.current_map.bgm)`：
1. `_ready` 啟動進圖完成處（`GameState.current_map_id = START_MAP_ID` 附近，~:128）
2. `_recenter_to` 末尾（~:197）——同曲時 AudioManager 內部 no-op，無縫跨圖不重啟
3. `_enter_via_link` 內 `current_map_id` 更新後（~:231）
4. `_on_loaded` 讀檔重建後（~:575）

- [ ] **Step 2: 戰鬥曲進出**

- `_start_combat_with_group`：`_set_overworld_visible(false)`（~:253）後加 `AudioManager.push_combat_bgm()`。
- `_on_combat_finished` 開頭（~:310）加：
```gdscript
	if result == CombatSystem.Result.DEFEAT:
		AudioManager.stop_music()
	else:
		AudioManager.pop_combat_bgm()
```

- [ ] **Step 3: SFX 掛點**

| 位置 | 呼叫 |
|---|---|
| combat_layer.gd `_animate_from` 傷害閃白處（~:306） | `AudioManager.play_sfx("hit")` |
| main.gd VICTORY 分支「戰鬥勝利！」（~:320） | `play_sfx("victory")` |
| main.gd DEFEAT 分支「全隊覆滅……」（~:331） | `play_sfx("defeat")` |
| main.gd `_on_chest_confirmed` 結算（~:351） | `play_sfx("chest")` |
| main.gd `_toggle_menu`：close 分支（~:523）/ open 分支（~:529） | `menu_close`／`menu_open` |
| main.gd `_on_dialogue_advanced`（~:414） | `play_sfx("dialogue")` |
| main.gd `_grant_rewards` 升級訊息處（~:471，僅升級時） | `play_sfx("levelup")` |
| main.gd `_ready` quest_event connect 旁（~:116） | `GameState.quest_event.connect(_on_quest_event_sfx)`；新函式 `func _on_quest_event_sfx(_e) -> void: AudioManager.play_sfx("dialogue")` |

- [ ] **Step 4: 驗證**：全 suite 綠；headless boot ~8 秒零 script error。
- [ ] **Step 5: Commit** `feat(audio): BGM/戰鬥曲/SFX 全掛點接線`

---

### Task 6: 收尾驗證

- [ ] **Step 1:** 全 suite＋`quest_lint_cli`＋`loot_lint_cli` 全綠/exit 0。
- [ ] **Step 2:** boot 煙霧 `godot --headless --path . --quit-after 120`：無新 ERROR。
- [ ] **Step 3:** 若實作有偏離 spec，於 spec 檔尾追加「落地修訂」；commit `docs(spec): A1 落地修訂`。
- [ ] **Step 4: 人工 gate 清單（轉交總監，不自動執行）**：`./run.sh` ①開場聞野外音床；②走進橡鎮＝crossfade 換鎮曲、進店＝室內曲、無縫跨野外圖不重啟；③遇敵切戰鬥曲、勝/逃恢復地圖曲、覆滅停樂＋defeat 音；④開箱/開關選單/對話/升級各有音；⑤存檔讀檔後曲正確；⑥改音量後重啟遊戲保留（暫用 API/後續 UI）。
