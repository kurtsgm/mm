# A2 演出框架（Cutscene）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建一套資料驅動的演出序列（cutscene timeline）引擎，並把已落地的「領航者殘響」從普通對話升級成真過場。

**Architecture:** 純資料 `CutsceneData`（engine，parse/validate）＋非同步 `CutscenePlayer`（presentation，逐 step `await`）。8 種 step 皆重用既有原料（fade/CG/對話/音訊/震動/effects）。地圖格 `scene` 加 `cutscene` 鍵觸發，沿用既有 once/require，不動 save。

**Tech Stack:** Godot 4.7（GDScript）、GUT 9.7 測試框架。

## Global Constraints

- **Godot 4.7．GDScript**：`:=` 型別推斷對 **Variant 右值**不編譯 → 該處改用顯式型別 `var x: T = ...` 或 `var x = ...`。
- **不需向後相容**：pre-release，breaking change 可接受；但本包刻意 **save-neutral（維持 `SAVE_VERSION = 12`）**。
- **UI 版面一律比例式**：anchor 比例、解析度無關、不寫死像素寬高（字級/小邊距可固定）。
- **溝通語言**：對使用者說明用繁體中文；程式碼/註解沿用專案既有繁中註解慣例。
- **不產美術/音訊資產**：CG 用 placeholder；SFX 引語意 id，缺檔靜音。
- **測試指令**：全 suite `godot --headless --path . -s addons/gut/gut_cmdln.gd -gexit`（~8s，基準 1111/1111 綠）。單目錄聚焦 `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/<sub> -ginclude_subdirs=false -gexit`。
- **git**：現在在 `main`；每個 Task 結尾 commit（不 push，push 待總監裁示）。commit 訊息結尾附 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`。

---

## 檔案結構

| 檔案 | 職責 |
|---|---|
| `engine/cutscene/cutscene_data.gd`（新） | 純資料：parse/validate 一段 cutscene，畸形→null |
| `presentation/cutscene/cutscene_catalog.gd`（新） | 由 id 載 `content/cutscenes/<id>.json`→`CutsceneData` |
| `presentation/cutscene/title_card.gd`（新） | 章節標題卡 Control（比例式版面） |
| `presentation/cutscene/cutscene_player.gd`（新） | 非同步播放器：逐 step await、閘輸入、`finished`/`dialogue_advanced` 訊號 |
| `presentation/world/main.gd`（改） | 建立/接線 `_cutscene_player`；`_try_scene` 加 cutscene 分支；選單閘 |
| `content/cutscenes/nav_echo_nest.json`（新） | 首發過場內容 |
| `content/maps/wild_ne.json`（改） | (9,9) scene：`dialogue`→`cutscene` |
| `content/audio/sfx.json`（改） | 註冊 `nav_echo_sting`（路徑待補） |
| `presentation/world/scene_image_catalog.gd`（改） | 註記 `nav_echo_relic` id |
| `tests/engine/cutscene/*`、`tests/presentation/cutscene/*`（新） | 對應測試 |

---

## Task 1: CutsceneData（純資料 parse/validate）

**Files:**
- Create: `engine/cutscene/cutscene_data.gd`
- Test: `tests/engine/cutscene/test_cutscene_data.gd`

**Interfaces:**
- Consumes: 無。
- Produces:
  - `class_name CutsceneData`（extends RefCounted）
  - `static func parse(raw: Dictionary) -> CutsceneData`（畸形→null）
  - 欄位 `var id: String`、`var steps: Array`（每個元素為已補預設的 step Dictionary，必含 `type`）

**驗證規則**（鏡射 `DialogueData.parse`）：頂層非 dict / `steps` 非陣列 → null；每個 step 非 dict / `type` 非 8 型之一 → null；型別必填缺失（`cg` 無 `image`、`dialogue` 無 `dialogue`、`title_card` 無 `title`、`fade.to` ∉ {black,clear}、`audio.op` ∉ {sfx,bgm,stop}）→ null；其餘套預設。

- [ ] **Step 1: 寫失敗測試**

Create `tests/engine/cutscene/test_cutscene_data.gd`:

```gdscript
extends GutTest

func _raw() -> Dictionary:
	return {
		"id": "cs1",
		"steps": [
			{ "type": "fade", "to": "black", "duration": 0.6 },
			{ "type": "cg", "image": "img1", "hold": 3.0 },
			{ "type": "shake", "intensity": 6.0, "duration": 0.5 },
			{ "type": "audio", "op": "sfx", "id": "sting" },
			{ "type": "dialogue", "dialogue": "d1" },
			{ "type": "title_card", "title": "第一章", "subtitle": "邊陲", "hold": 2.5 },
			{ "type": "wait", "duration": 1.0 },
			{ "type": "effects", "effects": [ {"op": "set_flag", "flag": "x"} ] },
		],
	}

func test_parse_valid():
	var d := CutsceneData.parse(_raw())
	assert_not_null(d)
	assert_eq(d.id, "cs1")
	assert_eq(d.steps.size(), 8)
	assert_eq(d.steps[0]["type"], "fade")
	assert_eq(d.steps[0]["to"], "black")

func test_defaults_applied():
	var d := CutsceneData.parse({ "id": "c", "steps": [ {"type": "fade"} ] })
	assert_eq(d.steps[0]["to"], "black")       # fade.to 預設 black
	assert_almost_eq(float(d.steps[0]["duration"]), 0.5, 0.001)

func test_top_level_not_dict_returns_null():
	assert_null(CutsceneData.parse([] as Array as Variant))

func test_steps_not_array_returns_null():
	assert_null(CutsceneData.parse({ "id": "c", "steps": {} }))

func test_unknown_type_returns_null():
	assert_null(CutsceneData.parse({ "id": "c", "steps": [ {"type": "nope"} ] }))

func test_cg_missing_image_returns_null():
	assert_null(CutsceneData.parse({ "id": "c", "steps": [ {"type": "cg"} ] }))

func test_dialogue_missing_id_returns_null():
	assert_null(CutsceneData.parse({ "id": "c", "steps": [ {"type": "dialogue"} ] }))

func test_fade_bad_to_returns_null():
	assert_null(CutsceneData.parse({ "id": "c", "steps": [ {"type": "fade", "to": "purple"} ] }))

func test_audio_bad_op_returns_null():
	assert_null(CutsceneData.parse({ "id": "c", "steps": [ {"type": "audio", "op": "boom"} ] }))

func test_step_not_dict_returns_null():
	assert_null(CutsceneData.parse({ "id": "c", "steps": [ 5 ] }))
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/engine/cutscene -ginclude_subdirs=false -gexit`
Expected: FAIL（`CutsceneData` 未定義 / parse 不存在）。

- [ ] **Step 3: 寫最小實作**

Create `engine/cutscene/cutscene_data.gd`:

```gdscript
class_name CutsceneData
extends RefCounted
# 演出序列（step 陣列）。畸形（缺 type / 型別必填缺失 / steps 非陣列）→ parse 回 null。
# 純資料：不做跨檔驗證（dialogue/image/sfx id 是否存在留執行期各自容錯）。

var id: String = ""
var steps: Array = []

const _FADE_TO := ["black", "clear"]
const _AUDIO_OP := ["sfx", "bgm", "stop"]

static func parse(raw) -> CutsceneData:
	if typeof(raw) != TYPE_DICTIONARY:
		return null
	var raw_steps = raw.get("steps", null)
	if typeof(raw_steps) != TYPE_ARRAY:
		return null
	var parsed: Array = []
	for rs in raw_steps:
		var step := _parse_step(rs)
		if step.is_empty():
			return null
		parsed.append(step)
	var d := CutsceneData.new()
	d.id = String(raw.get("id", ""))
	d.steps = parsed
	return d

# 回補完預設的 step；畸形回 {}（呼叫端視為 parse 失敗）。
static func _parse_step(rs) -> Dictionary:
	if typeof(rs) != TYPE_DICTIONARY:
		return {}
	var t := String(rs.get("type", ""))
	match t:
		"fade":
			var to := String(rs.get("to", "black"))
			if not _FADE_TO.has(to):
				return {}
			return { "type": t, "to": to, "duration": float(rs.get("duration", 0.5)) }
		"title_card":
			if not rs.has("title"):
				return {}
			return { "type": t, "title": String(rs["title"]),
				"subtitle": String(rs.get("subtitle", "")), "hold": float(rs.get("hold", 2.5)) }
		"cg":
			if not rs.has("image"):
				return {}
			return { "type": t, "image": String(rs["image"]), "hold": float(rs.get("hold", 0.0)) }
		"dialogue":
			if not rs.has("dialogue"):
				return {}
			return { "type": t, "dialogue": String(rs["dialogue"]) }
		"shake":
			return { "type": t, "intensity": float(rs.get("intensity", 5.0)),
				"duration": float(rs.get("duration", 0.4)) }
		"audio":
			var op := String(rs.get("op", ""))
			if not _AUDIO_OP.has(op):
				return {}
			return { "type": t, "op": op, "id": String(rs.get("id", "")) }
		"wait":
			return { "type": t, "duration": float(rs.get("duration", 1.0)) }
		"effects":
			var eff = rs.get("effects", [])
			if typeof(eff) != TYPE_ARRAY:
				return {}
			return { "type": t, "effects": eff }
		_:
			return {}
```

- [ ] **Step 4: 跑測試確認通過**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/engine/cutscene -ginclude_subdirs=false -gexit`
Expected: PASS（全部）。

- [ ] **Step 5: Commit**

```bash
git add engine/cutscene/cutscene_data.gd tests/engine/cutscene/test_cutscene_data.gd && git commit -m "feat(cutscene): CutsceneData 純資料 parse/validate（8 step 詞彙）"
```

---

## Task 2: CutsceneCatalog（由 id 載檔）

**Files:**
- Create: `presentation/cutscene/cutscene_catalog.gd`
- Test: `tests/presentation/cutscene/test_cutscene_catalog.gd`

**Interfaces:**
- Consumes: `CutsceneData.parse`（Task 1）。
- Produces: `class_name CutsceneCatalog`；`static func load(id: String) -> CutsceneData`（缺檔/JSON 畸形→null）。

- [ ] **Step 1: 寫失敗測試**

Create `tests/presentation/cutscene/test_cutscene_catalog.gd`（用測試專用暫存檔，避免依賴內容檔）:

```gdscript
extends GutTest

const DIR := "res://tests/presentation/cutscene/_tmp"

func before_all():
	DirAccess.make_dir_recursive_absolute(ProjectSettings.globalize_path(DIR))

func _write(id: String, text: String) -> void:
	var f := FileAccess.open("%s/%s.json" % [DIR, id], FileAccess.WRITE)
	f.store_string(text)
	f.close()

func test_load_valid():
	_write("okcs", '{"id":"okcs","steps":[{"type":"wait","duration":1.0}]}')
	var d := CutsceneCatalog.load_from("%s/okcs.json" % DIR)
	assert_not_null(d)
	assert_eq(d.id, "okcs")

func test_missing_file_returns_null():
	assert_null(CutsceneCatalog.load_from("%s/nope.json" % DIR))

func test_malformed_json_returns_null():
	_write("bad", "{ not json")
	assert_null(CutsceneCatalog.load_from("%s/bad.json" % DIR))
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/presentation/cutscene -ginclude_subdirs=false -gexit`
Expected: FAIL（`CutsceneCatalog` 未定義）。

- [ ] **Step 3: 寫最小實作**

Create `presentation/cutscene/cutscene_catalog.gd`（鏡射 `DialogueCatalog`；抽出 `load_from` 供測試指定路徑）:

```gdscript
class_name CutsceneCatalog
extends Object
# cutscene id → 載 content/cutscenes/<id>.json → CutsceneData（鏡射 DialogueCatalog）。
# 檔缺/JSON 畸形/step 違規 → null。

const CUTSCENES_DIR := "res://content/cutscenes"

static func load(id: String) -> CutsceneData:
	return load_from("%s/%s.json" % [CUTSCENES_DIR, id])

static func load_from(path: String) -> CutsceneData:
	if not FileAccess.file_exists(path):
		return null
	var text := FileAccess.get_file_as_string(path)
	var raw = JSON.parse_string(text)
	if typeof(raw) != TYPE_DICTIONARY:
		return null
	return CutsceneData.parse(raw)
```

- [ ] **Step 4: 跑測試確認通過**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/presentation/cutscene -ginclude_subdirs=false -gexit`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add presentation/cutscene/cutscene_catalog.gd tests/presentation/cutscene/test_cutscene_catalog.gd && git commit -m "feat(cutscene): CutsceneCatalog 由 id 載檔（鏡射 DialogueCatalog）"
```

---

## Task 3: TitleCard（章節標題卡，比例式版面）

**Files:**
- Create: `presentation/cutscene/title_card.gd`
- Test: `tests/presentation/cutscene/test_title_card.gd`

**Interfaces:**
- Consumes: 無。
- Produces: `class_name TitleCard`（extends Control）；`func set_content(title: String, subtitle: String) -> void`；內部 `_title_label`/`_subtitle_label`（測試可讀）。

- [ ] **Step 1: 寫失敗測試**

Create `tests/presentation/cutscene/test_title_card.gd`:

```gdscript
extends GutTest

func _card() -> TitleCard:
	var c := TitleCard.new()
	add_child_autofree(c)
	return c

func test_full_rect_ratio_anchors():
	var c := _card()
	# 版面比例式（解析度無關）：占滿全螢幕作黑底標題。
	assert_almost_eq(c.anchor_left, 0.0, 0.001)
	assert_almost_eq(c.anchor_right, 1.0, 0.001)
	assert_almost_eq(c.anchor_top, 0.0, 0.001)
	assert_almost_eq(c.anchor_bottom, 1.0, 0.001)

func test_set_content_fills_labels():
	var c := _card()
	c.set_content("第一章", "邊陲在地英雄")
	assert_eq(c._title_label.text, "第一章")
	assert_eq(c._subtitle_label.text, "邊陲在地英雄")

func test_empty_subtitle_hides_it():
	var c := _card()
	c.set_content("第一章", "")
	assert_false(c._subtitle_label.visible)
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/presentation/cutscene -ginclude_subdirs=false -gexit`
Expected: FAIL（`TitleCard` 未定義）。

- [ ] **Step 3: 寫最小實作**

Create `presentation/cutscene/title_card.gd`:

```gdscript
class_name TitleCard
extends Control
# 章節標題卡：黑底、標題＋副標置中。版面比例式（占滿全螢幕），字級固定。

var _bg: ColorRect
var _title_label: Label
var _subtitle_label: Label

func _init() -> void:
	set_anchors_preset(Control.PRESET_FULL_RECT)
	mouse_filter = Control.MOUSE_FILTER_IGNORE

func _ready() -> void:
	_bg = ColorRect.new()
	_bg.color = Color(0, 0, 0, 1)
	_bg.set_anchors_preset(Control.PRESET_FULL_RECT)
	_bg.mouse_filter = Control.MOUSE_FILTER_IGNORE
	add_child(_bg)

	var vb := VBoxContainer.new()
	vb.set_anchors_preset(Control.PRESET_CENTER)
	vb.grow_horizontal = Control.GROW_DIRECTION_BOTH
	vb.grow_vertical = Control.GROW_DIRECTION_BOTH
	vb.alignment = BoxContainer.ALIGNMENT_CENTER
	vb.add_theme_constant_override("separation", 12)
	add_child(vb)

	_title_label = Label.new()
	_title_label.horizontal_alignment = HORIZONTAL_ALIGNMENT_CENTER
	_title_label.add_theme_font_size_override("font_size", 56)
	_title_label.add_theme_color_override("font_color", Color(0.93, 0.90, 0.82))
	vb.add_child(_title_label)

	_subtitle_label = Label.new()
	_subtitle_label.horizontal_alignment = HORIZONTAL_ALIGNMENT_CENTER
	_subtitle_label.add_theme_font_size_override("font_size", 28)
	_subtitle_label.add_theme_color_override("font_color", Color(0.78, 0.74, 0.66))
	vb.add_child(_subtitle_label)

func set_content(title: String, subtitle: String) -> void:
	if _title_label == null:
		# 尚未 _ready（純資料測試前設值）→ 進 tree 後補；此處直接建最小節點避免 null。
		return
	_title_label.text = title
	_subtitle_label.text = subtitle
	_subtitle_label.visible = subtitle != ""
```

> 注意：測試 `test_set_content_fills_labels` 用 `add_child_autofree` → `_ready` 已跑、labels 已建。`_init` 設 anchors 讓 `test_full_rect_ratio_anchors` 不依賴 `_ready`。

- [ ] **Step 4: 跑測試確認通過**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/presentation/cutscene -ginclude_subdirs=false -gexit`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add presentation/cutscene/title_card.gd tests/presentation/cutscene/test_title_card.gd && git commit -m "feat(cutscene): TitleCard 章節標題卡（比例式版面）"
```

---

## Task 4: CutscenePlayer 核心（play 迴圈＋wait/effects/audio＋finished）

**Files:**
- Create: `presentation/cutscene/cutscene_player.gd`
- Test: `tests/presentation/cutscene/test_cutscene_player.gd`

**Interfaces:**
- Consumes: `CutsceneData`（Task 1）、`DialogueEffects`（既有 `engine/dialogue/dialogue_effects.gd`）。
- Produces:
  - `class_name CutscenePlayer`（extends CanvasLayer）
  - `signal finished`
  - `signal dialogue_advanced(descriptions: Array)`
  - `func setup(camera, ctx) -> void`（注入相機與 effects/dialogue 用 context；ctx 預設在 main 傳 `GameState`）
  - `func play(data: CutsceneData) -> void`（async；閘輸入、逐 step、emit `finished`）
  - `func is_playing() -> bool`

本 Task 只實作 **skeleton＋play 迴圈＋wait/effects/audio 三種「無畫面」step＋finished/is_playing**。fade/cg/title/shake（Task 5）、dialogue（Task 6）之後補。未知 step 於 dispatch 時略過（防呆）。

- [ ] **Step 1: 寫失敗測試**

Create `tests/presentation/cutscene/test_cutscene_player.gd`:

```gdscript
extends GutTest

class FakeCtx:
	var gold: int = 0
	var inventory := Inventory.new()
	var flags: Dictionary = {}
	func accept_quest(_q): pass
	func advance_quest(_q): pass

func _player(ctx) -> CutscenePlayer:
	var p := CutscenePlayer.new()
	add_child_autofree(p)
	var cam := Camera3D.new()
	add_child_autofree(cam)
	p.setup(cam, ctx)
	return p

func _data(steps: Array) -> CutsceneData:
	return CutsceneData.parse({ "id": "t", "steps": steps })

func test_effects_wait_finishes_and_mutates_ctx():
	var ctx := FakeCtx.new()
	var p := _player(ctx)
	watch_signals(p)
	await p.play(_data([
		{ "type": "effects", "effects": [ {"op": "set_flag", "flag": "seen"} ] },
		{ "type": "wait", "duration": 0.05 },
	]))
	assert_signal_emitted(p, "finished")
	assert_true(ctx.flags.get("seen", false))
	assert_false(p.is_playing())

func test_audio_bgm_step_switches_track():
	var p := _player(FakeCtx.new())
	await p.play(_data([ { "type": "audio", "op": "bgm", "id": "oak_town" } ]))
	assert_eq(AudioManager.current_track_id(), "oak_town")

func test_is_playing_true_during_play():
	# wait 步驟進行中 is_playing()==true；不 await play、給它一幀後檢查。
	var p := _player(FakeCtx.new())
	p.play(_data([ { "type": "wait", "duration": 0.3 } ]))
	await get_tree().process_frame
	assert_true(p.is_playing())
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/presentation/cutscene -ginclude_subdirs=false -gexit`
Expected: FAIL（`CutscenePlayer` 未定義）。

- [ ] **Step 3: 寫最小實作**

Create `presentation/cutscene/cutscene_player.gd`:

```gdscript
class_name CutscenePlayer
extends CanvasLayer
# 資料驅動演出播放器：逐 step await（fade/title/cg/dialogue/shake/audio/wait/effects）。
# 本檔核心：play 迴圈＋wait/effects/audio。fade/cg/title/shake（step 分派見 _play_step）
# 與 dialogue 由後續實作補上。過場期間玩家已由 main disable，故安全。

signal finished
signal dialogue_advanced(descriptions: Array)

var _camera: Camera3D
var _ctx
var _playing: bool = false

func _init() -> void:
	layer = 90

func setup(camera: Camera3D, ctx) -> void:
	_camera = camera
	_ctx = ctx

func is_playing() -> bool:
	return _playing

func play(data: CutsceneData) -> void:
	if data == null:
		return
	_playing = true
	for step in data.steps:
		await _play_step(step)
	_playing = false
	finished.emit()

func _play_step(step: Dictionary) -> void:
	match String(step.get("type", "")):
		"wait":
			await get_tree().create_timer(float(step["duration"])).timeout
		"effects":
			var descs := DialogueEffects.apply(step["effects"], _ctx)
			if descs.size() > 0:
				dialogue_advanced.emit(descs)
		"audio":
			_play_audio(step)
		_:
			pass  # fade/cg/title/shake/dialogue 由後續 Task 補；未知型別略過

func _play_audio(step: Dictionary) -> void:
	match String(step["op"]):
		"sfx":
			AudioManager.play_sfx(String(step["id"]))
		"bgm":
			AudioManager.play_map_bgm(String(step["id"]))
		"stop":
			AudioManager.stop_music()
```

- [ ] **Step 4: 跑測試確認通過**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/presentation/cutscene -ginclude_subdirs=false -gexit`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add presentation/cutscene/cutscene_player.gd tests/presentation/cutscene/test_cutscene_player.gd && git commit -m "feat(cutscene): CutscenePlayer 核心（play 迴圈＋wait/effects/audio＋finished）"
```

---

## Task 5: CutscenePlayer 視覺 step（fade/cg/title/shake）

**Files:**
- Modify: `presentation/cutscene/cutscene_player.gd`
- Test: `tests/presentation/cutscene/test_cutscene_player.gd`（追加）

**Interfaces:**
- Consumes: `SceneImageCatalog`（既有）、`TitleCard`（Task 3）。
- Produces（追加到 `CutscenePlayer`）：內部 `_fade_rect: ColorRect`、`_cg_rect: TextureRect`、`_title_card: TitleCard`（測試可讀）；`_play_step` 新增 fade/cg/title_card/shake 分派。

版面/層次：本 player CanvasLayer `layer = 90`；fade rect（黑、alpha 動）、cg rect、title card 為其 Control 子節點。cg/title 用比例 anchor 全螢幕。`cg`/`title_card` 有 `hold>0` 自動推進、亦可按鍵提前；`cg` `hold==0` 等按鍵。

- [ ] **Step 1: 追加失敗測試**

Append to `tests/presentation/cutscene/test_cutscene_player.gd`:

```gdscript
func test_fade_black_sets_overlay_opaque():
	var p := _player(FakeCtx.new())
	await p.play(_data([ { "type": "fade", "to": "black", "duration": 0.05 } ]))
	assert_almost_eq(p._fade_rect.color.a, 1.0, 0.01)

func test_fade_clear_sets_overlay_transparent():
	var p := _player(FakeCtx.new())
	await p.play(_data([
		{ "type": "fade", "to": "black", "duration": 0.05 },
		{ "type": "fade", "to": "clear", "duration": 0.05 },
	]))
	assert_almost_eq(p._fade_rect.color.a, 0.0, 0.01)

func test_cg_sets_texture_then_finishes():
	var p := _player(FakeCtx.new())
	await p.play(_data([ { "type": "cg", "image": "nav_echo_relic", "hold": 0.05 } ]))
	assert_not_null(p._cg_rect.texture)

func test_title_card_shows_content():
	var p := _player(FakeCtx.new())
	await p.play(_data([ { "type": "title_card", "title": "第一章", "subtitle": "邊陲", "hold": 0.05 } ]))
	# 播完標題卡不再可見，但曾設過內容（用 title label 驗）。
	assert_eq(p._title_card._title_label.text, "第一章")

func test_shake_restores_camera_transform():
	var p := _player(FakeCtx.new())
	var before := p._camera.position
	await p.play(_data([ { "type": "shake", "intensity": 4.0, "duration": 0.05 } ]))
	assert_almost_eq(p._camera.position.x, before.x, 0.001)
	assert_almost_eq(p._camera.position.y, before.y, 0.001)
	assert_almost_eq(p._camera.position.z, before.z, 0.001)
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/presentation/cutscene -ginclude_subdirs=false -gexit`
Expected: FAIL（`_fade_rect`/`_cg_rect`/`_title_card` 未建、fade/cg/title/shake 未分派）。

- [ ] **Step 3: 修改實作**

在 `presentation/cutscene/cutscene_player.gd` 加入表面建立與視覺 step。

`_init` 後新增 `_ready`（建 fade rect / cg rect / title card）：

```gdscript
var _fade_rect: ColorRect
var _cg_rect: TextureRect
var _title_card: TitleCard
var _skip_dwell: bool = false

func _ready() -> void:
	# 黑幕（fade）：全螢幕黑、alpha 由 0 動到 1。
	_fade_rect = ColorRect.new()
	_fade_rect.color = Color(0, 0, 0, 0)
	_fade_rect.set_anchors_preset(Control.PRESET_FULL_RECT)
	_fade_rect.mouse_filter = Control.MOUSE_FILTER_IGNORE
	add_child(_fade_rect)

	# 事件 CG：全螢幕、保長寬比置中；初始隱藏。
	_cg_rect = TextureRect.new()
	_cg_rect.set_anchors_preset(Control.PRESET_FULL_RECT)
	_cg_rect.expand_mode = TextureRect.EXPAND_IGNORE_SIZE
	_cg_rect.stretch_mode = TextureRect.STRETCH_KEEP_ASPECT_CENTERED
	_cg_rect.mouse_filter = Control.MOUSE_FILTER_IGNORE
	_cg_rect.visible = false
	add_child(_cg_rect)

	# 標題卡：初始隱藏。
	_title_card = TitleCard.new()
	_title_card.visible = false
	add_child(_title_card)

	set_process_unhandled_input(false)
```

`_play_step` 的 `match` 補上 4 個分支（放在既有分支之間、`_:` 之前）：

```gdscript
		"fade":
			var target: float = 1.0 if String(step["to"]) == "black" else 0.0
			var tween := create_tween()
			tween.tween_property(_fade_rect, "color:a", target, float(step["duration"]))
			await tween.finished
		"cg":
			_cg_rect.texture = SceneImageCatalog.get_texture(String(step["image"]))
			_cg_rect.visible = true
			await _dwell(float(step["hold"]))
			_cg_rect.visible = false
		"title_card":
			_title_card.set_content(String(step["title"]), String(step["subtitle"]))
			_title_card.visible = true
			await _dwell(float(step["hold"]))
			_title_card.visible = false
		"shake":
			await _shake(float(step["intensity"]), float(step["duration"]))
```

新增輔助方法：

```gdscript
# 停留 hold 秒；hold<=0 → 等按鍵。期間任意鍵可提前推進（無整段 skip）。
func _dwell(hold: float) -> void:
	_skip_dwell = false
	set_process_unhandled_input(true)
	if hold > 0.0:
		var timer := get_tree().create_timer(hold)
		while timer.time_left > 0.0 and not _skip_dwell:
			await get_tree().process_frame
	else:
		while not _skip_dwell:
			await get_tree().process_frame
	set_process_unhandled_input(false)

func _unhandled_input(event: InputEvent) -> void:
	if event is InputEventKey and event.pressed and not event.echo:
		_skip_dwell = true

# 鏡頭震動：在 duration 內以衰減隨機 offset 擾動相機，結束復位。
func _shake(intensity: float, duration: float) -> void:
	if _camera == null:
		return
	var base := _camera.position
	var t := 0.0
	while t < duration:
		var dt := get_process_delta_time()
		t += dt
		var falloff: float = 1.0 - (t / duration)
		var amp: float = intensity * 0.01 * maxf(falloff, 0.0)
		_camera.position = base + Vector3(
			randf_range(-amp, amp), randf_range(-amp, amp), 0.0)
		await get_tree().process_frame
	_camera.position = base
```

> `_ctx`、`play`、`_play_audio` 等 Task 4 內容保留不動。

- [ ] **Step 4: 跑測試確認通過**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/presentation/cutscene -ginclude_subdirs=false -gexit`
Expected: PASS（含 Task 4 的測試）。

- [ ] **Step 5: Commit**

```bash
git add presentation/cutscene/cutscene_player.gd tests/presentation/cutscene/test_cutscene_player.gd && git commit -m "feat(cutscene): 視覺 step（fade/cg/title_card/shake）＋dwell 提前推進"
```

---

## Task 6: CutscenePlayer dialogue step（自擁 overlay＋dialogue_advanced 轉出）

**Files:**
- Modify: `presentation/cutscene/cutscene_player.gd`
- Test: `tests/presentation/cutscene/test_cutscene_player.gd`（追加）

**Interfaces:**
- Consumes: `DialogueOverlay`、`DialogueRunner`、`DialogueCatalog`（皆既有）。
- Produces（追加到 `CutscenePlayer`）：內部自擁 `_dialogue_overlay: DialogueOverlay`（`layer = 95`，高於 player 的 90，故對話框顯示在黑幕之上）；`_play_step` 新增 `dialogue` 分派——開自擁 overlay、`await` 其 `finished`、把 overlay `advanced` 以 `dialogue_advanced` 轉出。**不接 main 的 `_on_dialogue_finished`**（避免過場中途解鎖玩家/標錯 scene）。

- [ ] **Step 1: 追加失敗測試**

Append to `tests/presentation/cutscene/test_cutscene_player.gd`（頂部若無則加 `_key` helper）:

```gdscript
func _key(code: int) -> InputEventKey:
	var ev := InputEventKey.new()
	ev.keycode = code
	ev.pressed = true
	return ev

func test_dialogue_step_opens_and_completes():
	# 用 test 專用暫存對話檔（單節點、末端 goto null）。
	var dir := "res://tests/presentation/cutscene/_tmp"
	DirAccess.make_dir_recursive_absolute(ProjectSettings.globalize_path(dir))
	var f := FileAccess.open("%s/../../../content/dialogues/_cs_probe.json" % dir, FileAccess.WRITE)
	# 直接寫進 content/dialogues 以便 DialogueCatalog 依 id 載入（測後刪）。
	f.store_string('{"id":"_cs_probe","start":"root","nodes":{"root":{"text":"hi","choices":[{"text":"ok","goto":null}]}}}')
	f.close()

	var p := _player(FakeCtx.new())
	watch_signals(p)
	# dialogue step 會等 overlay.finished；在背景推進，模擬玩家按鍵選擇。
	p.play(_data([ { "type": "dialogue", "dialogue": "_cs_probe" } ]))
	await get_tree().process_frame
	assert_true(p._dialogue_overlay.is_open(), "對話 overlay 已開")
	p._dialogue_overlay._unhandled_input(_key(KEY_1))   # 選唯一選項（goto null → finished）
	await get_tree().process_frame
	assert_signal_emitted(p, "finished")
	assert_false(p._dialogue_overlay.is_open())

	DirAccess.remove_absolute(ProjectSettings.globalize_path("res://content/dialogues/_cs_probe.json"))
```

> 若上述相對路徑寫檔在 harness 不便，改為：在 `content/dialogues/` 手動放一個 `_cs_probe.json`（同內容）並在 `.gitignore` 忽略。實作時擇一，確保 `DialogueCatalog.load_dialogue("_cs_probe")` 可載到。

- [ ] **Step 2: 跑測試確認失敗**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/presentation/cutscene -ginclude_subdirs=false -gexit`
Expected: FAIL（`_dialogue_overlay` 未建、`dialogue` 未分派）。

- [ ] **Step 3: 修改實作**

在 `_ready` 末尾（`set_process_unhandled_input(false)` 之前）建自擁 overlay：

```gdscript
	# 自擁 DialogueOverlay（layer 95 > 本 player 的 90 → 對話框顯示在黑幕之上）。
	# 刻意不接 main 的 _on_dialogue_finished：其會 set_enabled(true)/標 once，會在過場中途誤觸。
	_dialogue_overlay = DialogueOverlay.new()
	add_child(_dialogue_overlay)
	_dialogue_overlay.layer = 95
	_dialogue_overlay.advanced.connect(func(descs): dialogue_advanced.emit(descs))
```

宣告欄位：

```gdscript
var _dialogue_overlay: DialogueOverlay
```

`_play_step` 的 `match` 補 `dialogue` 分支：

```gdscript
		"dialogue":
			var data := DialogueCatalog.load_dialogue(String(step["dialogue"]))
			if data == null:
				return
			_dialogue_overlay.open(DialogueRunner.new(data, _ctx))
			await _dialogue_overlay.finished
```

- [ ] **Step 4: 跑測試確認通過**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/presentation/cutscene -ginclude_subdirs=false -gexit`
Expected: PASS。

- [ ] **Step 5: 全 suite 迴歸**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gexit`
Expected: PASS，總數 = 1111 + 新增測試數，0 fail。

- [ ] **Step 6: Commit**

```bash
git add presentation/cutscene/cutscene_player.gd tests/presentation/cutscene/test_cutscene_player.gd && git commit -m "feat(cutscene): dialogue step（自擁 overlay＋dialogue_advanced 轉出，避開 main finished handler）"
```

---

## Task 7: main.gd 整合（建立/接線＋_try_scene 分支＋選單閘）

**Files:**
- Modify: `presentation/world/main.gd`
- Test: `tests/presentation/world/test_main_cutscene_scene.gd`（新，輕量整合）

**Interfaces:**
- Consumes: `CutscenePlayer`（Task 4–6）。
- Produces: `main` 於 `_ready` 建 `_cutscene_player`、注入 `_camera`/`GameState`、接 `dialogue_advanced`→`_on_dialogue_advanced`；`_try_scene` 若 scene 帶 `cutscene` 鍵則走過場分支（沿用 once/require）；選單閘 `_unhandled_input` 加 `_cutscene_player.is_playing()` 檢查。

- [ ] **Step 1: 寫失敗測試**

Create `tests/presentation/world/test_main_cutscene_scene.gd`（驗證資料層路由，不啟整個 main 場景）:

```gdscript
extends GutTest

# _try_scene 的 cutscene 分支「該不該觸發」的純判定，可離開 main 直接驗 SceneTrigger 契約。
# 這裡驗證 wild_ne 地圖的 (9,9) scene 已改為 cutscene 觸發格式。
func test_wild_ne_9_9_scene_uses_cutscene_key():
	var map := MapManager.load_map("wild_ne")
	assert_true(map.has_scene(Vector2i(9, 9)), "wild_ne(9,9) 有 scene")
	var scene := map.get_scene(Vector2i(9, 9))
	assert_true(scene.has("cutscene"), "scene 用 cutscene 鍵")
	assert_eq(String(scene["cutscene"]), "nav_echo_nest")
	assert_false(scene.has("dialogue"), "不再用 dialogue 鍵")
```

> `MapManager.load_map` 若簽章不同，實作時對照 `engine/map/` 既有載入 API 調整（例如 `MapManager.peek_map`）。目的：斷言 (9,9) scene 帶 `cutscene:"nav_echo_nest"`。

- [ ] **Step 2: 跑測試確認失敗**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/presentation/world -ginclude_subdirs=false -gexit`
Expected: FAIL（此時 wild_ne 尚未改，Task 8 才改內容——但整合碼要先能吃 cutscene 鍵；本 Task 先讓程式支援，Task 8 改資料）。

> 註：此測試依賴 Task 8 的資料改動。若採 subagent 逐 task，將本測試標為「Task 8 一併轉綠」；Task 7 只做 main 程式支援並以下方 Step 3 的手動確認為主。實作者可把 Step 1 測試移到 Task 8。

- [ ] **Step 3: 修改 main.gd — 建立與接線**

在 `_ready` 的 travel overlay 接線之後（約 `main.gd:107` 後）新增：

```gdscript
	_cutscene_player = CutscenePlayer.new()
	add_child(_cutscene_player)
	_cutscene_player.dialogue_advanced.connect(_on_dialogue_advanced)
	_cutscene_player.finished.connect(_on_cutscene_finished)
```

在檔案頂部變數宣告區（約 `main.gd:37-47` 附近）新增：

```gdscript
var _cutscene_player: CutscenePlayer
```

因 `_cutscene_player.setup` 需 `_camera`（`@onready`），在 `_ready` 中確保於 `_camera` 就緒後呼叫（`@onready` 於 `_ready` 進入時已賦值）：

```gdscript
	_cutscene_player.setup(_camera, GameState)
```

（把此行接在上面 `add_child(_cutscene_player)` 之後。）

- [ ] **Step 4: 修改 main.gd — `_try_scene` 加 cutscene 分支**

將 `_try_scene`（`main.gd:381-397`）改為：先判 `cutscene` 鍵、否則走既有 `dialogue`：

```gdscript
func _try_scene(pos: Vector2i) -> bool:
	var map := MapManager.current_map
	if not map.has_scene(pos):
		return false
	var scene := map.get_scene(pos)
	var triggered := GameState.is_scene_triggered(map.map_id, pos)
	if not SceneTrigger.should_trigger(scene, GameState, triggered):
		return false
	if scene.has("cutscene"):
		_play_scene_cutscene(pos, scene)
		return true
	var data := DialogueCatalog.load_dialogue(String(scene["dialogue"]))
	if data == null:
		GameState.message_log.push("（對話 %s 遺失）" % scene["dialogue"])
		return false
	_scene_pos = pos
	_scene_once = bool(scene.get("once", false))
	_player.set_enabled(false)
	_dialogue_overlay.open(DialogueRunner.new(data, GameState))
	return true

# 過場分支：閘玩家 → await 播放 → 依 once 標記 → 復原玩家。
func _play_scene_cutscene(pos: Vector2i, scene: Dictionary) -> void:
	var data := CutsceneCatalog.load(String(scene["cutscene"]))
	if data == null:
		GameState.message_log.push("（過場 %s 遺失）" % scene["cutscene"])
		return
	_player.set_enabled(false)
	await _cutscene_player.play(data)
	if bool(scene.get("once", false)):
		GameState.mark_scene_triggered(MapManager.current_map.map_id, pos)
	GameState.refresh_collect()
	_player.set_enabled(true)
	_hud.refresh()

func _on_cutscene_finished() -> void:
	pass  # 收尾由 _play_scene_cutscene 的 await 之後處理；此處預留給未來非格子觸發
```

> 因 `_try_scene` 是 bool 函式、`_play_scene_cutscene` 為 async：`_try_scene` 在 `if scene.has("cutscene")` 分支呼叫（不 await、fire-and-forget，回傳 true 表「已接手、短路後續觸發」）。玩家在 `_play_scene_cutscene` 內立即 `set_enabled(false)`，播完才 `true`，期間移動被閘住，安全。

- [ ] **Step 5: 修改 main.gd — 選單閘**

將 `_unhandled_input` 的閘條件（`main.gd:520`）加入 cutscene 檢查：

```gdscript
	if _dialogue_overlay.is_open() or _vendor_overlay.is_open() or _travel_overlay.is_open() or _cutscene_player.is_playing():
		return  # 對話/商店/旅行/過場中，不開其他選單
```

- [ ] **Step 6: 跑全 suite 迴歸**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gexit`
Expected: PASS，0 fail（此時 `test_main_cutscene_scene` 可能仍紅——待 Task 8 改資料後轉綠；若採「Step 1 測試移到 Task 8」則此處全綠）。

- [ ] **Step 7: Commit**

```bash
git add presentation/world/main.gd tests/presentation/world/test_main_cutscene_scene.gd && git commit -m "feat(cutscene): main 整合（建立/接線＋_try_scene cutscene 分支＋選單閘）"
```

---

## Task 8: 首發內容 — 領航者殘響過場

**Files:**
- Create: `content/cutscenes/nav_echo_nest.json`
- Modify: `content/maps/wild_ne.json`（(9,9) scene：`dialogue`→`cutscene`）
- Modify: `content/audio/sfx.json`（註冊 `nav_echo_sting`）
- Modify: `presentation/world/scene_image_catalog.gd`（註記 `nav_echo_relic`）
- Test: `tests/content/test_cutscene_nav_echo.gd`（新）

**Interfaces:**
- Consumes: `CutsceneData.parse`、`CutsceneCatalog.load`（Task 1–2）、Task 7 的 `_try_scene` cutscene 分支。
- Produces: 可觸發的真過場內容。

- [ ] **Step 1: 寫失敗測試**

Create `tests/content/test_cutscene_nav_echo.gd`:

```gdscript
extends GutTest

func test_nav_echo_cutscene_parses():
	var d := CutsceneCatalog.load("nav_echo_nest")
	assert_not_null(d, "nav_echo_nest 過場可載並 parse")
	# 至少含 fade→cg→dialogue 幾個關鍵 step。
	var types: Array = []
	for s in d.steps:
		types.append(s["type"])
	assert_true(types.has("fade"), "含 fade")
	assert_true(types.has("cg"), "含 cg")
	assert_true(types.has("dialogue"), "含 dialogue")

func test_nav_echo_dialogue_still_exists():
	# dialogue step 重用既有 nav_echo_nest 對話（低語文字不重寫）。
	assert_not_null(DialogueCatalog.load_dialogue("nav_echo_nest"))

func test_wild_ne_scene_uses_cutscene():
	var raw = JSON.parse_string(FileAccess.get_file_as_string("res://content/maps/wild_ne.json"))
	var found := false
	for e in raw["entities"]:
		if String(e.get("type", "")) == "scene" and e.get("pos", []) == [9, 9]:
			found = true
			assert_true(e.has("cutscene"), "(9,9) scene 用 cutscene 鍵")
			assert_eq(String(e["cutscene"]), "nav_echo_nest")
			assert_false(e.has("dialogue"), "不再用 dialogue 鍵")
	assert_true(found, "找到 (9,9) scene entity")
```

> 若 `wild_ne.json` 的 entity 陣列鍵不是 `entities`，實作時對照檔案實際鍵名調整（見 Step 3 會先讀檔）。

- [ ] **Step 2: 跑測試確認失敗**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/content -ginclude_subdirs=false -gexit`
Expected: FAIL（過場檔不存在、wild_ne 仍用 dialogue 鍵）。

- [ ] **Step 3: 建立過場內容檔**

Create `content/cutscenes/nav_echo_nest.json`:

```json
{
  "id": "nav_echo_nest",
  "steps": [
    { "type": "fade", "to": "black", "duration": 0.7 },
    { "type": "cg", "image": "nav_echo_relic", "hold": 3.0 },
    { "type": "shake", "intensity": 6.0, "duration": 0.5 },
    { "type": "audio", "op": "sfx", "id": "nav_echo_sting" },
    { "type": "dialogue", "dialogue": "nav_echo_nest" },
    { "type": "fade", "to": "clear", "duration": 0.7 }
  ]
}
```

- [ ] **Step 4: 改 wild_ne.json 的 (9,9) scene**

先讀確認鍵格式：`cat content/maps/wild_ne.json`（找 `"pos": [9, 9]` 那筆 scene）。將該筆的 `"dialogue": "nav_echo_nest"` 改成 `"cutscene": "nav_echo_nest"`（`require`/`once` 不動）。原本（Task 0 勘查）：

```json
{"type": "scene", "pos": [9, 9], "dialogue": "nav_echo_nest", "require": {"quest_active": "goblin_menace"}, "once": true}
```

改為：

```json
{"type": "scene", "pos": [9, 9], "cutscene": "nav_echo_nest", "require": {"quest_active": "goblin_menace"}, "once": true}
```

- [ ] **Step 5: 註冊 SFX 語意 id（路徑待補）**

在 `content/audio/sfx.json` 加一筆（缺檔→AudioManager 靜音略過；真音效歸音訊組後補、授權登記）：

```json
"nav_echo_sting": { "path": "" }
```

> 先讀 `content/audio/sfx.json` 確認既有筆的結構（key→物件 or key→字串），比照現況格式加入（若既有值是字串路徑，寫 `"nav_echo_sting": ""`）。

- [ ] **Step 6: 註記 CG placeholder id**

在 `presentation/world/scene_image_catalog.gd` 的 `_IMAGES` 加一筆註記（缺圖→純色 placeholder，真圖歸美術組）：

```gdscript
	"nav_echo_relic": "res://content/scenes/nav_echo_relic.webp",
```

> 該 webp 尚不存在 → `get_texture` 走 `ResourceLoader.exists` 檢查後回 placeholder，不崩。

- [ ] **Step 7: 跑測試確認通過**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests/content -ginclude_subdirs=false -gexit`
Expected: PASS。若 Task 7 的 `test_main_cutscene_scene` 之前紅，此時應轉綠。

- [ ] **Step 8: 全 suite 迴歸**

Run: `godot --headless --path . -s addons/gut/gut_cmdln.gd -gexit`
Expected: PASS，0 fail，總數 ≥ 1111 + 新測。

- [ ] **Step 9: 人工視覺 gate（總監）**

啟動 `./run.sh`，取得 `goblin_menace` 進行中狀態、進 `wild_ne` 走到 (9,9)，確認：黑幕淡出 → 遺物 CG（placeholder 純色）→ 鏡頭一震 → 低語文字（原文不變）→ 淡回；播完玩家控制正確交還、無 soft-lock；過場中選單鍵無效。音訊 sting 現靜音（待音訊組補）。

- [ ] **Step 10: Commit**

```bash
git add content/cutscenes/nav_echo_nest.json content/maps/wild_ne.json content/audio/sfx.json presentation/world/scene_image_catalog.gd tests/content/test_cutscene_nav_echo.gd && git commit -m "feat(cutscene): 首發過場 領航者殘響（wild_ne 9,9 dialogue→cutscene，save-neutral）"
```

---

## Self-Review

**Spec coverage**：
- §2 資料格式/8 step → Task 1（parse）、Task 4–6（各 step 執行）✅
- §3.1 CutsceneData → Task 1；§3.2 CutsceneCatalog/TitleCard/CutscenePlayer（含自擁 overlay 隱患對策）→ Task 2/3/4/5/6 ✅
- §3.3 scene schema `cutscene` 鍵＋`_try_scene` 分支＋選單閘 → Task 7 ✅
- §4 首發內容（過場檔＋wild_ne 改＋dialogue 重用）→ Task 8 ✅
- §5 資產 placeholder（CG/SFX）→ Task 8 Step 5/6 ✅
- §6 save-neutral（不升 v12）→ Global Constraints＋全程無 save 改動 ✅
- §7 測試（data/catalog/player/title/content）→ Task 1–8 對應測試 ✅
- §8 gate（自動 suite＋人工 ./run.sh）→ Task 6/8 全 suite＋Task 8 Step 9 ✅

**Placeholder scan**：無 TBD/TODO 佔位；資產「路徑待補」為刻意延後（美術/音訊組），非程式缺口。

**Type consistency**：`CutsceneData.parse`/`CutsceneCatalog.load`/`load_from`、`CutscenePlayer.setup/play/is_playing/finished/dialogue_advanced`、`TitleCard.set_content` 全計劃一致；`_try_scene` 回 bool、`_play_scene_cutscene` async 的搭配已於 Task 7 Step 4 說明。

**已知實作待對照點**（實作者需就地核對，非缺口）：`MapManager` 載圖 API 名稱（Task 7 測試）、`wild_ne.json` entity 陣列鍵名與 `sfx.json` 值結構（Task 8 先讀檔再改）。
