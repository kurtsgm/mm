# M9 迷你地圖（角落自動地圖 + 迷霧探索 + save v4）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在畫面右上角加一個常駐俯視小地圖，逐格畫出已探索的當前地圖（牆/地板＋階梯/門/portal＋玩家箭頭），採迷霧探索且進度入存檔（save v4）。

**Architecture:** 探索狀態存 `GameState.explored`（map_id → Vector2i 集合），進格時揭示 3×3；`SaveSerializer` 升 v4 把它序列化（鏡射 `cleared_encounters`）；`MiniMap`（`CanvasLayer` + 內部自繪 `Control`）讀 `MapManager.current_map` 與 `GameState` 畫圖；`main.gd` 比照 `Hud` 接線，切圖/讀檔時 refresh。

**Tech Stack:** Godot 4.7 + GDScript；GUT 9.7 測試；純 2D `_draw()` UI（不開第二相機）。

## Global Constraints

- 引擎二進位不在 PATH：所有 godot 指令用 `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"`。
- 全套測試：`"$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gexit`（現況 302 綠）。聚焦單檔：加 `-gselect=<script_name.gd>`。
- 三層架構：`engine/` 純邏輯、`autoload/` 全域單例、`presentation/` Godot 節點。UI 沿用「程式建構 placeholder」慣例（鏡射 `Hud`，UI 不寫像素測試）。
- 座標系：`MapData.TileType{FLOOR=0,WALL=1,DOOR=2,STAIRS_UP=3,STAIRS_DOWN=4}`；`GridDirection.Dir{NORTH=0,EAST=1,SOUTH=2,WEST=3}`；地圖 row-major（`y*width+x`），小地圖 y 同向（上＝北）。
- 存檔向後相容：升 v4 後 `from_dict` 須仍接受 v1/v2/v3 舊檔；缺 `explored` 欄位 → 空 `{}`。
- commit 訊息結尾：`Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`。

---

## File Structure

- **Modify** `autoload/game_state.gd` — 加 `explored` 欄位 + `mark_explored/is_explored/explored_for`。
- **Modify** `engine/save/save_data.gd` — 加 `explored: Dictionary` 欄位。
- **Modify** `engine/save/save_serializer.gd` — `VERSION` 3→4；`explored` 序列化/反序列化；版本接受放寬到 1/2/3/4。
- **Modify** `autoload/save_system.gd` — `capture_from`/`apply_to` 帶 `explored`。
- **Create** `presentation/ui/mini_map.gd` — `MiniMap`（`CanvasLayer` + 內部 `_MiniMapPanel` 自繪）。
- **Modify** `presentation/world/main.gd` — 建 `MiniMap`、揭示接線（4 個重建點 + 進格）。
- **Create** `tests/autoload/test_game_state_explored.gd`。
- **Modify** `tests/engine/save/test_save_serializer.gd` — explored 往返 + v3 相容 + 畸形容錯 + version=4。
- **Modify** `tests/engine/save/test_save_serializer_spells.gd` — `test_version_is_3` 改為期望 4。
- **Modify** `tests/autoload/test_save_system_capture_apply.gd` — explored capture/apply。
- **Create** `tests/presentation/test_mini_map.gd` — `tile_color` 純對應測試。

---

### Task 1: GameState 探索狀態

**Files:**
- Modify: `autoload/game_state.gd`
- Test: `tests/autoload/test_game_state_explored.gd` (create)

**Interfaces:**
- Consumes: 無（純全域狀態）。
- Produces:
  - `var explored: Dictionary`  # `String map_id -> Dictionary[Vector2i -> true]`（內層當 set）
  - `func mark_explored(map_id: String, pos: Vector2i, w: int, h: int) -> void`（標記 pos 及 8 鄰格，以 w/h 濾越界）
  - `func is_explored(map_id: String, pos: Vector2i) -> bool`
  - `func explored_for(map_id: String) -> Dictionary`（回該圖 set，未知→空 `{}`）

- [ ] **Step 1: 寫失敗測試**

Create `tests/autoload/test_game_state_explored.gd`:

```gdscript
extends GutTest

const GameStateScript := preload("res://autoload/game_state.gd")

func _gs() -> Node:
	var gs = GameStateScript.new()
	add_child_autofree(gs)  # 進 tree → 觸發 _ready
	return gs

func test_explored_defaults_empty():
	var gs = _gs()
	assert_eq(gs.explored.size(), 0)
	assert_eq(gs.explored_for("nope").size(), 0)
	assert_false(gs.is_explored("nope", Vector2i(0, 0)))

func test_mark_explored_reveals_3x3_block():
	var gs = _gs()
	gs.mark_explored("m", Vector2i(2, 2), 5, 5)
	for dy in [-1, 0, 1]:
		for dx in [-1, 0, 1]:
			assert_true(gs.is_explored("m", Vector2i(2 + dx, 2 + dy)),
				"(%d,%d) 應已探索" % [2 + dx, 2 + dy])
	assert_false(gs.is_explored("m", Vector2i(0, 2)), "3×3 之外仍未探索")
	assert_eq(gs.explored_for("m").size(), 9)

func test_mark_explored_filters_out_of_bounds():
	var gs = _gs()
	gs.mark_explored("m", Vector2i(0, 0), 5, 5)  # 角落：越界鄰格應濾掉
	assert_true(gs.is_explored("m", Vector2i(0, 0)))
	assert_true(gs.is_explored("m", Vector2i(1, 1)))
	assert_false(gs.is_explored("m", Vector2i(-1, 0)))
	assert_false(gs.is_explored("m", Vector2i(0, -1)))
	assert_eq(gs.explored_for("m").size(), 4, "界內只剩 (0,0)(1,0)(0,1)(1,1)")

func test_mark_explored_dedupes_on_revisit():
	var gs = _gs()
	gs.mark_explored("m", Vector2i(2, 2), 5, 5)
	var n1: int = gs.explored_for("m").size()
	gs.mark_explored("m", Vector2i(2, 2), 5, 5)  # 重訪同格
	assert_eq(gs.explored_for("m").size(), n1, "重訪不應膨脹")

func test_explored_is_per_map():
	var gs = _gs()
	gs.mark_explored("a", Vector2i(2, 2), 5, 5)
	assert_true(gs.is_explored("a", Vector2i(2, 2)))
	assert_false(gs.is_explored("b", Vector2i(2, 2)))
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gselect=test_game_state_explored.gd -gexit`
Expected: FAIL（`mark_explored`/`is_explored`/`explored_for`/`explored` 不存在）。

- [ ] **Step 3: 實作**

In `autoload/game_state.gd`, after the `cleared_encounters` line (line 13) add the field:

```gdscript
var explored: Dictionary = {}  # String map_id -> Dictionary[Vector2i -> true]（內層當 set）
```

And after `cleared_for` (around line 32) add the methods:

```gdscript
func mark_explored(map_id: String, pos: Vector2i, w: int, h: int) -> void:
	var seen: Dictionary = explored.get(map_id, {})
	for dy in range(-1, 2):
		for dx in range(-1, 2):
			var c := Vector2i(pos.x + dx, pos.y + dy)
			if c.x < 0 or c.x >= w or c.y < 0 or c.y >= h:
				continue
			seen[c] = true
	explored[map_id] = seen

func is_explored(map_id: String, pos: Vector2i) -> bool:
	return explored.get(map_id, {}).has(pos)

func explored_for(map_id: String) -> Dictionary:
	return explored.get(map_id, {})
```

- [ ] **Step 4: 跑測試確認通過**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gselect=test_game_state_explored.gd -gexit`
Expected: PASS（5 個測試全綠）。

- [ ] **Step 5: Commit**

```bash
git add autoload/game_state.gd tests/autoload/test_game_state_explored.gd
git commit -m "feat(state): GameState.explored 迷霧探索狀態（3×3 揭示 + per-map set）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: 存檔 v4 — 序列化探索狀態

**Files:**
- Modify: `engine/save/save_data.gd`
- Modify: `engine/save/save_serializer.gd`
- Modify: `tests/engine/save/test_save_serializer.gd`
- Modify: `tests/engine/save/test_save_serializer_spells.gd`

**Interfaces:**
- Consumes: `GameState.explored` 形態（`map_id -> {Vector2i: true}`，Task 1）。
- Produces:
  - `SaveData.explored: Dictionary`（同形態）
  - `SaveSerializer.VERSION == 4`
  - `to_dict` 輸出 `state.explored`（`map_id -> [[x,y]…]`）
  - `from_dict` 讀回 `explored`，接受 v1/v2/v3/v4，缺欄位→`{}`，畸形座標略過

- [ ] **Step 1: 寫失敗測試**

In `tests/engine/save/test_save_serializer.gd`, append:

```gdscript
func test_to_dict_version_is_4():
	assert_eq(SaveSerializer.to_dict(_sample())["version"], 4)

func test_roundtrip_explored_multi_map():
	var d := _sample()
	d.explored = {
		"level01": {Vector2i(1, 1): true, Vector2i(2, 1): true},
		"town_oak": {Vector2i(0, 0): true},
	}
	var back := SaveSerializer.from_dict(SaveSerializer.to_dict(d))
	assert_eq(back.explored.size(), 2)
	assert_true(back.explored["level01"].has(Vector2i(1, 1)))
	assert_true(back.explored["level01"].has(Vector2i(2, 1)))
	assert_true(back.explored["town_oak"].has(Vector2i(0, 0)))

func test_old_v3_save_gets_empty_explored():
	var raw := {
		"version": 3,
		"state": {
			"gold": 0, "map_id": "level01",
			"player_pos": [1, 1], "player_facing": 0,
			"party": [], "inventory": [], "cleared_encounters": {},
		},
	}
	var back := SaveSerializer.from_dict(raw)
	assert_not_null(back, "v3 舊檔仍可讀")
	assert_eq(back.explored.size(), 0)

func test_explored_malformed_coords_skipped():
	var raw := SaveSerializer.to_dict(_sample())
	raw["state"]["explored"] = {"level01": [[1, 1], [9]]}  # 第二個 size<2 → 畸形
	var back := SaveSerializer.from_dict(raw)
	assert_not_null(back)
	assert_true(back.explored["level01"].has(Vector2i(1, 1)))
	assert_eq(back.explored["level01"].size(), 1, "畸形座標被略過")
```

In `tests/engine/save/test_save_serializer_spells.gd`, replace the `test_version_is_3` function (lines 15-16):

```gdscript
func test_version_is_4():
	assert_eq(SaveSerializer.to_dict(_sample())["version"], 4)
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gselect=test_save_serializer.gd -gexit`
Expected: FAIL（version 仍為 3、`explored` 未處理）。

- [ ] **Step 3: 實作**

In `engine/save/save_data.gd`, after the `cleared_encounters` line (line 10) add:

```gdscript
var explored: Dictionary = {}  # String map_id -> Dictionary[Vector2i -> true]
```

In `engine/save/save_serializer.gd`:

(a) Bump the version constant (line 4):

```gdscript
const VERSION := 4
```

(b) In `to_dict`, add `explored` to the `state` dict (after the `cleared_encounters` line inside `state`):

```gdscript
			"cleared_encounters": _cleared_to_dict(data.cleared_encounters),
			"explored": _explored_to_dict(data.explored),
```

(c) In `from_dict`, broaden the version acceptance (line 25) and read explored. Replace:

```gdscript
	if v != VERSION and v != 1 and v != 2:   # 接受目前版本與已知舊版（向後相容）
		return null
```

with:

```gdscript
	if v != VERSION and v != 1 and v != 2 and v != 3:   # 接受目前版本與已知舊版（向後相容）
		return null
```

And after the `data.cleared_encounters = ...` line (line 40) add:

```gdscript
	data.explored = _explored_from_dict(s.get("explored", {}))
```

(d) Add the two helpers near `_cleared_to_dict`/`_cleared_from_dict` (end of file):

```gdscript
static func _explored_to_dict(explored: Dictionary) -> Dictionary:
	var out: Dictionary = {}
	for map_id in explored:
		var arr: Array = []
		for pos in explored[map_id]:   # 內層 set 的 key（Vector2i）
			arr.append(_vec(pos))
		out[map_id] = arr
	return out

static func _explored_from_dict(raw) -> Dictionary:
	var out: Dictionary = {}
	if typeof(raw) != TYPE_DICTIONARY:
		return out
	for map_id in raw:
		var seen: Dictionary = {}
		for a in raw[map_id]:
			if _is_vec_shape(a):
				seen[_to_vec(a)] = true
		out[String(map_id)] = seen
	return out
```

- [ ] **Step 4: 跑測試確認通過**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gselect=test_save_serializer.gd -gexit`
Then: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gselect=test_save_serializer_spells.gd -gexit`
Expected: 兩檔皆 PASS（含新 explored 往返、v3 相容、畸形容錯；spells 的 v2/v1 舊檔測試仍綠）。

- [ ] **Step 5: Commit**

```bash
git add engine/save/save_data.gd engine/save/save_serializer.gd tests/engine/save/test_save_serializer.gd tests/engine/save/test_save_serializer_spells.gd
git commit -m "feat(save): schema v4 — 序列化 explored 探索進度（v1/2/3 向後相容）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: SaveSystem 帶上 explored

**Files:**
- Modify: `autoload/save_system.gd`
- Modify: `tests/autoload/test_save_system_capture_apply.gd`

**Interfaces:**
- Consumes: `GameState.explored`（Task 1）、`SaveData.explored`（Task 2）。
- Produces: `capture_from` 把 `gs.explored` 寫進 `SaveData`；`apply_to` 把 `data.explored` 還原到 `gs.explored`。

- [ ] **Step 1: 寫失敗測試**

In `tests/autoload/test_save_system_capture_apply.gd`, append:

```gdscript
func test_capture_from_reads_explored():
	var ss = _sys()
	var gs = _gs()
	gs.mark_explored("level01", Vector2i(2, 2), 5, 5)
	var data = ss.capture_from(gs)
	assert_true(data.explored["level01"].has(Vector2i(2, 2)))

func test_apply_to_restores_explored():
	var ss = _sys()
	var gs = _gs()
	var mm = _mm()
	mm.load_by_id("level01")
	var data := SaveData.new()
	data.map_id = "level01"
	data.party = Party.create_default()
	data.explored = {"level01": {Vector2i(3, 3): true}}
	ss.apply_to(data, gs, mm)
	assert_true(gs.explored["level01"].has(Vector2i(3, 3)))
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gselect=test_save_system_capture_apply.gd -gexit`
Expected: FAIL（`data.explored`/`gs.explored` 未被搬運）。

- [ ] **Step 3: 實作**

In `autoload/save_system.gd`:

(a) In `capture_from`, after `data.cleared_encounters = gs.cleared_encounters` (line 66) add:

```gdscript
	data.explored = gs.explored
```

(b) In `apply_to`, after `gs.cleared_encounters = data.cleared_encounters` (line 76) add:

```gdscript
	gs.explored = data.explored
```

- [ ] **Step 4: 跑測試確認通過**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gselect=test_save_system_capture_apply.gd -gexit`
Expected: PASS（含既有 capture/apply 測試 + 2 個新 explored 測試）。

- [ ] **Step 5: Commit**

```bash
git add autoload/save_system.gd tests/autoload/test_save_system_capture_apply.gd
git commit -m "feat(save): SaveSystem capture/apply 帶上 explored 探索進度

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: MiniMap 元件（自繪小地圖）

**Files:**
- Create: `presentation/ui/mini_map.gd`
- Test: `tests/presentation/test_mini_map.gd` (create)

**Interfaces:**
- Consumes: `MapManager.current_map`（`width/height/get_tile/has_link/map_id`）、`GameState.explored_for/player_pos/player_facing`、`PlayerController.entered_cell(Vector2i)` / `facing_changed(int)` 信號。
- Produces:
  - `class_name MiniMap extends CanvasLayer`
  - `func setup(player: PlayerController) -> void`（建面板、連信號、首次 refresh）
  - `func refresh() -> void`（依當前地圖尺寸重算面板大小並重畫；切圖/讀檔後由 main 呼叫）
  - `static func tile_color(tile: int, is_portal: bool) -> Color`（純對應，供測試）
  - 顏色常數：`COL_FLOOR/COL_WALL/COL_DOOR/COL_STAIRS_UP/COL_STAIRS_DOWN/COL_PORTAL`

- [ ] **Step 1: 寫失敗測試**

Create `tests/presentation/test_mini_map.gd`:

```gdscript
extends GutTest

const MiniMapScript := preload("res://presentation/ui/mini_map.gd")

func test_portal_overrides_tile_color():
	# portal 旗標優先於底層 tile
	assert_eq(MiniMapScript.tile_color(MapData.TileType.FLOOR, true), MiniMapScript.COL_PORTAL)
	assert_eq(MiniMapScript.tile_color(MapData.TileType.WALL, true), MiniMapScript.COL_PORTAL)

func test_tile_colors_by_type():
	assert_eq(MiniMapScript.tile_color(MapData.TileType.FLOOR, false), MiniMapScript.COL_FLOOR)
	assert_eq(MiniMapScript.tile_color(MapData.TileType.WALL, false), MiniMapScript.COL_WALL)
	assert_eq(MiniMapScript.tile_color(MapData.TileType.DOOR, false), MiniMapScript.COL_DOOR)
	assert_eq(MiniMapScript.tile_color(MapData.TileType.STAIRS_UP, false), MiniMapScript.COL_STAIRS_UP)
	assert_eq(MiniMapScript.tile_color(MapData.TileType.STAIRS_DOWN, false), MiniMapScript.COL_STAIRS_DOWN)
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gselect=test_mini_map.gd -gexit`
Expected: FAIL（`mini_map.gd` 不存在 / 無法 preload）。

- [ ] **Step 3: 實作**

Create `presentation/ui/mini_map.gd`:

```gdscript
class_name MiniMap
extends CanvasLayer
# 右上角常駐俯視小地圖（迷霧探索）。程式建構 placeholder UI，鏡射 Hud。
# 每格一色塊：已探索才畫（未探索露出底板＝迷霧）；玩家為朝向三角形。
# 資料來源：MapManager.current_map（tile/portal）、GameState（explored/pos/facing）。

const CELL_PX := 16
const PAD := 6
const BORDER := 1.0
const MARGIN := 12  # 離畫面右上角

const COL_BACKDROP := Color(0, 0, 0, 0.55)
const COL_BORDER := Color(1, 1, 1, 0.35)
const COL_FLOOR := Color(0.72, 0.72, 0.72)
const COL_WALL := Color(0.16, 0.16, 0.18)
const COL_DOOR := Color(0.78, 0.6, 0.28)
const COL_STAIRS_UP := Color(0.5, 0.72, 0.95)
const COL_STAIRS_DOWN := Color(0.66, 0.46, 0.85)
const COL_PORTAL := Color(0.36, 0.82, 0.46)
const COL_PLAYER := Color(0.95, 0.3, 0.3)

var _panel: Control

func setup(player: PlayerController) -> void:
	_panel = _MiniMapPanel.new()
	_panel.mouse_filter = Control.MOUSE_FILTER_IGNORE
	add_child(_panel)
	player.entered_cell.connect(func(_p): _panel.queue_redraw())
	player.facing_changed.connect(func(_f): _panel.queue_redraw())
	refresh()

# 依當前地圖尺寸把面板釘在右上角並重畫。切圖/讀檔後由 main 呼叫。
func refresh() -> void:
	if _panel == null:
		return
	var map := MapManager.current_map
	var w: int = map.width if map != null else 0
	var h: int = map.height if map != null else 0
	var pw: float = w * CELL_PX + PAD * 2
	var ph: float = h * CELL_PX + PAD * 2
	_panel.set_anchors_preset(Control.PRESET_TOP_RIGHT)
	_panel.offset_top = MARGIN
	_panel.offset_bottom = MARGIN + ph
	_panel.offset_right = -MARGIN
	_panel.offset_left = -MARGIN - pw
	_panel.queue_redraw()

# tile type + 是否 portal → 色塊顏色（portal 旗標優先）。純函式、可測。
static func tile_color(tile: int, is_portal: bool) -> Color:
	if is_portal:
		return COL_PORTAL
	match tile:
		MapData.TileType.WALL: return COL_WALL
		MapData.TileType.DOOR: return COL_DOOR
		MapData.TileType.STAIRS_UP: return COL_STAIRS_UP
		MapData.TileType.STAIRS_DOWN: return COL_STAIRS_DOWN
		_: return COL_FLOOR

# 內部繪製面板。_draw 在 CanvasItem(Control) 上，讀 autoload；與 MiniMap 拆開。
class _MiniMapPanel extends Control:
	func _draw() -> void:
		var map = MapManager.current_map
		if map == null:
			return
		draw_rect(Rect2(Vector2.ZERO, size), MiniMap.COL_BACKDROP, true)
		draw_rect(Rect2(Vector2.ZERO, size), MiniMap.COL_BORDER, false, MiniMap.BORDER)
		var explored: Dictionary = GameState.explored_for(map.map_id)
		for y in map.height:
			for x in map.width:
				var cell := Vector2i(x, y)
				if not explored.has(cell):
					continue
				var col := MiniMap.tile_color(map.get_tile(cell), map.has_link(cell))
				var r := Rect2(
					MiniMap.PAD + x * MiniMap.CELL_PX,
					MiniMap.PAD + y * MiniMap.CELL_PX,
					MiniMap.CELL_PX - 1, MiniMap.CELL_PX - 1)
				draw_rect(r, col, true)
		_draw_player()

	func _draw_player() -> void:
		var pos: Vector2i = GameState.player_pos
		var c := Vector2(
			MiniMap.PAD + pos.x * MiniMap.CELL_PX + MiniMap.CELL_PX * 0.5,
			MiniMap.PAD + pos.y * MiniMap.CELL_PX + MiniMap.CELL_PX * 0.5)
		var r: float = MiniMap.CELL_PX * 0.42
		var fwd := _facing_vec(GameState.player_facing)
		var side := Vector2(-fwd.y, fwd.x)
		var tip := c + fwd * r
		var bl := c - fwd * r + side * (r * 0.7)
		var br := c - fwd * r - side * (r * 0.7)
		draw_colored_polygon(PackedVector2Array([tip, bl, br]), MiniMap.COL_PLAYER)

	func _facing_vec(facing: int) -> Vector2:
		match facing:
			GridDirection.Dir.EAST: return Vector2(1, 0)
			GridDirection.Dir.SOUTH: return Vector2(0, 1)
			GridDirection.Dir.WEST: return Vector2(-1, 0)
		return Vector2(0, -1)  # NORTH（含預設）：螢幕上方
```

- [ ] **Step 4: 跑測試確認通過**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gselect=test_mini_map.gd -gexit`
Expected: PASS（2 個 tile_color 測試）。

- [ ] **Step 5: Commit**

```bash
git add presentation/ui/mini_map.gd tests/presentation/test_mini_map.gd
git commit -m "feat(ui): MiniMap 角落自繪小地圖（色塊 tile + 玩家朝向三角形）

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: 接線 main.gd（揭示 + 顯示 + 切圖/讀檔 refresh）

**Files:**
- Modify: `presentation/world/main.gd`

**Interfaces:**
- Consumes: `MiniMap`（Task 4）、`GameState.mark_explored`（Task 1）。
- Produces: 遊戲執行時右上角顯示小地圖，行走揭示迷霧，切圖/讀檔正確換圖並保留各圖進度。
- 註：main.gd 為整體場景接線，沿用專案慣例不寫單元測試；以全套測試綠 + headless 啟動 + 手動 `./run.sh` 驗收。

- [ ] **Step 1: 宣告欄位**

In `presentation/world/main.gd`, after `var _spell_menu: SpellMenu` (line 28) add:

```gdscript
var _mini_map: MiniMap
```

- [ ] **Step 2: `_ready` 建 MiniMap 並首次揭示**

In `_ready`, after the player-signal hookups block (after line 46 `_player.edge_exit_attempted.connect(_on_edge_exit_attempted)`) add:

```gdscript
	_mini_map = MiniMap.new()
	add_child(_mini_map)
	_mini_map.setup(_player)
```

Then at the end of `_ready`, after `GameState.player_facing = map.start_facing` (line 73) add:

```gdscript
	GameState.mark_explored(START_MAP_ID, map.start_pos, map.width, map.height)
	_mini_map.refresh()
```

(`_mini_map.setup` 在 main 的 `entered_cell` 連線之後才連，確保走格時 main 先標記、MiniMap 後重畫。)

- [ ] **Step 3: 走格揭示**

In `_on_entered_cell`, after `GameState.player_pos = pos` (line 94) add:

```gdscript
	GameState.mark_explored(GameState.current_map_id, pos, MapManager.current_map.width, MapManager.current_map.height)
```

- [ ] **Step 4: link 切圖後揭示 + refresh**

In `_enter_via_link`, after `GameState.player_facing = facing` (line 140) add:

```gdscript
	GameState.mark_explored(map_id, pos, MapManager.current_map.width, MapManager.current_map.height)
	_mini_map.refresh()
```

- [ ] **Step 5: 邊緣無縫切圖後揭示 + refresh**

In `_on_edge_exit_attempted`, after `GameState.player_pos = cell` (line 164) add:

```gdscript
	GameState.mark_explored(neighbor_id, cell, MapManager.current_map.width, MapManager.current_map.height)
	_mini_map.refresh()
```

- [ ] **Step 6: 讀檔後揭示 + refresh**

In `_on_loaded`, after `_player.setup(...)` (line 285) add:

```gdscript
	GameState.mark_explored(GameState.current_map_id, GameState.player_pos, MapManager.current_map.width, MapManager.current_map.height)
	_mini_map.refresh()
```

- [ ] **Step 7: 全套測試 + headless 啟動**

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . -s addons/gut/gut_cmdln.gd -gexit`
Expected: 全綠、無 fail/error（現況 302 + 本計畫新增的 explored/serializer/capture-apply/tile_color 測試皆 pass）。

Run: `GODOT="${GODOT:-/Applications/Godot.app/Contents/MacOS/Godot}"; "$GODOT" --headless --path . --quit-after 3`
Expected: 主場景載入無腳本錯誤即離開（boot smoke）。

- [ ] **Step 8: 手動驗收（`./run.sh`）**

Run: `./run.sh`
逐項確認：
- 右上角出現小地圖；開場 `wild_nw` 只見起點 (2,2) 周圍 3×3，其餘留黑（迷霧）。
- 走動時迷霧逐步揭開、走道旁的牆會顯示；玩家紅三角形隨移動/轉向更新位置與朝向（朝北＝指上）。
- 走到 `wild_nw` 的 (3,3) portal 格：探索後該格以綠色標出；踩上去切到 `town_oak`，小地圖換成城鎮地圖。
- 切到鄰圖（東/南野外）再回 `wild_nw`：仍保留先前揭示的格（per-map 記憶）。
- `town_oak` 牆面以深色、地板淺色顯示；若進 `level01`，門/上下階梯探索後分別以黃褐/淺藍/紫顯示。
- Tab 存檔 → 關遊戲 → 重開 → Tab 讀檔：小地圖探索進度完整還原。

- [ ] **Step 9: Commit**

```bash
git add presentation/world/main.gd
git commit -m "feat(world): main 接線 MiniMap + 走格/切圖/讀檔揭示迷霧

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## 完成後

- 全套測試綠 + `./run.sh` 手動驗收通過後，依 `superpowers:finishing-a-development-branch` 決定 ff-merge 到 `main`（沿用本專案慣例）。
- 更新記憶（MEMORY.md / mm3-blobber-build-status.md）：M9 迷你地圖完成、save schema 升 v4、新增 `presentation/ui/mini_map.gd`。
- 提醒：M8b-3 寶箱屆時若再加存檔欄位，沿用同一 v4「missing→default」模式追加即可，不需再升版。
